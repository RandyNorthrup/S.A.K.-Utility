// S.A.K. Utility -- cppcheck suppressions
// Only fundamental tool limitations. All real findings fixed in code.

// cppcheck does not have Qt/system headers available.
missingInclude
missingIncludeSystem

// Single-file analysis cannot determine cross-TU function/member usage.
unusedFunction
unusedStructMember

// Meta-noise from the suppression file itself.
unmatchedSuppression

// cppcheck does not understand Qt macros even with -D defines.
unknownMacro

// Style suggestion to use STL algorithms; Qt-idiomatic loops are preferred.
useStlAlgorithm

// Qt Test slots are invoked by the meta-object system and cannot be static. Scoped to tests/ ONLY
// -- a global silence hid 114 production "can be static" findings. All were resolved: every pure
// helper across the HFS/APFS/ext readers, parsers, preset builders, and stateless validators was
// made static (163 methods over 5 cascade rounds, R5-G3/G5). Production reports 0.
functionStatic:*tests*

// Test assertions intentionally verify enum distinctness with known values. Scoped to tests/
// ONLY -- a global silence hid real always-true/false logic findings in production code (R5-G5
// audit surfaced 9; the genuine false positives now carry inline // cppcheck-suppress with a
// justification, and two fail-open questions in the raw-filesystem writers are logged for review).
knownConditionTrueFalse:*tests*
