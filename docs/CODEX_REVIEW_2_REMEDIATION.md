# CODEX REVIEW 2 -- Full-codebase remediation (2026-08-02)

Second independent Codex (gpt-5.6-sol xhigh) read-only review of the whole
codebase (src/ + include/, ~290k lines), split into 7 area passes. Every finding
was verified against the current tree by a separate Claude checker and classified
CONFIRMED / PARTIAL / ALREADY_FIXED / BY_DESIGN / FALSE_POSITIVE. Only CONFIRMED
and PARTIAL items are listed here.

Total open: 195 confirmed defects; 69 are fallbacks / fail-open (see [[no-fallbacks-fail-closed]] -- fail closed, surface the error).

Legend: [FB] = fallback / fail-open. FIX: = intended remediation direction.

## CRIT (9)

- [ ] [FB] src/ai/ai_tool_policy.cpp:216 -- ReadOnlyPc allows shell commands via fail-open regex blacklist. FIX: Read-only shell must be an allowlist of known-diagnostic commands (or block shell entirely), not a mutation blacklist; the code even comments 'a blacklist is fail-open'.
- [ ] src/ai/ai_tool_policy.cpp:405 -- sak_app_action bypasses agent policy ceiling and takes no lease. FIX: Gate app actions against the effective (clamped) policy and require a mutation lease for destructive/catastrophic descriptors, not just the session-mode confirm.
- [ ] src/core/chocolatey_manager.cpp:96 -- verifyIntegrity only checks existence + `--version` runs. FIX: Pin an expected SHA-256 (or Authenticode signature) of the bundled choco.exe and verify it before use.
- [ ] src/core/cleanup_worker.cpp:333 -- Recycle uses pathname after verification handle already closed. FIX: Recycle inherently takes a path; re-verify by-handle immediately before the shell call or gate recycle behind the same handle-pinned check used for permanent delete
- [ ] [FB] src/core/cleanup_worker.cpp:645 -- Reboot-scheduled delete stores unpinned pathname, counted as success. FIX: No Win32 API pins a reboot delete; document as inherent OR require the reboot-pending path to be under a non-swappable verified root and stop counting it as unconditional success
- [x] [FB] src/core/flash_coordinator.cpp:32 -- OS-disk guard returns safe on GetWindowsDirectoryW/volume-open/extent-query failure -> raw write allowed when protection cannot be established. FIX: Fail closed: refuse write when OS-disk identity cannot be established. DONE: physicalDriveOsDiskCheck now tri-state (NotSystem/IsSystem/Undetermined); validateSingleTarget refuses on IsSystem OR Undetermined, and also refuses when the PhysicalDrive number cannot be parsed (was fail-open). test_flash_coordinator green.
- [ ] src/core/package_internalization_engine.cpp:447 -- Source .nupkg downloaded/extracted/repacked without signature verification. FIX: Verify the .nupkg author/repository signature (or a pinned hash) after download before extracting.
- [ ] [FB] src/core/package_internalization_engine.cpp:639 -- Missing installer checksum treated as verified. FIX: When no script checksum exists, either fail closed or verify against an independent trusted source; do not treat 'no checksum' as verified.
- [x] src/core/partition_apfs_writer.cpp:19181 -- Raw APFS format/mutation has NO is_system/is_boot/PhysicalDrive0 guard at writer+CLI layer (GUI mitigated by PartitionSafetyValidator); a confirmed CLI path typo can format PhysicalDrive0. FIX: Add is_system/is_boot/PhysicalDrive0 guard in appendRawFormatTargetBlockers + appendRawTargetMutationBlockers (defense in depth). DONE: guard added at the actual bypass -- the sak_apfs_writer_cli entry (rawTargetProtectedDiskRefusal): any confirmed --allow-raw command whose target is \\.\PhysicalDrive0 or backs the OS system volume (or whose PhysicalDrive identity cannot be established) is refused before dispatch. The cross-platform writer stays free of Win32; the GUI path keeps the authoritative PartitionSafetyValidator guard. NOTE: covers whole-disk \\.\PhysicalDriveN targets (the reported risk); partition-level raw paths still rely on the upstream guard. test_sak_apfs_writer_cli green.

## HIGH (90)

- [ ] include/sak/ai/ai_lease_manager.h:29 -- Lease TTL 9000s shorter than 14400s app-action timeout. FIX: Raise TTL above the true max hold (>=14400s + margin) or derive TTL from the op's own timeout; the '> 7200s max op' rationale is stale.
- [ ] include/sak/leftover_cleanup_guard.h:162 -- Critical registry roots blocked exact-only; brick-critical descendants pass. FIX: Add these launch/logon-critical subtrees (exefile\shell, ...\ProfileList, etc.) to cleanupDeniedRegistryPrefixes (prefix block)
- [ ] include/sak/leftover_scan_provenance.h:112 -- Provenance binds only normalized type/path text, not real object identity. FIX: Bind file ID (volume+FILE_ID)/key last-write + scan generation into the provenance key, or re-stat/re-verify identity at delete time
- [ ] src/ai/ai_tool_policy.cpp:128 -- Substring 'install ' treated as explicit consent for package mutation. FIX: Require imperative/directed intent (or an explicit confirm) rather than bare substring presence; substring matching is documented as unable to distinguish question from command.
- [ ] src/core/advanced_uninstall_controller.cpp:523 -- Batch uninstall never sets Uninstalling state (stays Idle). FIX: setState(State::Uninstalling) at start of batch (and keep it through processNextQueueItem) until finishBatchUninstall sets Idle
- [ ] src/core/app_mutating_actions.cpp:2127 -- Model-supplied technician_override bypasses proof-of-scan. FIX: Bind override to a separate human/technician-authenticated control (out-of-band confirm), not a model-controllable JSON flag
- [ ] src/core/app_mutating_actions.cpp:3394 -- Generic recycle op lacks provenance and protected/shared-root screening. FIX: Route the canonical path through filePathDeletionRefusal (and consider provenance) before recycling
- [ ] src/core/app_mutating_actions.cpp:3467 -- Recycle op deletes by pathname after canonical/reparse checks. FIX: Re-verify by-handle final path immediately before the shell op, mirroring cleanupHandleRedirectRefusal
- [ ] src/core/app_scanner.cpp:229 -- AppX scan invokes bare powershell.exe (search-order hijack). FIX: Invoke PowerShell by absolute path (e.g. %SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe) or enable safe process search.
- [x] src/core/bandwidth_tester.cpp:252 -- Async firewall add/remove race can leave a permanent inbound rule. FIX: Serialize the netsh add/remove (await add completion before allowing remove, or remove-after-add-finished), or make removal retry until the named rule is gone. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/bandwidth_tester.cpp:567 -- Privileged firewall changes invoke bare `netsh` (PATH/CWD hijack). FIX: Invoke the absolute path %SystemRoot%\System32\netsh.exe (as wifi connect path does for netsh.exe via full resolution). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/bandwidth_tester.cpp:567 -- iperf firewall rule allows all profiles/programs/remote addresses. FIX: Scope the rule: profile=private (exclude public), remoteip=LocalSubnet, and program=<iperf3 path>. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/chocolatey_manager.cpp:122 -- allow_unofficial unenforced; install pins no approved source. FIX: Honor allow_unofficial (e.g. reject/annotate) and pin --source to an approved feed in buildInstallArgs.
- [ ] [FB] src/core/cleanup_worker.cpp:146 -- Fixed-drive + 4GiB heuristic cannot prove recycle recoverability. FIX: Query the volume's actual Recycle Bin state/quota (or verify the item landed in the bin) rather than assuming from drive type + size cap
- [ ] [FB] src/core/cleanup_worker.cpp:315 -- Failed by-handle delete falls back to string QFile::remove. FIX: On NotOpenable re-run the handle-verified open/redirect check (or refuse) instead of a bare string unlink
- [ ] src/core/cleanup_worker.cpp:677 -- RegDeleteTreeW deletes unverified registry pathname. FIX: Open the key with REG_OPTION_OPEN_LINK to detect/refuse symbolic-link keys and verify identity before RegDeleteTreeW
- [ ] src/core/cleanup_worker.cpp:753 -- Firewall cleanup deletes every rule sharing the display name. FIX: Narrow the delete by adding dir=/profile=/program= qualifiers captured at scan time to target the specific rule
- [x] src/core/disk_benchmark_worker.cpp:481 -- Test file reopened by path with CREATE_ALWAYS (reparse race). FIX: Keep the CREATE_NEW handle open for the whole run and reuse it, or reopen with FILE_FLAG_OPEN_REPARSE_POINT + verify file identity; avoid CREATE_ALWAYS on a guessable path. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/drive_scanner.cpp:710 -- hasWindowsIndicators requires a Windows dir; split-boot ESP-only disk classified non-system -> boot disk exposed and erasable. FIX: Classify ESP/bootmgr disk as system even without a Windows dir
- [x] [FB] src/core/drive_unmounter.cpp:88 -- Empty volume list returns true without preventAutoMount -> blank target never taken offline, Windows auto-mounts mid-flash. FIX: Set OFFLINE attribute before flashing even when no volumes present. DONE: empty-volume branch now calls preventAutoMount and fails closed if it can't.
- [x] [FB] src/core/drive_unmounter.cpp:197 -- queryVolumeDriveNumber -1 silently skips volume; FindNextVolumeW loop never checks GetLastError -> mounted target volume missed while enumerationOk stays true. FIX: Treat -1 probe as fatal; verify ERROR_NO_MORE_FILES after loop else fail closed. DONE: queryVolumeDriveNumber now returns kVolumeProbeFailed(-2) for an openable-but-unprobeable volume (fail closed) vs kVolumeUnopenable(-1) for no-media (safely skipped, avoids over-blocking empty card readers); loop now fails closed unless GetLastError()==ERROR_NO_MORE_FILES.
- [x] src/core/elevated_pipe_server.cpp:52 -- Server pipe omits FILE_FLAG_FIRST_PIPE_INSTANCE. FIX: Add FILE_FLAG_FIRST_PIPE_INSTANCE so helper startup fails if the name already exists. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/elevation_broker.cpp:255 -- Broker never verifies pipe server PID/image. FIX: After connect, GetNamedPipeServerProcessId and verify it equals the helper process launched (m_helper_process PID) and its image path/signature; fail closed otherwise. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [PARTIAL] src/core/elevation_broker.cpp:372 -- Pipe HANDLE not lifetime-pinned in lock-free readers. FIX: Take m_send_mutex around the load+ReadFile/PeekNamedPipe in readExact/peekAvailable (or use a duplicated/refcounted handle) so the close cannot land mid-I/O. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_inspector_controller.cpp:157 -- Navigation methods have no state guard and race the parser QFile against a running search/export. FIX: Reject navigation while a background op is active (guard on m_state) or serialize all parser access behind a mutex. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/email_profile_manager.cpp:347 -- Manifest-write failure only logged before backupComplete emitted. FIX: On manifest failure emit errorOccurred and do not emit backupComplete (fail closed). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/email_profile_manager.cpp:371 -- Registry-export failure only logged; manifest still references missing .reg and backup reports complete. FIX: Propagate exportRegistryKey failure out of backupSingleProfile; count it as a backup failure and omit the registry_file from the manifest (or fail the whole backup). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/firewall_rule_auditor.cpp:481 -- Rules failing COM QueryInterface are silently dropped while audit stays clean. FIX: On extraction failure flag the audit incomplete (or set m_enumerationOk=false / emit errorOccurred) so a dropped rule cannot present as a clean audit. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/firewall_rule_auditor.cpp:639 -- Empty LocalPorts (== all ports) not treated as wildcard in RDP/SMB gap checks. FIX: In the gap checks treat localPorts.isEmpty() as wildcard exactly like '*' (mirror portsOverlap). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/image_source.cpp:323 -- CompressedImageSource never sets uncompressedSize -> size() returns compressed size -> capacity gate approves oversized decompressed stream. FIX: Populate uncompressedSize or fail the gate when unknown
- [x] src/core/keep_awake.cpp:23 -- Refcount ignores later flags and is thread-affine. FIX: Track the union of requested flags and re-apply SetThreadExecutionState on every change from a single owning thread (or use a dedicated power-request thread). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_parser.cpp:479 -- splitMimeParts split('\n') on up-to-100MB body amplifies memory. FIX: Stream/scan for boundary delimiters without materializing a per-line QList, or bound part count/size. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_writer.cpp:116 -- Combined MBOX output is a fixed mailbox.mbox opened Truncate; multiple jobs/files in one output dir overwrite each other. FIX: Namespace each source file's mbox output (per-file subdir or source-derived basename) so distinct jobs never share mailbox.mbox. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_writer.cpp:220 -- Untrusted sender written raw into the From_ separator line (CR/LF unescaped). FIX: Sanitize sender (strip/replace CR/LF and controls) before writing the From_ line, same as header values. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/memory_benchmark_worker.cpp:288 -- Latency alloc failure returns 0.0 -> inflated score. FIX: Return std::expected/error from runRandomLatency on alloc failure and fail the benchmark instead of scoring a 0ns latency. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/network_transfer_runner.cpp:137 -- Response size cap defaults to unlimited; in-scope callers omit it. FIX: Give max_response_bytes a sane non-zero default (or require callers to set it) and set an explicit cap in the bandwidth_tester speed-test requests. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/nuget_version_range.cpp:214 -- Unparseable version ranges silently become unrestricted/permissive. FIX: Add a validity flag; a range that fails to parse must reject all versions (satisfies=false) instead of matching everything.
- [ ] [FB] src/core/offline_deployment_worker.cpp:331 -- Dependency-resolution errors become warnings; bundle builds anyway. FIX: Treat any resolver error as fatal for a self-contained Bundle: abort the build (or exclude+report) rather than logging a warning and continuing.
- [ ] [FB] src/core/offline_deployment_worker.cpp:978 -- Bundle verify passes when size and checksum are both absent. FIX: Require both size_bytes>0 and a non-empty checksum (fail closed) for any Bundle-mode entry before install.
- [ ] src/core/offline_deployment_worker.cpp:1203 -- Predictable direct-download temp dir removeRecursively'd without ownership check. FIX: Create the temp dir under a unique/owned path (QTemporaryDir or ownership marker) and refuse to delete a pre-existing foreign dir.
- [ ] src/core/offline_deployment_worker.cpp:1391 -- Direct-download filename uniqueness resets per package; cross-package overwrite. FIX: Hoist used_names to executeDirectDownload scope (span all packages), and disambiguate copyEmbeddedInstallers dest names too.
- [ ] src/core/offline_deployment_worker.cpp:1404 -- Direct-download stores installers without checksum validation. FIX: Carry resource.checksum/checksum_type into the direct-download path and verify each downloaded installer before counting it.
- [x] [FB] src/core/ost_conversion_worker.cpp:372 -- ImapUpload counts every item converted but nothing is uploaded. FIX: Either wire a real controller-side upload of the produced messages using ImapUploader, or fail closed / remove ImapUpload from isOutputFormatSupported until implemented. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/ost_conversion_worker.cpp:534 -- collectAttachments logs dropped attachments but item still counts as converted. FIX: Treat an attachment read failure as an item failure (or gate on a strict-mode flag), so incomplete messages do not count as converted. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/ost_converter_controller.cpp:119 -- Concurrent/sequential MBOX jobs collide on <outdir>/mailbox.mbox (truncate). FIX: Derive a per-source unique mbox filename (or per-job output subdir) instead of the fixed mailbox.mbox. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/ost_converter_controller.cpp:295 -- Job marked Complete whenever >=1 item converted, despite failures/errors. FIX: Add a Partial/Failed classification when result.items_failed>0 or errors non-empty even if some items converted. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/package_internalization_engine.cpp:49 -- Binary filename from URL not reduced to safe basename. FIX: Run the basename through a safeInstallerFilename-style guard (reject '.', '..', '/', '\\', empty; fall back to binary_N).
- [ ] [PARTIAL] src/core/package_internalization_engine.cpp:241 -- Network-download detection omits curl/wget and dynamic URLs. FIX: Also flag curl/wget/Start-Process download invocations and variable-built URLs (or detect scheme concatenation) to close the dynamic-URL gap.
- [ ] src/core/package_internalization_engine.cpp:691 -- Installer URL schemes unrestricted (file://, ftp://, UNC). FIX: Reject any resource URL whose scheme is not https (or an approved allowlist) before download, in both internalization and direct-download paths.
- [ ] src/core/package_internalization_engine.cpp:810 -- Nupkg extracted with no decompressed-size/entry-count limit (zip bomb). FIX: Enumerate entries and enforce max decompressed size and entry count before/while extracting; abort past the cap.
- [ ] [FB] src/core/partition_apfs_writer.cpp:12709 -- Raw APFS resize: deviceSize<=0 (unknown; GetFileSizeEx fails on a physical-drive handle) treated as sufficient -> container claims capacity beyond physical target. FIX: Fail closed on unknown size; query real device length
- [ ] [FB] src/core/partition_apfs_writer.cpp:20822 -- Raw APFS format returns ok without any read-back; warning claims detection/root-listing verification that never runs. FIX: Real post-write APFS detect+root-listing readback for raw targets, or drop the claim and fail closed
- [ ] [FB] src/core/partition_apfs_writer.cpp:20990 -- Opened-size==requested check gated behind !isWindowsRawDevicePath -> raw targets skip it; raw blockers validate only requested size not device length -> overstated container zeroes device start then fails at tail. FIX: IOCTL_DISK_GET_LENGTH_INFO device-size check before any raw write
- [ ] [FB] src/core/partition_raw_device_io.cpp:96 -- FlushFileBuffers failure ignored in close() (void return) -> durable-flush failure swallowed and op reports success (QFile flush at apfs_writer:21002 also unchecked). FIX: Checked sync()/flush returning bool; surface the error before reporting success
- [ ] [FB] src/core/partition_safety_validator.cpp:955 -- Missing clone sizes treated as safe; zero-length mode uses unbounded CopyTo (script_builder:2174); only RestoreImage has a missing-size blocker -> Clone/Migrate with 0 sizes pass. FIX: Require known source+target sizes for Clone/Migrate; abort if size cannot be established
- [ ] src/core/partition_safety_validator.cpp:2038 -- Unallocated create blocks only is_system, not is_boot; blocksCurrentOsDiskMutation not invoked for the Unallocated kind -> create partition on running is_boot disk unblocked (split-boot). FIX: Block disk.is_system || disk.is_boot
- [ ] src/core/permission_manager.cpp:134 -- Elevated ACL/ownership changes follow path by name (no reparse/handle check). FIX: Open a verified no-follow handle and use SetSecurityInfo(SE_KERNEL_OBJECT) on that handle, or screen reparse points/verify identity before the name-based call
- [x] [FB] src/core/process_runner.cpp:67 -- Cancellation fire-and-forgets detached taskkill, unchecked. FIX: Check startDetached's return and fall back to proc->kill(); prefer wrapping every child in a Job object with KILL_ON_JOB_CLOSE and await termination before reporting cancelled. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/pst_parser.cpp:1321 -- NBT/BBT loaders have depth cap but no visited-page set (fan-out bomb). FIX: Thread a shared visited-page-offset QSet through loadNodeBTree/loadBlockBTree and fail closed on a repeat, mirroring readDataTreeGuarded. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/pst_parser.cpp:1544 -- Data-tree assembly stores cumulative offsets as int (overflow > INT_MAX). FIX: Use qsizetype/int64 for offsets and cap total assembled data-tree size, failing closed above a sane limit. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/pst_parser.cpp:2082 -- TC cell bound checks whole row matrix size, not current row end. FIX: Clamp/validate cell reads against row_view.row_off + row_view.row_size rather than row_data.size(). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/pst_parser.cpp:2512 -- loadChildFolders silently skips child folders that fail to parse. FIX: Surface/aggregate child-folder failures (record a partial-tree warning or fail closed) rather than dropping them silently. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/pst_parser.cpp:2586 -- readMessage discards readAttachments() failure, returns message attachment-free. FIX: Propagate the readAttachments error (return std::unexpected) or at minimum flag the detail as incomplete instead of swallowing it. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/stress_test_worker.cpp:511 -- Disk stress re-creates claimed path with CREATE_ALWAYS each loop. FIX: Hold one handle open for the run (open/truncate via that handle), or reopen with FILE_FLAG_OPEN_REPARSE_POINT and verify identity before writing. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/uninstall_worker.cpp:209 -- Cancel during leftover scan still emits uninstallComplete then cancelled. FIX: Add checkStop() after scanLeftovers() and return operation_cancelled before emitting the terminal uninstallComplete
- [ ] src/core/user_data_manager.cpp:431 -- Public deleteBackup recursively deletes any supplied directory. FIX: Validate backup_path is inside the managed backup root, has matching metadata, and is not a root/UNC/reparse before recursive delete
- [x] [FB] src/core/vulnerability_scanner.cpp:648 -- Network failure returns arbitrarily stale cache as a synthesized HTTP 200 success. FIX: On transport failure surface the error/append a sourceError instead of returning stale cache as success, or mark the result stale so transferOk/callers do not treat it as authoritative. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/vulnerability_scanner.cpp:1034 -- Empty body and valid-JSON-wrong-schema accepted as a clean zero-result scan. FIX: Require the expected top-level schema key (e.g. 'vulnerabilities'/'results') before accepting; treat empty/absent-schema HTTP 200 as an incomplete source, not a clean result. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/wifi_setup_script.cpp:133 -- Generated elevated .cmd invokes bare powershell/netsh (planted-exe hijack). FIX: Emit fully-qualified paths (%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe and %SystemRoot%\System32\netsh.exe) in the generated script. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/windows_usb_creator.cpp:53 -- diskpart clean/format runs before extraction capacity check -> undersized disk destroyed before failure. FIX: Check ISO/extracted size vs disk capacity before any destructive diskpart op. DONE: guardTargetDiskSafe now also returns the disk Size; validateUSBInputs refuses when the ISO does not fit (or capacity is unknown) BEFORE STEP 1's DiskPart clean.
- [x] [FB] src/core/windows_usb_creator.cpp:149 -- guardTargetDiskSafe ignores exit_code; missing bool field -> false; only literal True matches -> malformed output passes the guard. FIX: Require exit_code==0 AND 3 parseable boolean fields, else fail closed. DONE: guard now requires completedSuccessfully() (exit 0, not timed out/cancelled), exactly 4 fields, and each of IsBoot/IsSystem/IsReadOnly to parse as explicit True/False; anything else fails closed.
- [x] src/elevated/elevated_helper_main.cpp:404 -- Elevated helper launches unqualified powershell.exe. FIX: Resolve powershell.exe to an absolute system path (e.g. %SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe) before launching. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/gui/ai_assistant_panel.cpp:3201 -- Destructor drains async tool runner with no deadline. FIX: Bound the drain with a deadline and force-detach/abandon (or hard-cancel the inner op) once exceeded; do not spin indefinitely.
- [ ] src/gui/ai_assistant_panel.cpp:6680 -- Non-batch app-action args truncated to 200 chars before catastrophic confirm. FIX: Render key destructive fields (target/disk/path) explicitly and fully, or refuse when the JSON preview would be truncated, mirroring the batch refusal.
- [ ] src/gui/ai_assistant_panel.cpp:10950 -- Stop does not cancel package/MCP/app-action/win32/local-offline work. FIX: Cancel m_activeToolRunToken and the local offline worker on Stop, and thread the run token into package/MCP/app-action/win32 execution so they abort.
- [x] src/gui/file_management_explorer_panel.cpp:3800 -- Redo of 'Create File' writes empty data over any existing file at the historical path with no collision/identity check. FIX: Refuse (block) when the destination path is occupied by an entry that is not the one the create produced; only recreate when the path is vacant or verified-identical. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/file_management_explorer_panel.cpp:3925 -- Undo-delete verifies only file-vs-directory kind, not identity, so an unrelated same-named entry is recycled/permanently deleted. FIX: Record and re-verify item identity (size/mtime/hash, or a captured id) before deleting on undo; skip + block when identity no longer matches, especially for permanent raw-target deletes. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/file_management_explorer_panel.cpp:3983 -- Undo-delete confirmation shows only an item count, hiding paths, raw-media permanence, and directory-tree scope. FIX: Enumerate target paths, flag permanent (raw-media) vs recycle and recursive directory scope in the confirmation text so the operator can make an informed choice. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/network_diagnostic_panel.cpp:3183 -- Panel teardown joins netsh future but never delivers chained callback, skipping step 2 of a two-step DNS/DHCP mutation. FIX: Make the multi-step netsh sequence a single synchronous joined unit at teardown (or run it as one process/script), or explicitly complete queued continuations before/while joining. DONE: delegated GUI fix wave (commit 2116e66).
- [ ] src/threading/flash_worker.cpp:205 -- NO_BUFFERING with ~16B-aligned QByteArray buffers (not 512/4096); latent, fails on strict 4Kn drivers. FIX: Allocate I/O buffers via VirtualAlloc/_aligned_malloc to the queried sector size
- [x] [FB] src/threading/flash_worker.cpp:241 -- Sector-size query failure falls back to 512 -> 4Kn device substantially overwritten. FIX: Fail closed on geometry query failure. DONE: queryDeviceSectorSize now returns bool, sets m_sectorSize=0 and fails on IOCTL failure; openDevice closes+returns false so the flash aborts. test_flash_worker green.
- [x] [FB] src/threading/flash_worker.cpp:293 -- Failed capacity query -> imageFitsDevice returns true -> oversized image overwrites whole device. FIX: Fail closed on failed capacity query. DONE: imageFitsDevice(deviceBytes<0) now returns false; ensureImageFitsTarget refuses when capacity is unknown. test_flash_worker imageFitsDeviceRejectsUnknownCapacity green.
- [ ] src/threading/flash_worker.cpp:540 -- Full verify hashes only size() (compressed) bytes but compares full decompressed checksum -> compressed flashes always fail verify. FIX: Verify over the decompressed written length, not size()
- [x] src/threading/worker_base.cpp:32 -- stopAndJoin uses QThread::terminate() and ignores 2nd wait. FIX: Log/abort if the post-terminate wait fails rather than proceeding; prefer guaranteeing cooperative stop so terminate() is never needed. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/tools/sak_apfs_writer_cli.cpp:246 -- --output-json alias guard compares path strings, not file identity. FIX: Compare canonicalFilePath() and/or OS file identity (GetFileInformationByHandle nFileIndex/volume) so symlink+hardlink aliases are detected.
- [x] src/tools/sak_hfs_writer_cli.cpp:85 -- HFS report alias guard misses symlink/hardlink aliases. FIX: Use canonicalFilePath()/OS file-identity comparison instead of cleaned-string equality. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/win32mcp/browser_contract.cpp:459 -- copyArg silently coerces wrong-typed int/number args to 0. FIX: Validate the JSON value type against spec.type in copyArg and return an error on mismatch instead of coercing to the default.
- [x] src/win32mcp/win32_mcp_dispatch.cpp:181 -- Secret redaction misses quoted JSON like "token":"secret". FIX: Extend the regex to tolerate an optional closing quote before the separator and a quoted value, or redact structurally by walking the parsed JSON rather than regex over text. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/win32mcp/win32_mcp_input.cpp:170 -- activateTarget swallows lookup/activation errors and input proceeds. FIX: Surface the activation/lookup error (like focus_window already does) and abort the click/type when the requested target could not be raised, instead of silently proceeding. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/win32mcp/win32_mcp_input.cpp:194 -- mouse_click coerces non-numeric x/y to 0 -> (0,0) click. FIX: Reject when x/y are not JSON numbers (check isDouble()) before coercion, returning an error rather than defaulting to 0. DONE: delegated fail-closed fix wave (commit 9aa7689).

## MED (78)

- [ ] include/sak/email_types.h:338 -- recurse_subfolders, csv_include_header, flatten_attachments, eml_include_headers are never consumed. FIX: Consume each flag at its export site (or remove the flags if the behavior is fixed) so requested options take effect.
- [ ] include/sak/partition_hfs_internal.h:9994 -- HFS volume geometry never cross-checked vs backing device length -> truncated/over-claimed volume partially mutated before end-of-device failure. FIX: Preflight reject total_blocks*block_size > device size
- [ ] include/sak/uninstall_worker.h:31 -- ForcedUninstall doc says 'remove all traces' but only scans/reports Skipped. FIX: Correct the doc/name to reflect scan-and-report-only behavior (or implement the removal the name promises)
- [ ] src/ai/ai_command_tool_planner.cpp:29 -- Process confirm preview joins argv on spaces without quoting. FIX: Quote/escape each argv element (and make control chars visible) when building the confirmation preview.
- [ ] src/ai/ai_mcp_session_pool.cpp:30 -- Pooled MCP session key excludes timeout_ms; later calls inherit first timeout. FIX: Include timeout_ms in the session key (or apply per-call timeout to the pooled session) so timeout is not silently stale.
- [ ] [FB] src/ai/ai_package_tool_planner.cpp:16 -- Invalid package id silently rewritten by deleting characters. FIX: Validate and reject ids containing disallowed characters (fail closed) rather than silently rewriting to a substitute token.
- [ ] src/ai/ai_subagent_runner.cpp:365 -- Subagent wall-clock deadline not enforced mid-call; late success returned. FIX: Propagate the deadline into model/tool calls (bound the HTTP/process wait) and demote any result produced after deadline expiry to TimedOut.
- [ ] src/core/app_installation_worker.cpp:34 -- QVersionNumber ignores SemVer prerelease in already-installed check. FIX: Parse with NuGetVersion (prerelease-aware) and compare, so a prerelease does not satisfy a stable request.
- [x] src/core/bandwidth_tester.cpp:297 -- Server errorOccurred handler acts on m_serverProcess instead of sender(). FIX: Mirror the finished-handler: capture the specific process (or compare sender()) and act only when it equals m_serverProcess. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/config_manager.cpp:185 -- Typed getters don't enforce setter invariants. FIX: Re-validate and clamp in the getters (mirror the setter invariants, falling back to the default when the stored value is out of range). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/cpu_benchmark_worker.cpp:313 -- Zlib failure returns elapsed time instead of error. FIX: Return an error (std::expected) on non-Z_OK and fail the CPU benchmark instead of scoring the elapsed time of a failed compression. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/disk_benchmark_worker.cpp:446 -- I/O failures reduced to partial/zero, benchmark still succeeds. FIX: Propagate an error_code when I/O ops fail (or when zero ops completed) so genuine disk errors fail the benchmark rather than becoming a measurement. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/disk_benchmark_worker.cpp:564 -- Unvalidated test_file_size_mb underflows max_offset at 0. FIX: Validate test_file_size_mb >= a sane minimum (>= a few MB) in execute() before any offset arithmetic and fail closed on 0. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/drive_unmounter.cpp:116 -- Success path never calls allowAutoMount -> completed media left persistently offline. FIX: Re-online the disk after flash+verify completes
- [ ] src/core/drive_unmounter.cpp:257 -- FindFirstVolumeMountPointW lists child mounts, not the target's own -> target drive-letter mount not removed. FIX: Use GetVolumePathNamesForVolumeNameW + DeleteVolumeMountPointW
- [x] src/core/elevated_pipe_server.cpp:340 -- Client identity is PID-only comparison. FIX: Bind identity to more than PID: verify the client token/image path/process-creation-time, or use an authenticated handshake nonce over the pipe. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/email_export_worker.cpp:628 -- ICS and CSV collection silently drop readItemDetail failures, report remaining as success. FIX: On readItemDetail failure append to result.errors and increment items_failed in both the ICS and CSV collection loops. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/email_export_worker.cpp:1059 -- CSV and plain-text QTextStream writers never check stream status; disk-write failure returns success. FIX: Flush and check stream.status()==Ok and QFile error before returning true (or write via writeFully on a QByteArray). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/email_inspector_controller.cpp:358 -- Report reported generated when only one of HTML/JSON writes succeeds. FIX: Require both writes to succeed (AND, not OR) before emitting reportGenerated; surface which output failed. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_profile_manager.cpp:369 -- Registry profile name concatenated into backup .reg path unsanitized. FIX: Sanitize profile_name to a bare basename (reuse sanitizeFolderName-style stripping) before building reg_file, matching the confinement already applied on restore. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_profile_manager.cpp:432 -- Untrusted backup manifest read fully (readAll) with no size cap before JSON parse. FIX: Cap manifest size (stat before read, reject over a sane limit) before readAll/parse. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_profile_manager.cpp:506 -- Restore backup-source confinement is name-only (lexical), not symlink-resolved. FIX: Resolve the source via realCanonicalPath and require it to physically reside inside the backup dir (as done for the destination root). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_profile_manager.cpp:838 -- Registry key allowlist uses substring contains(\Outlook\Profiles). FIX: Require the segment boundary (e.g. match '\\OUTLOOK\\PROFILES' followed by end or '\\'), not a bare substring. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_search_worker.cpp:440 -- MBOX search never evaluates recipient or attachment-name criteria. FIX: Add recipient (to/cc/bcc via readMessageDetail) and attachment-name matchers to the MBOX path mirroring the PST matchers. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/flash_coordinator.cpp:649 -- Timed-out worker release()+setParent(null) with no deleteLater -> worker/QThread leak on repeated wedge. FIX: Connect finished->deleteLater before releasing the detached worker
- [x] [FB] src/core/html_email_writer.cpp:189 -- Attachment-save failure only logged; writeMessage still returns success. FIX: Return failure from saveFileAttachments and have writeMessage fail (or record partial) when any listed attachment does not save. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/image_source.cpp:398 -- isCompressed set {gz,bz2,xz,zip} contradicts DecompressorFactory {gz,gzip,bz2,bzip2,xz,lzma,magic; NOT zip} -> .gzip/.bzip2/.lzma written raw; .zip rejected. FIX: Delegate isCompressed to DecompressorFactory (single source of truth)
- [ ] [FB] src/core/leftover_scanner.cpp:704 -- Firewall parse hard-codes English 'Rule Name:'; localized output yields empty+reliable. FIX: Enumerate firewall rules via the COM API (INetFwPolicy2) or a locale-neutral parse, or mark the phase unreliable when the expected header is absent
- [ ] [FB] src/core/leftover_scanner.cpp:753 -- Fixed Run-value buffers skip ERROR_MORE_DATA entries but phase stays reliable. FIX: Retry with a grown buffer on ERROR_MORE_DATA, or mark the startup phase unreliable when a value is skipped
- [x] src/core/mbox_parser.cpp:215 -- Summary has_attachments only detects multipart/mixed. FIX: Broaden the summary heuristic (multipart/*, Content-Disposition attachment, non-text single part) to match parseMimeMessage. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_parser.cpp:595 -- filename regex [^"\;\s]+ truncates quoted filenames containing spaces. FIX: Parse quoted-string values properly (allow spaces inside quotes), e.g. filename\s*=\s*"([^"]*)"|filename\s*=\s*([^;\s]+). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_parser.cpp:760 -- MIME parse / attachment decode never check cancellation. FIX: Check m_cancelled at part boundaries / decode loops in the MIME parsing path. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/mbox_parser.cpp:780 -- Base64 decode uses Qt permissive default, malformed input yields partial bytes as valid. FIX: Use QByteArray::fromBase64Encoding with AbortOnBase64DecodingErrors and fail the attachment on error. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_parser.cpp:855 -- Any unescaped 'From ' body line becomes a message boundary. FIX: Tighten isFromLine toward the RFC4155 shape (sender + asctime date) to reduce false boundaries; accept it is best-effort for the format. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/mbox_writer.cpp:165 -- Predictable MIME boundary not verified absent from body content. FIX: Generate a random high-entropy boundary and/or verify it does not occur in any part payload before use. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/network_diagnostic_controller.cpp:178 -- Worker cleanup falls back to QThread::terminate() after quit timeout. FIX: Prefer a cooperative cancel flag on the worker and detach-as-safe-leak on timeout instead of terminate(); if kept, document the corruption risk. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/network_diagnostic_controller.cpp:783 -- LAN transfer server binds all interfaces with no auth/client-limit/idle-timeout. FIX: Bind to the intended interface, add a single/accepted-client cap and an idle-timeout that closes silent sockets, and bound speed_samples growth. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/network_share_browser.cpp:192 -- Cancelled enumeration returns ok=true with a truncated share list. FIX: Set ok=false (or a distinct cancelled state) when m_cancelled was observed so callers do not treat a truncated list as complete. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/nuget_dependency_resolver.cpp:170 -- Late diamond constraint only errors post-selection; no reselection. FIX: Record all constraints before selection or add a bounded reselection/backtracking pass rather than only validating after the fact.
- [ ] src/core/nuget_dependency_resolver.cpp:335 -- Dependencies deduped by id only; per-framework entries/ranges dropped. FIX: Merge ranges for repeated ids (intersect) or record each edge's range rather than discarding duplicates by id.
- [ ] src/core/nuget_version_range.cpp:109 -- Qt::SkipEmptyParts accepts malformed prerelease separators. FIX: Split with KeepEmptyParts and fail parse if any prerelease identifier is empty.
- [ ] src/core/offline_deployment_worker.cpp:451 -- Dependency-feed XML fetched with no size cap before DOM parse. FIX: Set request.max_response_bytes to a sane cap (e.g. 32MB) as makeVersionRequest does.
- [ ] [FB] src/core/offline_deployment_worker.cpp:825 -- Dependency cycle members silently appended; install ignores dependencies. FIX: Detect leftover (cyclic) nodes and emit a warning/flag instead of silently appending.
- [ ] src/core/offline_deployment_worker.cpp:1087 -- Only exit 0 accepted; choco reboot codes 1641/3010 reported as failure. FIX: Accept exit codes 0, 1641, 3010 as success (reuse ChocolateyManager::parseExitCode).
- [ ] [FB] src/core/offline_deployment_worker.cpp:1636 -- Missing offline_ready defaults to true, bypassing air-gap skip. FIX: Default missing offline_ready to false (fail closed) so packed_only skips unclassified entries.
- [x] [FB] src/core/ost_conversion_worker.cpp:159 -- Source checksum: open failure silent, cancellation stores prefix-only SHA-256. FIX: Report open/read failures via result.errors/errorOccurred; do not store a checksum when the read was cancelled/incomplete. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/package_internalization_engine.cpp:578 -- Latest-version resolution falls back to feed's last entry (no SemVer sort). FIX: Parse all versions and pick the SemVer-max via NuGetVersion::compare instead of trusting feed order/last-entry.
- [ ] src/core/partition_safety_validator.cpp:1850 -- MigrateOs requires is_system but the OS lives on the is_boot disk -> rejects the real Windows disk and accepts the ESP-only is_system disk on split-boot. FIX: Require is_boot (or is_system || is_boot)
- [ ] src/core/partition_script_builder.cpp:2237 -- Clone target restored online (finally) before the verify script runs -> Windows can mount/modify the disk between copy and compare. FIX: Keep the disk offline through verification; restore online only after verify completes
- [x] [FB] src/core/pst_parser.cpp:1162 -- Unknown PST data version only warns, continues with guessed layout. FIX: Fail closed (pst_invalid_header) on unrecognized wVer instead of guessing a layout. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/pst_parser.cpp:1292 -- B-tree page only checks duplicated page-type byte, not type/BID/CRC. FIX: Validate expected ptype (0x80/0x81), page BID/signature, and CRC in parseBTreePage; fail closed on mismatch. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/pst_parser.cpp:1420 -- readBlock accepts a short read instead of requiring cb bytes. FIX: After readBytes in readBlock, require size()==cb (or >= needed) and return read_error otherwise. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/pst_parser.cpp:1604 -- decompressBlockIf4k returns compressed raw bytes on footer/decompress failure. FIX: Return an error (std::unexpected) on footer-read or decompression failure so readBlock fails closed instead of yielding compressed bytes. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/pst_parser.cpp:1814 -- readPropertyContext ignores subnode B-tree failure, HNID values become empty. FIX: Propagate readSubNodeBTree failure instead of leaving subnode_map empty and continuing. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/registry_snapshot_engine.cpp:113 -- Registry snapshot swallows open/query/enum failures, returns partial set. FIX: Propagate a reliability/ok flag so a failed enumeration is not consumed as an authoritative snapshot
- [x] [FB] src/core/restore_point_manager.cpp:152 -- PowerShell -ErrorAction SilentlyContinue hides query failures as empty. FIX: Drop SilentlyContinue (or surface $Error/exit non-zero on failure) and treat dropped/unparseable rows as queryOk=false so failures don't read as a genuine empty result. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] [FB] src/core/script_rewriter.cpp:57 -- Rewrite reports success even when zero URLs were replaced. FIX: Fail (or flag) when parsed.resources exist but result.replacements is empty; require every expected URL to be replaced.
- [ ] [FB] src/core/uninstall_worker.cpp:108 -- Snapshot failure ignored; registrySnapshotCaptured emitted unconditionally. FIX: Check captureRegistrySnapshot() and surface a warning / degraded state instead of emitting captured unconditionally
- [ ] [FB] src/core/uninstall_worker.cpp:167 -- Restore-point creation failure ignored; destructive uninstall proceeds. FIX: On failed restore-point creation, surface the error and abort (or require explicit user acknowledgement) before destructive steps
- [ ] src/core/uninstall_worker.cpp:274 -- Native uninstaller exit code never copied into report (3010 hidden). FIX: Capture result.exit_code into report.nativeExitCode before emitting nativeUninstallerFinished
- [x] src/core/wifi_setup_script.cpp:45 -- All non-WEP/non-open security mapped to WPA2PSK/AES (WPA3/enterprise wrong). FIX: Detect WPA3/SAE and enterprise/802.1X security strings and emit the correct authentication/encryption/useOneX, or fail closed on unsupported security types. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/gui/ai_assistant_panel.cpp:7129 -- Offline operation blocks on unbounded semaphore acquire. FIX: Use tryAcquire with a bounded deadline (or guarantee a terminal release on every start/failure path) and wire cancellation to abort the wait.
- [ ] src/gui/ai_assistant_panel.cpp:9067 -- Workflow policy request omits user_message, over-blocking package phases. FIX: Set plan->request.user_message = context.user_message in initializeWorkflowToolPlan.
- [x] src/gui/detachable_log_window.cpp:284 -- LogToggleSwitch toggles on ANY mouse button and has no keyboard handling; used for the security-sensitive Images toggle. FIX: Gate the toggle on Qt::LeftButton, set a focus policy, and handle Space/Enter (or subclass QAbstractButton) for keyboard accessibility. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/email_inspector_panel.cpp:2091 -- Remote email image fetch has no per-response size cap and no image-count cap. FIX: Cap the number of concurrent/total image requests and abort replies exceeding a max byte budget (setReadBufferSize + downloadProgress guard). DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/file_explorer_properties_dialog.cpp:116 -- QtConcurrent::run size/hash scans are not cancellable; destructor waitForFinished() freezes GUI on close. FIX: Thread a cancellation flag (QPromise / std::atomic checked inside the loops) so cancel() actually stops the walk/hash, letting waitForFinished() return promptly. DONE: delegated GUI fix wave (commit 2116e66).
- [x] [FB] src/gui/network_diagnostic_panel.cpp:3186 -- Secondary-DNS and DHCP-DNS set failures are logged as warnings then reported as full success. FIX: Propagate the failure: surface an error/partial status to the operator instead of emitting the success statusMessage when the second netsh step fails. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/organizer_panel.cpp:1278 -- Dedup directory summary recursively enumerates every file on the GUI thread. FIX: Compute the summary asynchronously (or show a cheap count and defer sizing), or bound/skip the recursive walk on the UI thread. DONE: delegated GUI fix wave (commit 2116e66).
- [x] src/gui/user_profile_backup_wizard_pages.cpp:1564 -- App-data scan recursively sizes directories synchronously inside a GUI slot. FIX: Move the size scan to a worker thread (QtConcurrent/QThread) and update the tree via a watcher, keeping the GUI responsive. DONE: delegated GUI fix wave (commit 2116e66).
- [x] [FB] src/gui/wifi_manager_panel.cpp:2318 -- Short JSON write to credential table only logs a warning, then UI reports success. FIX: Use QSaveFile with commit() and treat a short write / commit failure as a hard error (warning dialog + no success status), matching the fail-closed rule. DONE: delegated GUI fix wave (commit 2116e66).
- [ ] src/threading/flash_worker.cpp:801 -- Final verify read is a non-sector-multiple on the NO_BUFFERING handle -> ReadFile ERROR_INVALID_PARAMETER -> verify fails for arbitrary-size images. FIX: Read a sector-aligned length, compare only the meaningful prefix
- [x] [FB] src/win32mcp/browser_bridge.cpp:160 -- Stale-ref gate checks only ref, not to_ref (browser_drag). FIX: Include to_ref in the stale-gate check (refuse when ref_index is stale and either ref or to_ref is present). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/win32mcp/browser_extension_installer.cpp:312 -- enumStringValues returns partial list on arbitrary error without signaling failure. FIX: Propagate an enumeration-error flag out of enumStringValues and have callers fail closed (report read error) instead of acting on a truncated list. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/win32mcp/browser_extension_installer.cpp:503 -- Native-host registration failure leaves force-install policy written. FIX: On registerNativeHostKey failure, undo the just-written forcelist entry and generated manifests before returning failure (or perform native-host registration first). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/win32mcp/win32_mcp_desktop.cpp:452 -- UIA ValuePattern text unbounded and names/values not escaped in outline. FIX: Cap node.value with .left(kMaxUiaValueChars) and escape embedded quotes/newlines (or JSON-encode) before composing outline lines. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/win32mcp/win32_mcp_dispatch.cpp:135 -- Read-only allowlist includes browser_focus/hover/reveal. FIX: Drop focus/hover/reveal from the read-only set (keep truly passive inspectors) and keep it in sync with AiProviderGateway::isWin32ReadOnlyTool. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/win32mcp/win32_mcp_entry.cpp:121 -- stdio request read has no line-size ceiling. FIX: Impose a max request-line length and drop/close the stream when exceeded. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/win32mcp/win32_mcp_input.cpp:86 -- Partial SendInput reports failure without releasing injected modifiers/buttons. FIX: On partial delivery, inject the matching key-up/button-up events for whatever was already sent before returning the error. DONE: delegated fail-closed fix wave (commit 9aa7689).

## LOW (18)

- [ ] include/sak/ai_assistant_panel.h:981 -- Resumed-approval id lists consumed and cleared but never populated. FIX: Either populate the lists when a run is resumed with prior approvals, or remove the lists and the resume_supported plumbing that depends on them.
- [ ] include/sak/diagnostic_types.h:254 -- sequential_block_size_kb / random_block_size_kb never read. FIX: Either wire these fields into disk_benchmark_worker's I/O block sizing or remove them (and their default constants) to avoid misleading configuration.
- [x] include/sak/user_profile_backup_wizard.h:465 -- UserProfileBackupExecutePage::m_worker is never assigned or read; execution uses a shadowing local. FIX: Either assign/track the worker via m_worker (for cancel/teardown) or remove the dead member to make ownership explicit. DONE: delegated GUI fix wave (commit 2116e66).
- [ ] src/core/chocolatey_manager.cpp:439 -- Unused buildChocoCommand builds an unquoted shell string. FIX: Delete buildChocoCommand (execution already goes through argv-based executeChoco).
- [ ] [FB] src/core/drive_unmounter.cpp:450 -- closeAllHandles returns true on Restart Manager failures; caller ignores the result. FIX: Propagate failure and honor it, or document as advisory-only
- [x] src/core/email_export_worker.cpp:41 -- foldHeaderLine, exportSingleMboxMessage, buildMboxEmlContent, buildEmlContent have no live callers. FIX: Delete the unused helpers and their declarations, or wire them in if intended. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_inspector_controller.cpp:337 -- Report statistics fields never populated. FIX: Compute and set the per-type totals (from folder tree/scan results) before generateHtml/generateJson. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/email_search_worker.cpp:290 -- Body/attachment/recipient/MAPI matchers each independently reparse the same item. FIX: Read the item detail/properties once per item and pass to all field matchers. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/logger.cpp:139 -- File log writes/flush not checked for stream failure. FIX: Check stream good()/fail() after write/flush, only advance m_bytes_written on success, and surface persistent write failure (e.g. one-shot stderr notice / disable). DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/network_adapter_inspector.cpp:108 -- tryQueryAdapterAddresses returns true unconditionally and result is ignored. FIX: Either return the real success/failure and check it, or drop the bool return and rely solely on the result out-param. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/ost_conversion_worker.cpp:417 -- folders_processed incremented once per item, not per folder. FIX: Increment folders_processed once in processFolder, not per item. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] src/core/ost_conversion_worker.cpp:700 -- MBOX/DBX bytes_written adds cumulative writer total each item (triangular overcount). FIX: Assign (=) the cumulative total like the EML/HTML writers, not +=. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [ ] src/core/partition_hfs_file_system_writer.cpp:30 -- 27 near-identical FromImage wrappers duplicate raw-target gating (safety-policy drift risk). FIX: Extract a shared openImageTargetForWrite() helper
- [ ] src/core/permission_manager.cpp:113 -- bool and std::expected ACL APIs duplicate security-sensitive logic (6 paths / 3 ops). FIX: Have the bool overloads delegate to the try* variants (or vice versa) so each operation has one implementation
- [ ] src/core/registry_snapshot_engine.cpp:73 -- diffSnapshots has no production caller (duplicated by LeftoverScanner). FIX: Remove diffSnapshots (and its tests) or route scanRegistryDiff through it to eliminate the drift
- [x] src/core/smart_disk_analyzer.cpp:593 -- Drive enumeration capped at PhysicalDrive0..15. FIX: Probe until the first N consecutive misses or enumerate via SetupDi/IOCTL rather than a fixed 0..15 range. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/core/thermal_monitor.cpp:108 -- hasCpuTemperature scans full history, not latest poll. FIX: Check only the most recent reading/poll (or the current cycle) so a stale entry does not report present CPU temperature. DONE: delegated fail-closed fix wave (commit 9aa7689).
- [x] [FB] src/win32mcp/browser_bridge_security.cpp:111 -- Rendezvous-file write ignores short-write/flush errors. FIX: Check write() == bytes.size() and flush()/error() and return false with the error on any short write. DONE: delegated fail-closed fix wave (commit 9aa7689).
