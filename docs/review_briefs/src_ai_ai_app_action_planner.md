- [HIGH] `src/ai/ai_app_action_planner.cpp:17-19,154-159` -- Planner accepts 14,400-second timeouts, but execution path caps PowerShell at 3,600 seconds. Multi-hour actions receive inaccurate plans and can terminate early.
- [HIGH] `src/ai/ai_app_action_planner.cpp:109-119` -- Command guard and risk classifier receive preview containing manifest-controlled display/action text. Metadata can cause false blocks or alter guard matching, including bypassing broad-registry detection via injected narrow-scope text.
- [MEDIUM] `src/ai/ai_app_action_planner.cpp:96-103` -- `cli` is declared supported, but plan carries no CLI execution mode; downstream executes every command plan through PowerShell. CMD-specific CLI commands can fail or change meaning.
- [MEDIUM] `src/ai/ai_app_action_planner.cpp:53-55` -- `timeout_ms` validation accepts fractional, negative, and out-of-range numbers. Planner returns an executable plan although live step planning later rejects or silently clamps them.
- [MEDIUM] `src/ai/ai_app_action_planner.cpp:160-163` -- Caller-provided output bounds are unvalidated. `min_output_bytes > max_output_bytes` violates `std::clamp` preconditions; invalid nonpositive or oversized bounds can also produce plans rejected by executor.
- [LOW] `src/ai/ai_app_action_planner.cpp:154-163` -- Wrong-typed, fractional, or overflowing `timeout_seconds` and `max_output_bytes` values silently become defaults instead of producing validation errors.
- [LOW] `src/ai/ai_app_action_planner.cpp:143-146` -- Explicit unsupported action with empty `reason` returns empty `error_message`, yielding failure without diagnostic text.
- [LOW] `src/ai/ai_app_action_planner.cpp:133` -- Full manifest is copied into plan but never read anywhere in local tree; dead retained state duplicates `action_profile` and extracted fields.
