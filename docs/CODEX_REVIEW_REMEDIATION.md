# Codex Full-Tree Review -- Remediation Tracking

Source: 10 Codex (gpt-5.6-sol, xhigh) passes over all 1st-party src, 2026-07-30.
~424 raw findings; 12 Claude verification agents (~141 CONFIRMED, ~24 PARTIAL,
3 FALSE-POSITIVE, ~2% FP). Raw lists preserved in scratchpad codex-*.md.

GOAL: every issue FIXED, TESTED, CERTIFIED. Check items off as completed.

## Pre-existing test-build breakage (found during B1 cert, unrelated to review)
Two test targets fail to LINK in Release (missing TU in their CMake wiring;
their exes never built). Not caused by any remediation batch. Fix during the
owning subsystem batch:
- [ ] test_file_management_explorer_panel -- unresolved sak::combinedSize /
  sak::hashInputTruncated / sak::formatHashValue (file_explorer_properties_dialog.obj).
  Owner: Batch 8.
- [ ] test_wifi_manager_panel -- unresolved sak::buildWifiSetupScriptWindows
  (wifi_manager_panel.obj). Owner: Batch 9.

## How to read this doc
- Each item: `- [ ] ID [SEV] file:line -- problem. Fix: approach.`
- Flip `[ ]` -> `[x]` when the item is fixed AND covered by a test AND the
  full suite is green. Append `(commit <sha>)`.
- Each batch closes with a CERT line: build clean + gate green + unit suite
  green + targeted test added. Only mark a batch DONE when its CERT passes.
- FALSE-POSITIVE and ACCEPTED items are listed at the bottom for the record
  (not counted against completion).

## Fix order (batches)
0. Own teardown-fix gaps (806d5c5)      -- 2 items
1. Elevated command injection            -- 7 items
2. Disk destructive / data-loss          -- 14 items
3. WorkerBase base-join UAF + teardown   -- 15 items
4. flash_worker memory-safety            -- 8 items
5. System security (ACL/elevation/probe) -- 16 items
6. Actions fail-open cluster             -- 22 items
7. Email subsystem                       -- 34 items
8. Files subsystem                       -- 30 items
9. Network subsystem                     -- 24 items
10. Apps / supply-chain                  -- 40 items
11. Partition APFS/HFS write-safety      -- 14 items
12. AI harness                           -- 8 items
13. win32 / browser control              -- 10 items

Progress: 7 batches DONE (B0, B1, B2, B3 crash-fixes, B4, B5, B6). B6 FULLY
CLOSED: all 22 numbered findings (01-06 bitlocker keys, 07/08 verify-system-files,
09/10 check-disk, 11-14 reset-network, 15/16 gen-report, 17 screenshot, 18 power,
19 archive TOCTOU, 20 DHCP DNS, 21 dup-finder unhashed, 22 adv-search
unreadable+bad-regex) fixed+tested+committed across 12 commits, each with a
per-file unit test (8 new test targets) + lizard/clang-format green; whole
integration suite (test_ai_assistant_panel_tool_dispatch) stays 265 passed. The
drive_scanner mini-cluster (B6-23..26) is ALSO CLOSED: enumeration_ok gating (no
spurious detach), OOB-safe descriptor read, fail-closed writability probe, and
unmounted-Windows-partition detection via \\?\Volume{GUID}\ roots -- 15 new
offscreen seams in test_drive_scanner, live enumeration/isSystemDrive unchanged.
B5 FULLY CLOSED:
01/02/03 ACL + 04 pipe-PID-gate + 05 PS-success + 06 pipe-handle-atomic + 07
bounded-output + 08 write-probe-unique + 09 disk-bench-unique + 10 restore + 11
SMART + 12 hw-scan + 13 stress + 14 diag-suite + 15 reparse-walk + 16
report-atomic, plus tail (thermal-interval-floor, input_validator empty-asserts,
config QSettings-status). Each fixed+unit-tested+committed; remaining B5 tail =
deferred pure-quality items only (listed in Batch-5 section). B3 part 3 (deferred
quality/lifetime items) still open. All cert in RELEASE (repo standard; Debug has
pre-existing Debug-only test crashes -- see docs note under Batch 4). B4 also has
a guarded live-USB certifier (tests/certification/flash_live_certifier.cpp): its
elevation + removable-USB + system-disk guards are verified firing; the live
destructive run is user-executed (elevated) against a disposable USB stick.

---

## BATCH 0 -- Own teardown-fix gaps (extends 806d5c5) -- DONE
- [x] B0-01 [CRIT] src/gui/main_window.cpp:1635 -- appendLogIfActive writes m_panelLogs[tabIdx] with no m_shutting_down guard; m_panelLogs destroyed before panels, ~AdvancedUninstallPanel emits logOutput -> write into destroyed QMap (UAF on normal close). FIXED: guard with m_shutting_down.
- [x] B0-02 [CRIT] src/gui/main_window.cpp:631 -- currentChanged->materializeTab lambda has no m_shutting_down guard; panels emit currentChanged during teardown -> builds panel + assigns member unique_ptr mid-teardown. FIXED: guard with m_shutting_down.
- CERT B0: Release offscreen smoke close-loop 20/20 exit 0 (all panels materialized then torn down); shipping-config method, matches 806d5c5 release cert. Debug app-loop N/A (pre-existing Q_ASSERT pops modal dialog under smoke teardown; unrelated to this fix; guards are deterministic early-returns).

## BATCH 1 -- Elevated command injection -- DONE
- [x] B1-01 [CRIT] src/core/partition_script_builder.cpp:4534,4551 -- new_file_system inserted UNQUOTED into Format-Volume (Split op). FIXED: isSupportedFileSystem allowlist rejects unsupported values (enum arg, no quoting needed).
- [x] B1-02 [CRIT] src/core/partition_script_builder.cpp:4447,4474 -- target_style UNQUOTED into Initialize-Disk (ConvertStyle). FIXED: GPT/MBR allowlist rejects other values.
- [x] B1-03 [CRIT] src/core/partition_script_builder.cpp:1776 (diskPartLabel; callers 4918/5057) -- diskpart label kept CR/LF. FIXED: strip all control chars (<0x20, DEL) + double-quote; collapses to single token.
- [x] B1-04 [HIGH] src/core/partition_executor.cpp:114 -- credential temp path into single-quoted PS literal, no escaping. FIXED: double apostrophes in staged path before replace.
- [x] B1-05 [HIGH] resources/ai/workflows/technician_tool_assisted_task.json:33 -- ${tool_arguments} substituted UNQUOTED (all other placeholders are inside '...' + PowerShellSingleQuoted mode). FIXED: confine to single-quoted literal, split to arg array with @splat.
- [x] B1-06 [CRIT] src/actions/reset_network_action.cpp:148 -- cmd.exe /C "netsh ... > %1" allowed %VAR%/metachar parsing on TEMP path. FIXED: run netsh directly, write captured stdout to file in-process (no cmd.exe).
- [x] B1-07 [HIGH] src/core/package_internalization_engine.cpp:599,699 -- unescaped user paths into single-quoted PowerShell (extract + repack). FIXED: double apostrophes in both native paths.
- CERT B1: new tests scriptBuilder_rejectsEnumArgumentCommandInjection (B1-01/02) + scriptBuilder_stripsControlCharsFromDiskPartLabel (B1-03); negative-control confirmed they run. Full suite Release/offscreen 179/179 passed (incl. test_ai_assistant_panel_tool_dispatch which loads the edited workflow resource). Gate green (clang-format + lizard + cppcheck).

## BATCH 2 -- Disk destructive / data-loss -- DONE
Part 1 (B2-01..07) + part 2 (B2-08..14) both committed; full suite 179/179.
- [x] B2-01 [CRIT] src/core/partition_safety_validator.cpp:152,1777 -- WipeDisk blocked only on is_system; boot-but-not-system disk wiped. FIXED: blocksCurrentOsDiskMutation + WipeDisk block now also test is_boot. Test: safetyValidator_blocksWipeOfBootNotSystemDisk.
- [x] B2-02 [CRIT] src/core/storage_inventory_worker.cpp buildLayoutHash -- layout hash omitted is_boot/is_system, so a flag change didn't invalidate the apply hash. FIXED: fold is_system+is_boot into per-disk hash (also fixed a dangling %5); a boot/system change now forces refresh + re-validation (which applies B2-01).
- [x] B2-03 [CRIT] src/core/partition_safety_validator.cpp:974 -- region clone required target_disk_number but not target_offset_bytes; builder defaulted offset 0 -> overwrote partition table. FIXED: require target_offset_bytes. Test: safetyValidator_blocksClonePartitionWithoutTargetOffset.
- [x] B2-04 [CRIT] src/core/windows_usb_creator.cpp -- any numeric disk accepted for DiskPart clean. FIXED: guardTargetDiskSafe preflight (Get-Disk IsBoot/IsSystem/IsReadOnly, fail-closed) before format. Cert: compile + logic (needs live disk to e2e; defense-in-depth behind GUI removable-only).
- [x] B2-05 [CRIT] src/core/flash_coordinator.cpp validateTargets -- any physical disk passed for raw write. FIXED: native physicalDriveHostsSystemVolume (IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS on OS volume) blocks OS-disk targets. Cert: compile + logic.
- [x] B2-06 [CRIT] src/core/leftover_scanner.cpp:891 -- installLocation exemption classified C:\Windows Safe. FIXED: exempt only when installLocation is a STRICT subfolder of the protected root (never the root itself). Cert: existing test_leftover_scanner subfolder-exemption case still Safe (regression side); root case closed by logic.
- [x] B2-07 [CRIT] src/core/advanced_uninstall_controller.cpp cleanLeftovers -- GUI path had no protected-path screen. FIXED: cleanupItemRefusal moved to new shared include/sak/leftover_cleanup_item_guard.h (base leftover_cleanup_guard.h kept QtCore-only for its unit test); GUI path now screens every item (drops+reports refused) before CleanupWorker. Cert: existing test_leftover_cleanup_guard + test_advanced_uninstall_controller in suite (179/179).
Part 2 (B2-08..14) analyzed via workflow, applied here:
- [x] B2-08 [HIGH] src/core/drive_unmounter.cpp -- getVolumesOnDrive now signals enumeration failure via out-param; unmountDrive fails closed when enumeration failed (empty != safe). Cert: compile + logic (FSCTL path needs real disk).
- [x] B2-09 [HIGH] src/core/drive_unmounter.cpp -- preventAutoMount failure now aborts (fail closed); partial failure rolls back via new allowAutoMount (clears persistent OFFLINE). Cert: compile + logic.
- [x] B2-10 [CRIT] src/core/partition_raw_device_io.cpp isWindowsRawDevicePath -- broadened to \\?\PhysicalDrive*, bare \\?\Volume{GUID} (excludes long-path files after the brace), and POSIX /dev/*; fixes all 28 duplicated HFS image-only gates at the chokepoint. Test: rawDeviceClassifier_recognizesExtendedDeviceForms.
- [x] B2-11 [CRIT] src/tools/sak_apfs_writer_cli.cpp + sak_hfs_writer_cli.cpp -- new outputJsonAliasesTarget rejects an --output-json path that aliases the target/output-image (literal + canonical, case-insensitive) before the fs op. Cert: compile + logic (CLI-flow).
- [x] B2-12 [HIGH] src/tools/sak_apfs_writer_cli.cpp -- import-image now publishes via QSaveFile (atomic commit) instead of remove-before-copy; a failed publish leaves the prior output intact. Cert: compile + logic.
- [x] B2-13 [PARTIAL] src/core/partition_script_builder.cpp -- validator textual checks kept as preview signal; added runtime createImageDestinationGuardScript (Get-SakVolumeGuid resolves reparse points, throws if dest lands on the source disk) injected for CreateImage. Test: scriptBuilder_createImageEmitsDestinationGuard. Residual junction resolution is runtime-only (hence PARTIAL).
- [x] B2-14 [HIGH] src/core/partition_script_builder.cpp sakShadowBackupFunctionScript -- VSS-unavailable fallback now fails closed for VSS-capable filesystems (NTFS/ReFS/unknown throw); only FAT/FAT32/exFAT keep the warned live copy. Test: scriptBuilder_vssFallbackFailsClosedForVssCapableFs.
- CERT B2 part 2: rawDeviceClassifier_recognizesExtendedDeviceForms + scriptBuilder_createImageEmitsDestinationGuard + scriptBuilder_vssFallbackFailsClosedForVssCapableFs; full suite green (pending). Hardware/CLI-flow items (08/09/11/12) compile+logic-certified.

## BATCH 3 -- WorkerBase base-join UAF + teardown races
Systemic root: ~WorkerBase joins the thread as BASE subobject, AFTER derived
members destroyed -> UAF for any subclass destroyed mid-execute(). Preferred
root fix: move stop+join into a protected helper called by each most-derived
dtor (or a CRTP/enforced pattern), then remove terminate() fail-open.
- [x] B3-01 [HIGH] src/threading/worker_base.cpp -- SYSTEMIC ROOT FIXED: added protected idempotent WorkerBase::stopAndJoin(); ~WorkerBase calls it as last resort. Every WorkerBase subclass now calls stopAndJoin() in its OWN destructor (before its members are freed), eliminating the base-join-too-late UAF class.
- [x] B3-02 [MED] src/threading/worker_base.cpp:42 -- run() no longer clears m_stop_requested (a requestStop between start() and run-entry is no longer lost; defaults false at construction).
- [x] B3-03 [HIGH] include/sak/advanced_search_worker.h -- ~AdvancedSearchWorker now { stopAndJoin(); }.
- [x] B3-04 [HIGH] include/sak/organizer_worker.h -- ~OrganizerWorker now { stopAndJoin(); }.
- [x] B3-05 [HIGH] AdvancedSearchWorker derived join (B3-03) covers the file_management_explorer_panel path (panel deletes the worker whose dtor now joins).
- [x] B3-06 [HIGH] include/sak/{cpu,memory,disk}_benchmark_worker.h + stress_test_worker.h + uninstall_worker.h + cleanup_worker.h + file_explorer_{archive,transfer}_worker.h -- all now { stopAndJoin(); }. duplicate_finder/network_probe/partition_apply_worker already joined in prior work. B4-02 (flash_worker) also fixed here: ~FlashWorker now stopAndJoin() before closeDevice().
Part 1 (B3-01..06 root) committed d0f0d72. Part 2 (controller/panel joins) here:
- [x] B3-07 [CRIT] src/gui/app_installation_panel.cpp -- ~AppInstallationPanel now waitForFinished() on m_searchFuture/m_offlineSearchFuture (QtConcurrent::run is uncancelable) so the raw-`this`/m_choco_manager-capturing lambdas cannot deref freed state.
- [x] B3-08a [CRIT] src/core/quick_action_controller.cpp cleanupThread -- on wait-timeout now blocks until joined then `delete` (not deleteLater, which never fires in teardown), so m_actions is never freed under a running thread. (B3-08b same-action scan/exec serialization deferred to part 3 -- a race warning, not a crash.)
- [x] B3-09a [CRIT] src/core/network_diagnostic_controller.cpp cleanupThread -- 2nd wait result now honored; a refuse-to-stop thread is detached (setParent nullptr) as a bounded safe leak instead of ~QObject aborting on a live QThread. (B3-09b LAN-upload cancelability + cancel() coverage deferred to part 3 -- reduces terminate() likelihood; the abort is already prevented.)
- [x] B3-10 [CRIT] src/core/ost_converter_controller.cpp -- new disposeWorker() joins before delete; on refuse-to-stop defers deletion to the thread's finished signal instead of deleting a live QThread/worker. Both disposal sites use it.
- [x] B3-11 [HIGH] src/core/advanced_uninstall_controller.cpp -- new stopEnumThread() deletes the enum QThread only after a confirmed join (before ~controller frees m_enumerator), widened to kThreadWaitMs; unkillable case leaves the handle. (Uninstall Idle-then-replace already safe via ~UninstallWorker stopAndJoin.)
- [~] B3-12 [HIGH] src/threading/partition_apply_worker.cpp -- ALREADY joins in its own dtor (prior work, verified during B3 part 1); terminate() only reachable in the pre-write UAC window (no disk write yet). No change needed.
- [ ] B3-13 [HIGH] src/core/app_mutating_actions.cpp:1734,2046 -- DEFERRED to part 3 (workflow agent hit output cap; do inline).
- [x] B3-14 [CRIT] src/core/partition_manager_controller.cpp -- dtor now JOINs (cancel-then-waitForFinished-then-delete via cancelJoinDeleteWatcher) instead of detaching, so destructive disk work cannot outlive the controller.
- [ ] B3-15 [MED] GUI detached mutations -- DEFERRED to part 3 (needs design: naive waitForFinished() in a GUI dtor freezes the UI for minutes on a choco install; UI deref is already QPointer-guarded so not a UAF).
- CERT B3 part 2: full suite green (controller tests construct/destroy the controllers, exercising the new join dtors). Part 1 + part 2 close every confirmed teardown UAF/abort; part 3 = the deferred quality/lifetime items.

## BATCH 4 -- flash_worker memory-safety  [DONE -- Release-certified; live USB cert pending]
- [x] B4-01 [CRIT] src/threading/flash_worker.cpp verifySampleBlocks -- FIXED: removed the sourceBuffer/targetBuffer.resize(bytesRead) shrink; a short read now tracks a local compareLen and the buffers stay at block_size capacity, so the next iteration's full-block read can no longer overflow a shrunken buffer (HEAP OOB WRITE eliminated).
- [x] B4-02 [CRIT/HIGH] src/threading/flash_worker.cpp ~FlashWorker -- already FIXED in B3-06: ~FlashWorker calls stopAndJoin() BEFORE closeDevice(), so a coordinator wait-timeout can no longer let a live execute() touch a closed handle/freed source.
- [~] B4-03 [HIGH] src/threading/flash_worker.cpp lockVolume/dismountVolume -- HONEST RETURNS (not the blunt hard-fail, which would abort every flash: the target is a \\.\PhysicalDriveN handle where FSCTL_LOCK/DISMOUNT_VOLUME return ERROR_INVALID_FUNCTION). Both now return the real DeviceIoControl result and execute() logs the outcome; NON-fatal by design because the AUTHORITATIVE fail-closed dismount is FlashCoordinator::unmountVolumes() (hardened in B2-08/09), which MUST succeed before any worker starts. Verified unmountAndFlash returns false if unmount fails.
- [x] B4-04 [CRIT] src/threading/flash_worker.cpp execute -- FIXED: new queryDeviceCapacity() (IOCTL_DISK_GET_LENGTH_INFO) + pure imageFitsDevice() gate before the write; an oversized image now fails closed (error_code::invalid_argument) instead of clobbering the whole device. Unknown capacity (-1) still writes (best-effort; the raw write fails safely at end-of-device).
- [x] B4-05 [HIGH] src/threading/flash_worker.cpp writeImage -- FIXED: after the write loop, a premature zero-length source read that leaves written < m_totalBytes now fails closed instead of reporting success (padding only grows the tail, so a complete write has written >= totalBytes).
- [x] B4-06 [HIGH] src/threading/flash_worker.cpp verifySample -- FIXED: images smaller than one sample block now go through new verifySmallImage() which reads back a sector-aligned length (NO_BUFFERING-safe via pure alignUpToSectorSize()) and byte-compares the whole image, instead of auto-passing with nothing compared.
- [x] B4-07 [HIGH] src/threading/flash_worker.cpp verifySampleBlocks -- FIXED: the device ReadFile now checks bytesReadFromDevice == compareLen and fails closed on a short read (was comparing stale buffer tail and counting a short read as verified).
- [x] B4-08 [CRIT] src/core/flash_coordinator.cpp cleanupWorkers -- FIXED: a worker that refuses to stop within 15s is now DETACHED (setParent(nullptr)+release, a bounded intentional leak) so it finishes its current write and self-cleans, instead of m_workers.clear() destroying it and terminate()-ing a live raw-disk write (corrupted media + leaked handle). The fatal "QThread destroyed while running" abort was already prevented by B3-06/B4-02.
- [x] Related: duplicate target paths -- FIXED: validateTargets rejects duplicate (case-insensitive) device paths before opening any handle (two workers on the same disk interleave raw writes and corrupt each other). FALSE POSITIVE: "signals emitted under m_mutex -> deadlock" -- workers are QThreads, so all worker->coordinator signals are cross-thread QueuedConnection; slots run later on the coordinator thread, never re-entrantly under the lock. NOTED (cosmetic, deferred): setBufferCount(m_bufferCount) is stored but never applied to workers (per-worker buffer count unused); low value, no correctness impact.
- New pure static seams (offscreen-unit-tested in test_flash_worker): FlashWorker::alignUpToSectorSize (sector-aligned read length + overflow guard) and FlashWorker::imageFitsDevice (capacity fit, unknown=-1 allowed). New dup-target test in test_flash_coordinator.
- CERT B4: Release suite is the repo standard (ctest -C Release; Debug has pre-existing Debug-only crashes -- see [[headless-gui-tests]]). test_flash_worker + test_flash_coordinator + app (sak_utility) all GREEN in Release. LIVE USB cert on a real removable disk (tests/certification/flash_live_certifier.cpp, guarded+elevated, drive 4) PENDING.

## BATCH 5 -- System security (ACL / elevation / probe / diagnostics)
- [x] B5-01 [CRIT] src/core/permission_manager.cpp stripPermissions + tryStripPermissions -- FIXED: passed nullptr as pDacl with DACL_SECURITY_INFORMATION, which sets a NULL DACL == allow EVERYONE full access (MSDN-confirmed), the opposite of stripping. Now passes an initialized EMPTY (non-NULL) ACL so explicit access is removed and UNPROTECTED_DACL_SECURITY_INFORMATION re-inherits the parent ACL. Test: stripPermissions_doesNotProduceNullDacl (asserts no NO_ACCESS_CONTROL, unprotected).
- [x] B5-02 [HIGH] src/core/permission_manager.cpp setStandardUserPermissions + trySetStandardUserPermissions -- FIXED: new buildStandardUserDacl() adds SYSTEM (WinLocalSystemSid) + Administrators (WinBuiltinAdministratorsSid) ACEs alongside the user, so a "standard user" ACL no longer locks out the OS/services/admins. Test: setStandardUser_keepsSystemAndAdmins (asserts SY + BA present).
- [x] B5-03 [HIGH] src/core/permission_manager.cpp setSecurityDescriptorSddl -- FIXED: builds SECURITY_INFORMATION dynamically from what the SDDL actually provides (OWNER only if an owner is present; DACL only if daclPresent; PROTECTED vs UNPROTECTED from SE_DACL_PROTECTED control flag) instead of always forcing OWNER|DACL|PROTECTED (which nulled the owner / set a NULL DACL when the SDDL omitted them). Test: setSddl_ownerlessDoesNotNullOwner.
- [x] B5-04 [CRIT->DEF] src/elevated/elevated_helper_main.cpp:60,307,857 + elevated_pipe_server.cpp:29,314 -- missing/invalid parent PID disables client validation while pipe permits Builtin Users. NOT exploitable today (requireAdministrator, broker passes valid PID, fail-closed on mismatch, pipe nonce) -- defense-in-depth. FIXED (PID gate): validateClient() no longer returns true (skip) when m_parent_pid<=0 -- it now refuses; the match test is the new inline pure static clientPidMatchesParent(expected, client) = expected>0 && client==expected, so a missing/invalid parent PID authorizes nobody. NOT dropping the BU ACE (deliberate, documented): the main app ships an asInvoker manifest, so the broker connects with a NON-elevated Users token and REQUIRES GRGW to open the pipe -- dropping BU would break the elevation IPC entirely. The real per-process control is the now-fail-closed PID gate (pins the exact launching process, stronger than a user-level DACL) plus the per-session pipe-name nonce; DACL comment updated to record this. Certified: test_elevated_pipe_protocol 22/22 (1 new testClientPidGateFailsClosed: match ok, mismatch rejected, and <=0 expected rejects even an equal client). Server cpp + sak_elevated_helper recompile clean.
- [x] B5-05 [HIGH] src/elevated/elevated_helper_main.cpp:380 -- elevated PowerShell task sets success=true after process start even on timeout/cancel/nonzero exit. FIXED: task.success now = result.completedSuccessfully() (new ProcessResult method: !timed_out && !cancelled && exit_code==0 -- stricter than succeeded(), which ignores cancellation); task.error_message set from elevatedPowerShellResultError(), now extended to report a non-zero exit code (previously only timeout/cancel). Certified: test_process_runner 14/14 (1 new completedSuccessfully_strictOutcome: clean=true, nonzero/timeout/cancelled=false, incl. the cancelled-but-exit-0 case where succeeded() disagrees). Helper exe (sak_elevated_helper) recompiles clean; the one-line wiring is by-inspection (helper is a privileged exe with no unit-test target -- live elevated e2e).
- [x] B5-06 [HIGH] src/core/elevation_broker.cpp:44,158,406 -- m_pipe_handle read/written across cancel/exec threads without sync -> data race UB. FIXED: m_pipe_handle is now std::atomic<HANDLE>; every access is an explicit acquire-load / release-store (isConnected, connectPipe store, sendRaw, readExact, peekAvailable, cleanup). The m_send_mutex still serializes WriteFile-vs-CloseHandle in sendRaw/cleanup (unchanged); the atomic removes the UB on the previously lock-free reads (isConnected/readExact/peek could not take that mutex -- readExact holds it across a blocking ReadFile would deadlock the cancel path). cleanup now stores INVALID before CloseHandle so a concurrent reader loads INVALID and bails. Certified: elevation_broker.cpp compiles clean; test_partition_manager_core 223/223 (10 platform-skips). Thread-race itself is by-inspection (no TSan on this toolchain); the atomicity + acquire/release ordering is the standard fix for the reported data race.
- [x] B5-07 [HIGH] src/core/process_runner.cpp:26 + elevated_helper_main.cpp:400 -- output accumulated unbounded; helper output limit applied only afterward -> memory exhaustion. FIXED: appendOutput now bounds the RETAINED std_out/std_err via new pure bool appendCappedOutput(target, chunk, cap) against a hard per-stream ceiling kMaxAccumulatedOutputChars (128 MiB chars, well above the ~64 MiB caller caps), setting ProcessResult.output_truncated when output is dropped; the live on_output stream still receives every chunk in full. The elevated helper (runProcessStreaming) inherits the bound automatically -- its post-hoc cappedTaskOutput(half_cap) trim still runs but the flood can no longer accumulate first. Certified: test_process_runner 13/13 (1 new appendCappedOutput_boundsAccumulation: fits-whole, empty-noop, clip-to-cap, frozen-when-full, non-positive-cap-drops).
- [x] B5-08 [HIGH] src/core/app_paths.cpp:33 + logger.cpp:169 -- predictable probe filenames opened truncating + deleted; pre-existing files / planted links overwritten by elevated helper. FIXED: both write-permission probes drop the fixed names (".sak_write_probe" / ".test_write") for an unpredictable per-call name (pid/steady-clock + atomic counter) AND an exclusive create that never truncates or follows a link: app_paths uses QIODevice::WriteOnly|NewOnly; logger uses std::ios::out|std::ios::noreplace. A planted file/symlink at the name -> create fails closed (dir treated not-writable), never clobbered. Certified: test_logger 17/17 (1 new ensureLogDirectory_leavesNoProbeFile: a pre-existing unrelated file survives byte-for-byte, no ".test_write", no probe leftover; ensureLogDirectory made public static for the test). app_paths parallel fix compiles clean + runs at every startup (dataRoot/logsDirectory); no offscreen symlink-race harness.
- [x] B5-09 [HIGH] src/core/disk_benchmark_worker.cpp:279,347,429 -- fixed benchmark filename check/create/delete TOCTOU can truncate/delete replacement file. FIXED: createTestFile no longer does QFile::exists() then CREATE_ALWAYS on the fixed "sak_disk_benchmark.tmp"; it now builds a unique per-run name via new pure static makeUniqueBenchmarkFileName(pid,msecs,counter) and claims it with a single exclusive CREATE_NEW (fails closed if anything -- file or planted symlink -- exists at the name; no check-then-open window). Claimed path stored in m_test_file_path and returned by testFilePath() so every read/write/cleanup targets the file we own; cleanupTestFile clears it (no double-delete). Certified: test_disk_benchmark_worker 11/11 (1 new: uniqueBenchmarkFileName_isUniqueAndWellFormed). Live raw-I/O path was covered under B4's flash certifier pattern; exclusive-create is by-inspection (no offscreen way to plant a race).
- [x] B5-10 [HIGH] src/core/restore_point_manager.cpp isSystemRestoreEnabled -- FIXED: removed the VSS-service fallback (VSS runs for shadow copies/backups independently of System Restore, so it falsely reported ENABLED = fail-open). Now checks the authoritative RPSessionInterval registry value (>0 iff System Protection on) and fails closed (DISABLED) on any probe failure via new pure restoreEnabledFromProbe(). Test: test_restore_point_manager newly wired + restoreEnabledFromProbe_failsClosed.
- [x] B5-11 [HIGH] src/core/smart_disk_analyzer.cpp:129,270 -- malformed/incomplete smartctl JSON -> default report assessed Healthy; :93 missing smartctl/zero disks -> empty report treated success. FIXED: assessHealth now fail-closed via pure static reportHasAssessableData() (no smart_status + no SATA attrs + no NVMe log -> Unknown, never Healthy); analyzeDrive emits errorOccurred on Unknown; generateRecommendations says "could not be determined" instead of "health is good". Certified: test_smart_disk_analyzer 19/19 (6 new B5-11 tests, incl. malformed+empty JSON -> Unknown, valid still Healthy, FAILED still Critical).
- [x] B5-12 [HIGH] src/core/hardware_inventory_scanner.cpp:98,204 -- WMI timeout/process/JSON failure returns empty data without errorOccurred; scan still succeeds. :635 EnumAdapters1 non-NOT_FOUND falls through -> null deref. FIXED: (A) wmiQuery now sets m_wmiQueryFailed via pure static wmiQueryIsReportableFailure() (timeout|exit!=0|bad-JSON, but NOT cancel and NOT empty-success); scan()/scanX() reset it and emit errorOccurred("inventory may be incomplete") through new finishScan() before scanComplete; extracted pure static jsonDocToVariantMaps(). (B) DXGI loop captures the HRESULT and breaks on FAILED(hr)||!adapter instead of only NOT_FOUND, so an error HRESULT no longer derefs a null ComPtr. Certified: test_hardware_inventory_scanner 23/23 (4 new tests for the two pure seams + existing real-WMI CPU scan). DXGI null-guard is by-inspection (no offscreen DXGI mock).
- [x] B5-13 [HIGH] src/core/stress_test_worker.cpp:184,354,454,801 -- CPU thread-count zero + memory/disk/GPU init failures silently skip components, still PASS. FIXED: (CPU) new pure static resolveCpuThreadCount() clamps to >=1 so hardware_concurrency()==0 never launches zero threads; (memory) allocateStressMemory failure returns 1 (folds into errors_detected) instead of 0; (disk) buffer-alloc failure sets disk_errors=1 (mirrors could-not-claim-file guard); (GPU) any init step failure (device/shader/UAV) or unsupported-platform sets gpu_errors=1 instead of returning silently. computeStressPassed (already static+certified) fails closed on all of these. Certified: test_stress_test_worker 20/20 (2 new: resolveCpuThreadCount_neverZero, computeStressPassed_errorsDetected_fails; existing disk/memory/gpu/abort verdict tests cover the recorded-error paths). Init-failure code paths verified by inspection (no offscreen way to force VirtualAlloc/D3D11 failure).
- [x] B5-14 [HIGH] src/core/diagnostic_controller.cpp:105-180,293,339,622 -- CPU/disk/memory/hardware failures discarded, AllPassed without successful results; teardown ignores waits; cancelled SMART worker re-advances new step. FIXED (3 parts): (1) benchmark failed lambdas record the step in new m_suite_failures (cleared per runFullSuite); aggregateResults appends a "did not complete" warning per failure and routes overall_status through new pure static statusWithStepFailures() (AllPassed->Warnings on any failure, never downgrades a critical). (2) cancelCurrent uses a new stopWorkerAndWait() that CHECKS wait() and logs a worker still running past the timeout. (3) advanceSuiteStep now takes the completed step and no-ops unless new pure static suiteAdvanceAllowed(running, current, completedStep) matches -- a stale completion from a cancelled/skipped worker (and skipCurrentStep's own queued advance, now state-captured) can no longer double-advance/skip a step; all onXComplete handlers + failed/cancelled lambdas pass their own step. Certified: test_diagnostic_controller 9/9 (2 new pure-seam tests: suiteAdvanceAllowed_onlyForCurrentStep, statusWithStepFailures_failClosed).
- [x] B5-15 [MED] src/core/input_validator.cpp:201,276 -- write validation checks only DOS read-only attr not ACL; allow_symlinks=false checks only final component (ancestor junction bypass). FIXED: (write-ACL) checkWindowsPermissions already probes the real DACL via windowsAccessDenied(GENERIC_WRITE) in addition to the read-only attr (prior batch); (ancestor reparse) validatePathExistence no longer checks only std::filesystem::is_symlink(final) -- it now rejects a reparse point ANYWHERE via new anyComponentIsReparsePoint() over new pure static pathPrefixes() (root-to-leaf cumulative prefixes), using the FILE_ATTRIBUTE_REPARSE_POINT attribute so it catches junctions too (is_symlink misses mount points). Runs even for a not-yet-existing leaf so an ancestor junction cannot smuggle a later write past the policy. Certified: test_input_validator 44/44 (1 new pathPrefixes_decomposesEveryAncestor). Reparse detection is by-inspection (creating a junction needs privileges/no offscreen harness); the decomposition it walks is unit-tested.
- [x] B5-16 [MED] src/core/diagnostic_report_generator.cpp:78 + diagnostic_controller.cpp:414 -- reports overwrite non-atomically (truncated on short write/full disk); second-resolution names collide; empty format list reports success with no file. FIXED: (atomic) generateHtml/generateJson/generateCsv + writeJsonDocument now write via QSaveFile (temp sibling + atomic commit()/rename); a short write / disk-full / crash never leaves a truncated report and any prior good file is preserved (flushAndClose -> flushAndCommit, cancelWriting on stream-status failure). (unique names) generateReport uses new pure static uniqueReportBaseName(dir, when, counter) = millisecond timestamp + a monotonic atomic counter, so same-second reports never collide. (require output) generateReport now gates on new pure static requestedReportFormats(formats) (subset of html/json/csv) and refuses with an error when none is named, instead of reporting success having written nothing. Certified: test_diagnostic_controller 11/11 (2 new: requestedReportFormats_requiresKnownFormat, uniqueReportBaseName_neverCollides) + test_diagnostic_report_generator 10/10 (existing real-file write tests pass on QSaveFile).
- Lower/quality (system): [DONE] thermal_monitor.cpp:48 nonpositive interval -> pure static clampPollIntervalMs() floors to kMinThermalPollIntervalMs=250 (0 busy-spins QTimer, negative invalid); test_thermal_monitor 8/8. [DONE] input_validator empty-input Q_ASSERT crash -> removed the debug-only non-empty asserts from isValidUtf8 / sanitizeString / containsTraversalSequences / containsSuspiciousPatterns (all handle empty correctly: valid/empty/false); test_input_validator 45/45. [DONE] config_manager QSettings::status unchecked -> sync() now returns bool and logs on non-NoError status (a failed disk write no longer looks like a successful save); constructor logs a load-time Access/Format error; new isHealthy() + pure static describeSettingsStatus(); test_config_manager 22/22. REMAINING (deferred quality, not fail-open bugs): restore_point_manager.cpp:145 SilentlyContinue->queryOk (already reworked in B5-10 area; verify); keep_awake.cpp global refcount vs per-thread API; registry_snapshot_engine 3-level cap; logger.h source_location in template; hardware_inventory DXGI/WMI index join; smart disk PhysicalDrive0-15 cap.
- CERT B5: ACL round-trip test (strip restores inherited, not Everyone); diagnostics fail-closed tests; suite green.

## BATCH 6 -- Actions fail-open cluster
- [x] B6-01 [HIGH] src/actions/backup_bitlocker_keys_action.cpp:624 -- ACL-hardening failure leaves plaintext keys under inherited ACL, still success. FIXED: new secureBackupDirectory() fails closed -- when restrictFilePermissions() fails it removeRecursively()'s the whole backup dir (which holds plaintext recovery keys still under the default inherited ACL) and emitFailedResult, so success is never reported with exposed secrets on disk. Certified by inspection (ACL PowerShell path has no offscreen harness); the gate/removal wiring is exercised via the refactor's compile + the seam tests.
- [x] B6-02 [HIGH] backup_bitlocker_keys_action.cpp:118 -- Get/Set-Acl -Path (wildcard) not -LiteralPath ([ ] in path). FIXED: kRestrictPermissionsScriptTemplate now uses Get-Acl/Set-Acl/Get-ChildItem -LiteralPath (all four sites), so a backup path containing [ or ] is treated literally instead of as a wildcard that could target the wrong item or nothing. By-inspection (PS ACL path).
- [x] B6-03 [HIGH] backup_bitlocker_keys_action.cpp:573 -- same-second dir + WriteOnly truncate overwrites prior backup. FIXED: backupTimestamp() is now millisecond resolution (yyyyMMdd_HHmmss_zzz) and the dir name goes through new pure static uniqueBackupDirName(timestamp, counter) (monotonic atomic counter); createBackupDirectory() creates the leaf with QDir::mkdir (which, unlike mkpath, FAILS if the dir already exists) so any residual collision fails closed instead of reopening + truncating a prior backup. Certified: uniqueBackupDirName_distinctPerCounter + backupTimestamp_hasMillisecondResolution.
- [x] B6-04 [HIGH] backup_bitlocker_keys_action.cpp:523 -- failed protector query -> empty list, keys omitted, success. FIXED: getKeyProtectors(drive, bool& query_ok) and parseKeyProtectorResponse(out, bool& parse_ok) now distinguish process/parse FAILURE (query_ok=false) from a genuine empty-but-ran result (query_ok=true); executeExtractKeys fails closed (emitFailedResult naming the drive) on any query failure rather than silently omitting that volume's recovery key. Certified: parseKeyProtectorResponse_signalsParseFailure (malformed -> !parse_ok, empty) + _parsesValidPayload.
- [x] B6-05 [HIGH] backup_bitlocker_keys_action.cpp:552 -- gate counts any protector -> "Backed up 0 recovery keys" success. FIXED: new pure static evaluateKeyGate(total_keys_found, total_recovery_passwords) -> {Ok, NoProtectors, NoRecoveryPasswords}; the gate now requires total_recovery_passwords>0 (a TPM-only volume no longer passes to "success" with zero recovery keys), routed through extractKeysGatePassed() with a distinct message per case. Certified: evaluateKeyGate_requiresRecoveryPassword.
- [x] B6-06 [HIGH] backup_bitlocker_keys_action.cpp:720 -- QTextStream/close errors ignored -> truncated key files counted written. FIXED: writeRecoveryDocument, writeOneVolumeKeyFile (extracted from writePerVolumeKeyFiles, now bool + int& out) and writeJsonBackup all write via QSaveFile with out.flush()+status check then commit(); on stream error or a failed commit they cancelWriting() (no truncated key file left) and fail closed -- writePerVolumeKeyFiles returns false and executeSaveKeyFiles aborts, so a partial key file is never counted written. Certified: recoveryPasswordHelpers_countAndDetect + formatters_mapKnownCodes; real-file QSaveFile write pattern proven under B5-16. New test target test_backup_bitlocker_keys_action 9/9 (friend class), Release build of action + sak_elevated_helper clean, lizard/clang-format green.
- [x] B6-07 [HIGH] src/actions/verify_system_files_action.cpp:92 -- DISM "No component store corruption detected" contains "corruption" -> needless RestoreHealth on every clean machine. FIXED: new pure static dismReportsCorruption() excludes the healthy "No component store corruption" verdict FIRST, then treats "repairable" / other corruption-detected phrasing as positive; both runDismCheckHealth and runDismScanHealth route through it, so a clean machine no longer triggers RestoreHealth. (dismProbeCompleted, which detects that the probe RAN, deliberately still matches "corruption" -- the clean verdict does prove the probe ran.) Certified: dismReportsCorruption_cleanMachineIsNotCorrupt + _repairableAndDetectedAreCorrupt.
- [x] B6-08 [HIGH] verify_system_files_action.cpp:35 -- SFC fixed temp filename (concurrent overwrite/delete; reparse redirection). FIXED: runSFC redirects SFC stdout to an unpredictable per-run name via new pure static makeUniqueSfcOutputName(pid, msecs, counter) (atomic monotonic counter) instead of the fixed "sak_sfc_output.txt", so concurrent runs cannot clobber each other and a fixed-name reparse point cannot redirect the write. Certified: makeUniqueSfcOutputName_uniqueAndWellFormed (unique per counter, .txt, no quote/space so it is safe inside the single-quoted PS string). New test target test_verify_system_files_action 5/5; Release build clean; lizard/clang-format green.
- [x] B6-09 [HIGH] src/actions/check_disk_errors_action.cpp:108 -- "Check Disk Errors" schedules Repair-Volume -OfflineScanAndFix (mutation during a check). FIXED: buildRepairVolumeScript renamed buildScanVolumeScript and no longer calls -OfflineScanAndFix (the boot-time mutating repair) -- the action is documented as a "CHKDSK scan" and is now genuinely read-only (Repair-Volume -Scan only). On detecting the $corrupt marker it reports RepairRecommended:Yes instead of scheduling; the parser/summary/scan-details wording changed from "repair scheduled"/"REBOOT REQUIRED" to "repair recommended (this check does not modify the disk)". Certified: buildScanVolumeScript_isReadOnly (asserts no OfflineScanAndFix, no RebootRequired:Yes, has -Scan + RepairRecommended).
- [x] B6-10 [MED] check_disk_errors_action.cpp:280 -- one drive ok -> failed/timed-out drives don't affect success/failure count. FIXED: new pure static evaluateDiskCheckOutcome(drives_scanned, total_drives) -> {success, drives_failed} where drives_failed = total-scanned and success requires ALL enumerated drives to have scanned; executeBuildReport (now takes total_drives via a DiskCheckTotals struct to stay <=5 params) reports "%1 of %2 scanned, %3 could not be checked" and only succeeds when nothing failed, so a single good drive can no longer mask a timed-out/failed one. Certified: evaluateDiskCheckOutcome_aggregatesAllDrives (3/3 ok; 1/3 -> fail + 2 failed; 0/0 -> fail; 0/2 -> fail + 2). New test target test_check_disk_errors_action 4/4; Release build clean; lizard/clang-format green. (Also removed dead DriveCheckResult/checkDrive/parseChkdskOutput/m_drive_results declarations left from the old repair path.)
- [x] B6-11 [HIGH] src/actions/reset_network_action.cpp:142 -- Winsock-backup failure only appended to errors, destructive reset proceeds. FIXED: the backup moved into new executeBackupWinsock(), called FIRST in execute(); on any backup failure it emitFailedResult ("Network reset aborted: Winsock backup failed") and returns false so NO destructive reset runs without a rollback reference. Certified by inspection (process-driven; the abort path has no offscreen harness) + the stepFailed seam it uses is unit-tested.
- [x] B6-12 [HIGH] reset_network_action.cpp:279 -- Restart-NetAdapter no -ErrorAction Stop -> non-terminating errors still success. FIXED: new pure static buildAdapterResetScript() sets $ErrorActionPreference='Stop', adds -ErrorAction Stop, and wraps the pipeline in try/catch { ...; exit 1 } so a per-adapter failure terminates and exits non-zero (recorded via stepFailed). Certified: buildAdapterResetScript_failsClosed (asserts Stop pref + -ErrorAction Stop + catch + exit 1 + Restart-NetAdapter).
- [x] B6-13 [HIGH] reset_network_action.cpp:334 -- verify timeout/start-fail/nonzero exit ignored in success. FIXED: executeBuildReport now feeds the verify ProcessResult through new pure static stepFailed(timed_out, exit_code); a failed verification appends an error to a local all_errors copy so success = all_errors.isEmpty() reflects it (previously the verify result was logged and ignored). Certified: stepFailed_truthTable (clean=false; timeout/nonzero/both=true).
- [x] B6-14 [HIGH] reset_network_action.cpp:256 -- firewall reset with no export/backup of custom rules. FIXED: new executeResetFirewall() calls exportFirewallRules() (netsh advfirewall export to a unique temp .wfw, verified non-empty, path stored in m_firewall_backup_path) and resets ONLY if the export succeeded; a failed export skips the reset and records an error so custom rules are never wiped without a backup. Backup path surfaced in the report log. Certified by inspection (netsh export is process-driven) + the stepFailed seam it uses. New test target test_reset_network_action 4/4; Release build clean; lizard/clang-format green. (Also removed dead flushDNS/resetWinsock/resetTCPIP/releaseRenewIP/resetFirewall pre-decomposition methods.)
- [x] B6-15 [HIGH] src/actions/generate_system_report_action.cpp:257 -- collector start/nonzero-exit -> empty output, saving empty report still success. FIXED: new pure static collectorFailed(timed_out, exit_code) is applied to each collector (OS/hardware, storage, network); a failed collector records its name in m_collector_errors and embeds a "[collection failed]" marker instead of returning empty silently. saveReportAndFinish now gates via new pure static reportGenerationSucceeded(save_ok, all_collectors_ok) -- a saved-but-incomplete report reports Failed ("System report incomplete: X data could not be collected", partial file path still surfaced) instead of a clean success. Certified: collectorFailed_truthTable + reportGenerationSucceeded_requiresSaveAndCollectors (incl. the saved-but-collector-failed=false case).
- [x] B6-16 [MED] generate_system_report_action.cpp:229 -- BiosSeralNumber -- CONFIRMED FALSE POSITIVE, kept as-is. Get-ComputerInfo genuinely exposes the property as "BiosSeralNumber" (missing 'i') -- a documented Microsoft typo; "correcting" it to BiosSerialNumber returns $null. Added a code comment so it is not re-"fixed" later. New test target test_generate_system_report_action 4/4; Release build clean; lizard/clang-format green.
- [x] B6-17 [MED] src/actions/screenshot_settings_action.cpp:280 -- report-write failure ignored, path always advertised, success on screenshots_taken>0. FIXED: generateReport now returns bool (writes via QSaveFile with write-size check + commit(), cancelWriting on short write); the report text moved to pure static buildScreenshotReportText(). buildExecutionResult emits the report line via new pure static reportPathLine(written, path) -- the REPORT_PATH is advertised ONLY when the file was actually written, else "REPORT_WRITE_FAILED:1", and the message gains "(report not written)". Certified: reportPathLine_onlyAdvertisesWrittenReport + buildScreenshotReportText_containsPagesAndSections + generateReport_writesAndDetectsFailure (real temp write ok; missing-dir -> false).
- [x] B6-18 [MED] src/actions/optimize_power_settings_action.cpp:306 -- custom plan containing "High Performance" -> skips activation. FIXED: new pure static isHighPerformanceGuid(guid) matches the built-in High Performance (8c5e7fda-...) and Ultimate Performance (e9a42b02-...) GUIDs (case-insensitive, trimmed); execute()'s already_optimized and activateHighPerformancePlan's verify now compare by GUID instead of a name substring, so a custom plan merely NAMED "...High Performance..." no longer suppresses activation of the real plan. Certified: isHighPerformanceGuid_matchesBuiltinsByGuid (both builtin GUIDs any-case/whitespace true; custom GUID / empty / the literal name string false). New targets test_screenshot_settings_action 5/5 + test_optimize_power_settings_action 3/3; Release build clean; lizard/clang-format green.
- [x] B6-19 [HIGH] src/core/app_mutating_actions.cpp:164,3195 -- new/empty-dir + zip-output TOCTOU (WriteOnly clobbers raced-in file; archive engine removes on failure). FIXED in the engine (file_explorer_archive_service.cpp) where the actual open happens: compressToZip now opens the output via QFile WriteOnly|NewOnly (exclusive create -- fails if the path exists) and drives QZipWriter over that device, so a file raced in after the op-layer exists() check is NOT clobbered; because the archive is now guaranteed to be the file we created, the remove-on-failure cleanup can never delete a pre-existing/raced-in file. writeExtractedFile likewise uses WriteOnly|NewOnly (fail closed on a foreign file raced into the verified new/empty dir; validated entries never collide). Certified: new test target test_file_explorer_archive_service -- compressToZip_refusesExistingOutputWithoutClobber (existing file left byte-for-byte intact) + compressThenExtract_roundTrips (happy path still works). Removed the now-unused QSaveFile include.
- [x] B6-20 [MED] app_mutating_actions.cpp:2290 -- DHCP reset discards DNS-reset result but claims both reset. FIXED: EthernetConfigManager::restoreDhcpMode no longer static_cast<void>'s the DNS "source=dhcp" netsh call -- it captures the result into a new optional out-param (threaded through restoreSettings(..., bool* dnsApplied)). The network.reset-to-DHCP op passes &dns_applied and now reports the ACTUAL DNS outcome: message says "...including DNS" only when it worked, else "IPv4 to automatic (DHCP), but DNS could NOT be set to automatic -- set DNS manually", and the result data carries dns_automatic. Certified: netsh EXECUTION is not offscreen-testable (needs admin + mutates the host -- precedented by the existing test file's own note), so this leg is by-inspection; the wider suite (test_ai_assistant_panel_tool_dispatch, which links both files) stays green at 265 passed / 0 failed. lizard/clang-format green.
- [x] B6-21 [HIGH] src/threading/duplicate_finder_worker.cpp:573 + app_readonly_actions.cpp:1385 -- hash failures filtered then incomplete set reported success. FIXED: DuplicateFinderWorker now records m_files_unhashed = scanned - successfully-hashed in BOTH execute() (parallel/sequential) and executeFileSystemTarget() (files that fail to hash are dropped from the comparison, so a non-zero count = INCOMPLETE results), exposed via new filesUnhashed() accessor. findDuplicates (app_readonly) surfaces it: the result data carries files_unhashed and the message appends "; N file(s) could not be read/hashed and were skipped (results may be incomplete)" so a partial scan is no longer a bare success. Certified: test_duplicate_finder_worker 8/8 -- new unhashableLockedFileIsCounted (exclusively-locked file via CreateFileW share=0 -> filesUnhashed()==1) + a clean-scan filesUnhashed()==0 assertion; integration suite test_ai_assistant_panel_tool_dispatch stays green (265 passed). lizard/clang-format green.
- [x] B6-22 [HIGH] src/core/advanced_search_worker.cpp:671,210 + app_readonly_actions.cpp:1247 -- unreadable/over-limit == "no matches" reported success (false negative); invalid exclude regex silently dropped (searches excluded paths). FIXED (both): (unreadable) searchTextContent now increments a new m_files_unreadable counter when a file cannot be opened OR exceeds the 500k-line cap (previously both returned an empty match set indistinguishable from a clean file); exposed via filesUnreadable(), reset per run in prepareSearchConfig; searchFiles (app_readonly) surfaces it (data.files_unreadable + "; N file(s) could not be read and were skipped (results may be incomplete)"). (bad regex) prepareSearchConfig no longer silently drops an invalid exclude pattern -- new pure static firstInvalidExcludePattern() detects the first bad one and the search FAILS CLOSED (invalid_argument, logs the pattern) rather than descending into paths the user asked to exclude; the valid patterns are then compiled. Certified: test_advanced_search_worker 44/44 (3 new: firstInvalidExcludePattern_detectsBadRegex, invalidExcludePattern_failsClosed [emits failed], unreadableFile_isCounted [exclusive-locked file -> filesUnreadable()==1]); integration suite 265 passed. lizard/clang-format green. (Codex also cited the target/raw path, which already had the TargetBatchState.incomplete surfacing from a prior batch.)
- [x] B6-23 [MED] src/core/drive_scanner.cpp:187 -- enumerateDrivesOnce's enumeration_ok is captured but DISCARDED by the QtConcurrent lambda, so a failed enumeration (empty/partial list) is indistinguishable from "no drives" and spuriously DETACHES all drives in the watcher-finished handler. FIXED: the worker now returns a new sak::DriveScanResult {drives, enumeration_ok}; onScanFinished threads enumeration_ok into applyDriveScan(newDrives, enumeration_ok). Two new pure static seams gate the diff: drivesDetached(current, scanned, enumeration_ok) returns EMPTY when !enumeration_ok (a partial/probe-only scan NEVER reports a detach), and mergeDriveList(current, scanned, enumeration_ok) keeps every cached drive and only unions in newly-seen ones on a non-authoritative scan (authoritative -> the scanned list). Attach stays additive (a freshly-seen drive is real even under a partial scan). Certified: test_drive_scanner drivesDetached_partialScanKeepsAll/authoritativeReportsRemoved + mergeDriveList_partialUnionsNeverDrops/authoritativeReplaces; the live enumeration/isSystemDrive tests still pass (real Samsung boot + 2 USB detected).
- [x] B6-24 [MED] drive_scanner.cpp:315 -- storage-descriptor OOB read (VendorIdOffset/ProductIdOffset used as raw buffer offsets, unbounded). FIXED: new pure static descriptorString(buffer, buffer_size, bytes_returned, field_offset) returns empty for an absent (0) or out-of-range (>= min(bytes_returned, buffer_size)) offset and reads at most to the returned-data end even when the field is not NUL-terminated -- so a buggy/hostile driver offset can no longer walk past the buffer; getDriveName also now requires bytes_returned >= sizeof(STORAGE_DEVICE_DESCRIPTOR) before touching the header. Certified: descriptorString_readsBoundedField / rejectsOutOfRangeOffset (offset == and > bytes_returned) / stopsAtBufferEndWhenUnterminated / absentFieldIsEmpty.
- [x] B6-25 [MED] drive_scanner.cpp:478 -- writability probe error fails OPEN (reports writable on error). FIXED: new pure static driveReadOnlyFromProbe(is_writable_ioctl_ok, is_writable_error, length_ok, length_bytes) returns writable ONLY when IOCTL_DISK_IS_WRITABLE affirmatively succeeded; ERROR_WRITE_PROTECT and zero-length map to read-only, and EVERY other failure (access-denied, not-ready, unsupported) also maps to read-only instead of the old fail-open false. Safe because IOCTL_DISK_IS_WRITABLE is FILE_ANY_ACCESS (verified in the SDK header) so a genuinely writable drive succeeds on the 0-access query handle; the error is captured immediately, before the length IOCTL can overwrite GetLastError(). Certified: driveReadOnly_confirmedWritable / writeProtected / zeroLength / unknownErrorFailsClosed.
- [x] B6-26 [MED] drive_scanner.cpp:622 -- an unmounted Windows partition is classified as non-system (containsWindowsInstallation only inspected MOUNTED paths; a Windows volume without a drive letter -> isSystem=false -> the flasher's system-drive gate misses it -> catastrophic overwrite risk). FIXED: new getVolumeRootsForDrive(driveNumber) enumerates EVERY volume on the physical drive (shared volumeMatchesDrive helper) and yields the mount path when mounted, else the volume's \\?\Volume{GUID}\ path; containsWindowsInstallation now inspects all of those. Indicator check moved to static hasWindowsIndicators(root) using GetFileAttributesW on native backslash paths (QDir mangles the \\?\ prefix), so an UNMOUNTED Windows partition is still detected. Certified: hasWindowsIndicators_detectsSystem32 (temp Windows/System32/ntoskrnl.exe tree -> true) + emptyRootIsNotSystem; the live isSystemDrive_systemDriveDetected still detects the real boot drive through the new GUID-root path (no regression). getMountPoints refactored onto the same volumeMatchesDrive helper.
- [x] CERT B6: fail-open unit tests landed per finding (bitlocker gate/parse, clean DISM, drive aggregation, ErrorAction Stop, empty report, report-write, power GUID, archive no-clobber, dup-finder unhashed, adv-search unreadable + bad regex, + drive_scanner enumeration_ok/OOB/writability/unmounted); each batch built + run in Release offscreen; whole integration suite 265 passed. FULL CTEST CERT: full Release build (exit 0) + `ctest --test-dir build -C Release` = 191/191 passed, 0 failed, 224s. B6 COMPLETE incl. B6-23..26 (drive_scanner mini-cluster, commit 63c17eb; 15 new offscreen seams folded into the existing test_drive_scanner, live enumeration + isSystemDrive unchanged).

## BATCH 7 -- Email subsystem
- [x] B7-01 [CRIT] src/core/user_profile_backup_worker.cpp:49,99,109 + user_profile_restore_worker.cpp:100,141,151 -- QThread shadows isRunning() with late m_running; destroy/second-start before run() sets it -> destroy live thread / config race. FIXED: deleted the custom `bool isRunning() const { return m_running; }` override AND the std::atomic<bool> m_running member in BOTH workers; all call sites (destructor guard, startBackup/startRestore second-start guard, external wizard caller) now resolve to QThread::isRunning(), which Qt sets true synchronously inside start() BEFORE run() begins -- so the destroy-before-run() window (destructor skipped wait -> "QThread: Destroyed while thread is still running" abort) and the second-start race are both closed. Removed the 6 backup + 4 restore `m_running = true/false` writes in run(). Certified: test_user_profile_backup_worker testStartThenImmediateDestroyIsSafe (5x start-then-immediate-destroy, no prior wait, no abort) 7/7 + test_user_profile_restore_worker startThenImmediateDestroyIsSafe (40-file restore, same) 17/17; whole existing start->wait->destroy suite still green. lizard/clang-format green.
- [x] B7-02 [CRIT] src/core/email_inspector_controller.cpp:179 + mbox_parser.cpp:317,681 -- concurrent MBOX attachment requests race shared QFile/offsets/m_attachment_sink -> UB / mixing. FIXED: added `mutable QRecursiveMutex m_file_mutex` to MboxParser and take it at EVERY public entry point that touches the shared file cursor / m_message_offsets / m_attachment_sink (open, close, isOpen, indexMessages, loadMessages, loadMessageDetail, readMessages, readMessageDetail, readAttachmentData, readAllAttachments, messageCount, filePath). Recursive so a public entry (readAttachmentData) can call another (readAllAttachments) without self-deadlock; private helpers (readRawMessage/buildMessageIndex) stay lock-free since they only run under a held lock; cancel() deliberately stays lock-free (atomic flag only). This serializes the GUI-thread loads and the worker-thread attachment/search/export reads so a seek for message A can no longer be clobbered by a concurrent seek for message B. Controller loadAttachmentContent's concurrent QtConcurrent tasks are now safe by construction. Certified: test_mbox_parser concurrentReadsDoNotRace -- 8 messages each with a UNIQUE attachment payload+subject, 256 pool tasks hammer readAttachmentData+readMessageDetail across indices and assert every read returns its OWN index's bytes/subject (0 mismatches); 21/21 green across 3 back-to-back runs. lizard/clang-format/cppcheck green.
- [x] B7-03 [CRIT] src/core/ost_converter_controller.cpp:125,139,144,328,337 -- ignores failed waits, deletes worker/thread possibly executing; terminate() on cancel. (Also B3-10.) FIXED: the join-before-delete / defer-on-refuse skeleton was already in place from B3-10; this closes the two residual hazards. (1) REMOVED terminate(): disposeWorker now does quit() + a single graceful wait(shutdown+terminate budget) and, if the thread STILL has not stopped, defers deletion to its own finished signal -- it never force-kills a thread mid-PST-write (which could corrupt the output file / leave locks held). The OstConversionWorker honors m_cancelled at every folder/item loop (8 checkpoints), so a cancelled convert() returns promptly and the graceful wait succeeds; worst case is a bounded leak, strictly safer than terminate(). (2) The worker QThread is now created UNPARENTED (new QThread() not new QThread(this)) so the deferred-deletion path is crash-safe: a controller parented thread would be force-deleted by ~QObject while still running (abort), defeating the deferral. Certified: test_ost_converter_controller testCancelDuringConversionStopsSafely (4 workers, cancel immediately -> graceful stop, batch finalized, allConversionsComplete fired, isRunning()==false, controller destructs without abort) 18/18 across 3 runs. lizard/clang-format green.
- [x] B7-04 [HIGH] src/core/email_profile_manager.cpp:375,680 -- restore blindly imports attacker-controlled .reg via reg.exe; no hive/value restriction -> arbitrary HKCU/HKLM. FIXED: path-traversal confinement of the .reg was already in place (restoreRegistryFromManifest -> resolveWithinDirectory); this closes the CONTENT hole -- a .reg INSIDE the backup dir could still write any key. importRegistryKey now reads the file, decodes it (new decodeRegFile handles reg.exe's UTF-16LE-with-BOM + ANSI/REGEDIT4), and validates it with new pure static regContentConfinedToEmailHives BEFORE shelling to reg.exe: every [key] section (and every [-key] deletion) must match regKeyPathAllowed = starts with HKEY_CURRENT_USER\SOFTWARE\MICROSOFT\OFFICE\ AND contains \OUTLOOK\PROFILES (exactly the subtree exportRegistryKey backs up). Anything under HKLM, or any other HKCU area (Run/RunOnce persistence, shell hooks), or a file with zero key sections, is refused (import skipped, logged). Certified: test_email_profile_manager 24/24 -- allowsOutlookProfileSubtree, rejectsHklmRunKey, rejectsHkcuOutsideOutlook, rejectsMixedGoodAndBad (one allowed key does not launder a Run key), rejectsDeletionOutsideSubtree, rejectsEmpty, decodeRegFile_handlesUtf16Bom. reg.exe import itself is not offscreen-testable (needs the host registry) so that leg is by-inspection; the decision seam is fully covered. lizard/clang-format/cppcheck green.
- [x] B7-05 [HIGH] src/core/html_email_writer.cpp:212 -- saved HTML embeds untrusted email HTML verbatim (no sanitize/CSP) -> script/remote content on open. FIXED (two layers): (1) new shared header-only sak::sanitizeEmailBodyHtml (include/sak/email_html_sanitizer.h) strips <script> blocks, framing/loading tags (iframe/object/embed/frame/applet/base/meta/link/form), inline on* event handlers, and javascript:/vbscript: URIs from the body before it is written; (2) buildHtmlPage now emits a strict CSP meta -- default-src 'none' (blocks scripts, frames, and ALL remote loads -> no tracker/beacon/SSRF on open), img-src data: (only the inline data-URI images we embed), style-src 'unsafe-inline' -- as the hard backstop for anything the sanitizer misses. Certified: test_html_email_writer 11/11 -- sanitizerStripsActiveContent (script/iframe/onerror/javascript: all removed, benign <p> kept) + savedHtmlHasCspAndStripsScript (saved file carries the CSP + default-src 'none', no <script>/onerror, real body preserved); testHtmlBodyPreserved still passes (benign <b> untouched). lizard/clang-format green.
- [x] B7-06 [HIGH] src/core/pdf_email_writer.cpp:103,153 -- untrusted HTML to QTextDocument; absolute local refs disclose local files into PDF. FIXED (two layers): (1) the body is run through the same sak::sanitizeEmailBodyHtml; (2) rendering now uses a new ResourceDenyingTextDocument (QTextDocument subclass whose loadResource() returns empty for every scheme except self-contained data:), so an email body's <img src="file:///C:/secret"> (or relative/UNC/remote ref) can NEVER be resolved and embedded -- no disk or network access happens during rendering. Certified: test_pdf_email_writer 9/9 -- hostileBodyRendersWithoutResourceLoad (body references a planted local secret.txt + a remote beacon + a script; still renders a valid non-empty %PDF, no hang/crash). The loadResource denial guaranteeing non-disclosure is a clear override certified by inspection; the render path is exercised by the test. lizard/clang-format green.
- [x] B7-07 [HIGH] src/core/email_profile_manager.cpp:47,393 -- destination confinement lexical only; junction under home redirects QFile::copy outside home. FIXED: destinationWithinRoot (promoted from an anon free function to a testable private static) no longer trusts a textual QDir::cleanPath prefix check. It now finds the deepest EXISTING ancestor of the target (new deepestExistingAncestor helper -- the leaf may not exist yet) and resolves it through every reparse point via new realCanonicalPath (Win32 GetFinalPathNameByHandleW on a FILE_FLAG_BACKUP_SEMANTICS handle), then requires that real path to sit inside the real root. QFileInfo::canonicalFilePath does NOT follow directory junctions (proven -- my first cut used it and the junction test FAILED), so the native final-path call is required. Certified: test_email_profile_manager 26/26 -- destinationWithinRoot_allowsInsideRejectsOutside (inside allowed, sibling/`..`/empty rejected) + destinationWithinRoot_rejectsJunctionEscape (creates a REAL junction inside home via `mklink /J` pointing outside, asserts a path through it is rejected; QSKIP if mklink unavailable). Residual: a TOCTOU where the ancestor is swapped for a junction between check and copy is a narrow local-attacker race, noted not fixed. lizard/clang-format/cppcheck green.
- [x] B7-08 [HIGH] src/core/user_profile_backup_worker.cpp:188,246,324 -- unvalidated username+relative_path; `..` reads/writes outside roots; file symlinks copied without reparse check. FIXED (three parts): (1) new pure static isSafePathSegment validates user.username before it becomes a backup subdirectory -- rejects empty/"."/".."/separators/drive-colon/wildcards, so a crafted "..\\..\\Windows" can't escape the destination root (unsafe user counted as an error -> backup not a clean success). (2) new pure static isSafeRelativePath validates folder.relative_path before it is joined onto BOTH the source profile and destination roots -- rejects absolute/drive-relative/leading-slash/UNC and any "."/".." component, blocking read-outside-profile and write-outside-backup. (3) the reparse-point skip (previously DIRECTORY-only in copyDirectoryEntry) now also covers FILES, and is applied in backupFolder too: a FILE symlink would otherwise copy the content of its target (e.g. a symlink to a privileged system file) into the backup. Reuses the existing isReparsePoint (GetFileAttributesW & FILE_ATTRIBUTE_REPARSE_POINT, which catches file and dir reparse points alike). Certified: test_user_profile_backup_worker 9/9 -- isSafePathSegment_acceptsNamesRejectsTraversal + isSafeRelativePath_acceptsRelativeRejectsEscapes (both cover the accept + every reject class). File-symlink skip is by-inspection (symlink creation needs privilege in a headless test; same isReparsePoint gate already proven for dirs). lizard/clang-format/cppcheck green.
- [x] B7-09 [HIGH] src/core/user_data_manager.cpp:293,668,737 -- overwrite_existing ignored: ZIP restore always -Force, dir restore never overwrites. FIXED: (ZIP) extractArchive now appends `-Force` to Expand-Archive ONLY when config.overwrite_existing (previously hard-coded, always clobbering). (dir) copyDirectory/copyPlainFiles gained a 3-state ExistingFilePolicy {Fail, Skip, Overwrite} instead of always failing on an existing dest: restore passes Overwrite (replace) or Skip (leave the user's file) per the flag, while BACKUP CREATION keeps the default Fail so two distinct sources colliding on one name still fails closed (kept the existing copyDirectoryFailClosedOnCollision test green -- a naive bool broke it). Certified: test_user_data_manager 23/23 incl. new dirRestoreHonorsOverwriteFlag (seed dest data.txt=OLD; overwrite=false leaves OLD, overwrite=true -> NEW). lizard/clang-format green.
- [x] B7-11 [HIGH] src/core/user_data_manager.cpp:617,648,668 -- encrypted restore decrypts to extensionless temp then ZIP-only Expand-Archive; encrypted backups cannot restore. FIXED: decryptArchiveToTempFile now creates the QTemporaryFile with a `.zip`-suffixed template (QDir::tempPath()+"/sak_udm_XXXXXX.zip"); Expand-Archive refuses any source not ending in .zip, so the old extensionless temp made every encrypted backup unrestorable. Random XXXXXX part preserved (TOCTOU-safe unique name). Certified: new encryptedBackupRestoreRoundTrip -- real encrypt (password) backup -> restore with password -> secret.txt content byte-exact; this returned FALSE before the fix. lizard/clang-format green.
- [x] B7-30 [MED] user_data_manager.cpp:445,485 -- verifyBackup true for checksum-less metadata even if payload missing; compareChecksums true when both reads fail. FIXED: verifyBackup now requires the payload (QFileInfo::exists(backup_path)) before returning true, so a stray .json without its payload no longer verifies; when a checksum IS recorded it also requires a non-empty computed digest. compareChecksums requires BOTH digests non-empty before comparing, so two unreadable files (both hashing to "") are no longer reported "equal". Certified: verifyBackupFailsWhenPayloadMissing (delete payload, keep .json -> false) + compareChecksumsRejectsUnreadablePair. lizard/clang-format green.
- [x] B7-31 [MED] user_data_manager.cpp:427 -- deleteBackup uses QFile::remove for directory payloads -> uncompressed backups undeletable. FIXED: deleteBackup now checks QFileInfo(backup_path).isDir() and uses QDir::removeRecursively() for directory (uncompressed) payloads, QFile::remove for file (.zip) payloads. Certified: deleteBackupRemovesDirectoryPayload (uncompressed backup dir + .json both gone). lizard/clang-format green.
- [x] B7-10 [HIGH] src/core/user_profile_restore_worker.cpp:72,87 -- "atomic" replace removes original before rename; rename failure loses destination. FIXED: copyFileReplacingExisting no longer QFile::remove(finalDestPath) then rename(temp,final) -- a rename failure in that gap left the destination gone. It now writes the new copy to a temp, then moves the ORIGINAL aside by RENAME to <dest>.sakold.tmp (QFile::rename can't overwrite so the slot must be vacated, but by relocating not destroying), renames the temp into place, and only then removes the moved-aside original; if the temp->final rename fails it rolls back by renaming .sakold.tmp back to the destination. So no failure can lose the original. Certified: test_user_profile_restore_worker 18/18 -- overwriteRestoreLeavesNoTempArtifacts (KeepLarger replaces a small dest with the larger backup; asserts final content, NO leftover .sakold.tmp/.sakrestore.tmp, and a .sakbak recovery copy holding the original when create_backup=true); the existing conflictKeepNewer/KeepLarger overwrite-success tests stay green. lizard/clang-format green.
- [ ] B7-11 [HIGH] src/core/user_data_manager.cpp:617,648,668 -- encrypted restore decrypts to extensionless temp then ZIP-only Expand-Archive; encrypted backups cannot restore. Fix: restore correct format.
- [x] B7-12 [HIGH] src/core/pst_writer.cpp:134 + msg_writer.cpp:82 + dbx_writer.cpp:94 + pst_splitter.cpp:36 -- PST/MSG/DBX writers always not_implemented; PST is DEFAULT OST-conversion format. Fix: implement or block+relabel default. DONE (460eb4a): blocked + relabeled default. Added pure isOutputFormatSupported() (single source of truth: Eml/Mbox/Html/Pdf/ImapUpload supported, Pst/Msg/Dbx gated). Config+result default format Pst->Eml so an untouched convert yields readable output. GUI markUnsupportedFormats() greys out + " - not supported" the three gated formats, picker defaults to EML, settings restore by enum value with unsupported->EML fallback. Worker up-front reject derived from the support table (drops hardcoded Dbx||Msg); PST still routed through create() so WIP NDB/LTP path stays exercised + fails closed. AI convert_ost action refuses pst/msg/dbx up front with a precise reason. Cert: test_ost_converter_types support-table+default cases 16/16, convertOstRefusesUnsupportedFormat 266/266, controller 18/18, app+widget compile clean; clang-format/lizard/cppcheck green.
- [x] B7-13 [HIGH] src/core/user_profile_types.cpp:267,314 + user_profile_restore_worker.cpp:578,697 -- manifest/per-user checksums serialized but never computed/verified; restore checks only manifest existence; "verify" checks only readability. Fix: compute+verify checksums. DONE (0ac89cb): fully implemented. Pure statics on BackupManifest: checksumOfManifestJson() SHA-256s the manifest JSON with the self-referential "manifest_checksum" key removed (deterministic -- QJsonObject serializes keys sorted); computeManifestChecksum()/verifyManifestChecksum() wrap it (empty stored checksum = legacy/unverifiable -> pass, non-empty mismatch -> fail); hashDirectoryTree() deterministic SHA-256 over a user's payload dir (files sorted by fwd-slash relpath, path+bytes folded so rename/edit/readability-flip all change it). Backup saveManifest() now digests each <dest>/<username> payload into checksum_sha256 and seals the manifest before writing. Restore validateBackup() verifies the manifest checksum then recomputes each selected user's payload digest vs the manifest, fail-closed on mismatch BEFORE any copy; verifyFile stale comment fixed. Legacy backups (empty checksums) still restore with a logged note. Cert: test_user_profile_types +11 (compute/exclude-self/tamper/legacy/save-load; dir-hash missing/order-independent/content/name) 26/26; restore +3 (manifest-mismatch fail, payload-mismatch fail, correct pass) 21/21; backup 9/9. clang-format/lizard/cppcheck green.
- [x] B7-14 [HIGH] src/core/user_profile_types.cpp:104,120,296,347 -- missing/malformed filter JSON clears mandatory dangerous-file defaults; empty-object Q_ASSERT abort in debug. Fix: keep defaults; no assert on empty. DONE (8e6b444): SmartFilter::fromJson replaced dangerous_files/exclude_* with JSON arrays unconditionally, so a manifest missing those keys (or edited to empty/malformed) CLEARED the mandatory NTUSER.DAT/UsrClass.dat exclusions -> a later backup would copy+corrupt a live hive; size limits were zeroed when keys absent. New mandatoryDangerousFiles() static is the non-negotiable set (seeds initializeDefaults + re-unioned case-insensitively after parse so no supplied/hostile/partial manifest can drop it); fromJson now overrides a list ONLY when the JSON carries an array for its key (missing/non-array keeps seeded default), and sizes default to the seeded value when absent. Removed Q_ASSERT(!json.isEmpty()) from SmartFilter/FolderSelection/BackupUserData/BackupManifest fromJson (each degrades to defaults; loadFromFile of a truncated manifest no longer aborts debug). Cert: test_user_profile_types +6 (empty-json keeps defaults, explicit-empty dangerous still mandatory, custom unions mandatory, size default preserved, explicit list overrides, empty-object manifest/folder/userdata no-abort) 32/32; backup 9/9; restore 21/21; gates green.
- [x] B7-15 [HIGH] src/core/email_inspector_controller.cpp:116 -- public closeFile() can close/clear parser while QtConcurrent tasks still use it -> UAF. Fix: guard/await tasks. DONE (c5f6d5d): closeFile() now calls cancelOperation() (cancel workers + m_tasks.waitForFinished + clearFutures) BEFORE m_pst/mbox_parser->close(), mirroring the destructor, so no QtConcurrent pool thread is still inside the parser at close (use-after-close closed). Threading leg certified by inspection (mirrors already-certified cancelOperation path); compiles clean into sak_utility.
- [x] B7-16 [HIGH] src/core/email_inspector_controller.cpp:250 + email_profile_manager.cpp:206,227,332 -- discovery/backup/restore no state guards, run concurrently on shared vectors/maps, reset each other's cancel. Fix: single-flight guard. DONE (c5f6d5d): two-layer single-flight. Controller discover/backup/restore/generateReport now refuse when m_state != Idle (matching the existing search/export guards) so runTracked never launches two concurrent profile ops. Manager gained an atomic m_operation_active + ScopedActiveFlag RAII guard: discover/backup/restore refuse reentry (exchange(true)) even for direct callers, releasing on every return path -- so m_profiles/m_backup_dest_names/m_cancelled are never raced. Cert: test_email_profile_manager +1 singleFlightRefusesReentry (nested restore fired from inside profilesDiscovered while the flag is held is refused "already in progress", never runs -- no "Failed to open backup manifest") 27/27; gates green.
- [x] B7-17 [HIGH] src/core/conversion_report_generator.cpp:54,73,125 -- CSV export no formula-injection protection; email cells execute in Excel/Calc; headers unescaped. FIXED: replaced the quoting-only local csvEscape lambda with a new pure static csvSafeCell that (1) neutralizes formula injection -- a cell beginning with = + - @ or a leading tab/CR is prefixed with a single quote so a spreadsheet treats it as literal text (OWASP guidance; email subject/sender/message-id + MAPI display values are attacker-controlled) -- and (2) applies RFC 4180 quoting. It is now used for every data cell AND for the property-name header row (previously written raw, so a hostile property name was unescaped). Certified: test_conversion_report_generator 9/9 -- csvSafeCell_neutralizesFormulaInjection (=cmd/+/-/@ all prefixed), csvSafeCell_quotesButDoesNotPrefixSafeText (plain text unchanged, comma/quote quoted per RFC 4180, empty stays empty), csvManifest_neutralizesFormulaInSubject (=HYPERLINK subject lands as "'=HYPERLINK..., never a raw leading-= cell). lizard/clang-format green.
- [x] B7-18 [HIGH] src/core/email_inspector_controller.cpp:25 + html_email_writer.cpp:111 + mbox_writer.cpp:70 + email_export_worker.cpp:916,963 + email_profile_manager.cpp:748 -- short writes accepted as success -> truncated reports/messages/attachments. Fix: verify bytes written. DONE (5b2483f): new header-only sak::writeFully(QIODevice&, QByteArray) loops over partial writes, true iff every byte reached the device (empty=success), fails on stalled/negative write. Routed all 6 buggy sites through it (html write() return was ignored entirely; mbox only checked <0 -- a short write splits a message mid-stream; vcf/ics/manifest ignored return; report checked <0 only) so each fails closed on short/failed write. eml_writer already verified full length (unchanged). createBackupManifest client-type switch hoisted to a helper to stay under CCN gate. Cert: test_mbox_writer +3 (writeFully writes every byte; empty succeeds; capped stall-device -> failure) 12/12; html 11/11, export 15/15, manager 27/27; gates green.
- [x] B7-19 [HIGH] src/core/html_email_writer.cpp:119,131 -- attachment names sanitized but not deduped; colliding names truncate earlier files. Fix: dedupe. DONE (6ad3d8c): saveFileAttachments sanitized each name but wrote a fixed path, so two same-named attachments (common: image001.png per inline image) collided -- the second truncated the first while the HTML still linked it. Routed the save through the existing sak::saveAttachmentToDirectory which sanitizes the bare filename (QFileInfo::fileName first -> keeps the ../.. traversal containment), dedupes (photo.png -> photo_1.png) and writes via QSaveFile (no truncated file reported saved). PDF writer only lists names (no on-disk write), unaffected. Cert: test_html_email_writer +1 collidingAttachmentNamesDeduped (two same-named attachments -> two distinct files, both payloads present) 12/12; traversal-containment test still green; gates green.
- [x] B7-20 [HIGH] src/core/user_profile_backup_worker.cpp:188,225,297,219 -- backup reports success after missing folders / dir failures / elevation skips (don't increment m_filesErrored). Fix: count errors. DONE (5536b7a): several leaf failures returned false without counting, and elevation skips never affected success, so an incomplete backup read as "Backup complete!" success=true. Now each leaf failure is counted exactly once (callers only log/propagate -> no double count): backupUser per-user-root mkpath failure; backupFolder missing selected source + neither-file-nor-dir; copyDirectory dest-dir create failure; copyFileWithFiltering parent-dir create failure (file-copy failure already counted). Terminal success now requires m_filesErrored==0 AND m_filesElevationSkipped==0 (an elevation-skipped folder is unbacked data); intentional filter skips (m_filesSkipped: caches/*.tmp/oversized) still do NOT fail it. Cert: test_user_profile_backup_worker +2 (selected-but-missing folder -> failure with Errors=1; real readable folder -> success, no false failure) 11/11; gates green.
- [x] B7-21 [HIGH] src/core/user_profile_restore_worker.cpp:444,543 -- AssignToDestination never receives destination username -> always strips permissions. Fix: pass username. DONE (d03d354): copyFileWithConflictResolution called applyPermissions(path, "") with a hardcoded empty username, so PermissionMode::AssignToDestination ALWAYS hit the empty-user branch and stripped instead of assigning ownership -- the mode never worked. Threaded the mapping's effective destination user (explicit destination else source) into a new m_currentDestUser member set at the top of restoreUser() (safe: restore does one mapping at a time on the worker thread) and passed it to applyPermissions. Refactored the decision into pure testable statics resolvePermissionAction()/effectiveDestUser() with Win32 ownership moved to assignOwnershipToUser(). Cert: test_user_profile_restore_worker +2 (AssignToDestination+real user -> AssignOwnership, empty -> Strip, other modes map right; effectiveDestUser prefers destination then source; combined mapping resolves to AssignOwnership) 23/23; gates green.
- [x] B7-22 [HIGH] include/sak/migration_report.h:109 -- public getters use unchecked vector operator[] -> OOB on bad index. FIXED: both getEntry(int) overloads now bounds-check; a negative/out-of-range index returns a reference to a shared default entry instead of std::vector::operator[] UB (the const overload returns a static const empty; the non-const returns a static scratch that is reset on each OOB access so a stray write never leaks back or corrupts real entries). updateEntry/removeEntry/selectEntry were already guarded. Certified: test_migration_report 27/27 -- testGetEntryBoundsChecked (valid index returns real entry; index 1/999/-1 return empty; a write through getEntry(999) does NOT corrupt entry 0 and does not stick). clang-format green (header-only inline change; no lizard-relevant functions).
- [x] B7-23 [MED] email_inspector_controller.cpp:206,229 -- search/export with no open file sets busy but launches nothing -> stuck. Fix: reset state. DONE (c2e94ca): startSearch/exportItems setState(Searching/Exporting) BEFORE checking file type, so with no file open neither branch launched a task and the state latched busy forever (every later op refused). Both now refuse up front with "no file is open" (isFileOpen() guard), state unchanged. No controller test target; guard mirrors existing certified state guards + compiles clean into sak_utility (cert by inspection).
- [x] B7-24 [MED] email_inspector_controller.cpp:339 + mbox_parser.cpp:140,173 -- cancelling MBOX parser leaves cancel flag set; later reads fail until reopen. Fix: reset flag. DONE (c2e94ca): cancel() sets m_cancelled but only open()/indexMessages() cleared it, so after cancelOperation() every readMessages/readMessageDetail/readAttachmentData saw the stale flag and returned operation_cancelled until reopen. Each read worker now clears the flag at its start; readAllAttachments deliberately does NOT (a cancel arriving during an in-flight extraction still interrupts). Cert: test_mbox_parser +1 cancelDoesNotBlockLaterReads (readMessages + readMessageDetail both succeed after a stale cancel) 22/22; gates green.
- [x] B7-25 [MED] email_export_worker.cpp:532,1200 -- unreadable embedded attachments add error but message counts fully exported. Fix: mark partial. DONE (a07c982): collectAttachmentData/collectMboxAttachmentData now return AttachmentCollection{data, dropped}; exportOnePstItem/exportOneMboxItem count a message with any dropped attachment as items_failed (partial) -- specific loss stays in result.errors, recoverable body still written. Cert: mboxExportWithAttachmentSucceeds proves the readable case stays items_failed=0 (no false partial).
- [x] B7-26 [MED] email_export_worker.cpp:1058,1315 -- attachment-only PST export passes vector position not att.index -> wrong payload. Fix: use att.index. DONE (a07c982): exportAttachments passed the loop position att_idx to extractAttachment, fetching readAttachmentData(msg, position) -- wrong blob when position != att.index -- and indexed the vector by that value for the filename too. extractAttachment now takes the PstAttachmentInfo directly (fetch by att.index, name from att, write via writeFully so a truncated attachment doesn't count as extracted). PST leg has no OST fixture -> cert by inspection.
- [x] B7-27 [MED] email_export_worker.cpp:196,236 -- folder/mailbox paging silently stops on read error, exports partial without recording failure. Fix: record failure. DONE (a07c982): pageFolderItemIds now returns completeness; collectItemIds/collectMboxIndices record an error into result and ++items_failed when enumeration stops early on a read error, so a truncated export is never reported clean. Cert: mboxExportWithAttachmentSucceeds exercises the whole-mailbox paging path (item_ids empty) with items_failed=0 on a clean read.
- [x] B7-28 [MED] mbox_parser.cpp:178 + email_search_worker.cpp:179,192 -- body search uses result position not msg.message_index; one skip shifts later lookups. Fix: use message_index. DONE (6b68bd5): searchMbox passed the loop position into matchMboxItem, used as the index for readMessageDetail(); readMessages() skips unreadable messages so a skip shifted every later position and the body lookup fetched the WRONG message's body (while hit.item_node_id already used msg.message_index -> inconsistent hit). matchMboxItem now receives + looks up msg.message_index. Cert: test_email_search_worker +1 mboxBodySearchUsesMessageIndex (body-only search for a keyword unique to the 2nd message -> exactly one hit, item_node_id=1, match_field="body") 15/15; gates green.
- [x] B7-29 [MED] email_profile_manager.cpp:353,365 -- restore increments "profiles restored" regardless of rejected/missing/registry/copy failures. Fix: count only success. DONE (862cacb): restoreProfiles bumped the count once per manifest profile regardless of outcome (registry import failure, rejected traversal path, missing backup source, copy failure all still counted). restoreSingleProfile/restoreRegistryFromManifest/restoreOneDataFile now return bool; the count rises only for a profile with NO such failure. Intentional non-overwrite skip (destination present) is not a failure; a missing registry export or missing backup source now is. Cert: test_email_profile_manager +1 restoreCountsOnlyCleanProfiles (manifest whose sole profile references a backed-up file missing from the backup -> restoreComplete(0), not 1) 28/28; gates green.
- [ ] B7-30 [MED] user_data_manager.cpp:445,485 -- verifyBackup true for checksum-less metadata even if payload missing; compareChecksums true when both reads fail. Fix: require payload+hash.
- [ ] B7-31 [MED] user_data_manager.cpp:427 -- deleteBackup uses QFile::remove for directory payloads -> uncompressed backups undeletable. Fix: remove recursively.
- [x] B7-32 [MED] mbox_writer.cpp:95,120 -- existing outputs opened append -> rerun duplicates/merges old mail. Fix: truncate/new. DONE (db2f3c1): getOrCreateFile opened each mbox with QIODevice::Append, but a file is opened exactly ONCE per run (guarded by m_open_files) then written through the kept-open handle, so Append served no within-run purpose -- it only merged a new run's output onto a previous run's stale file, duplicating old mail. Now WriteOnly|Truncate: within-run messages still accumulate through the open handle, a rerun starts fresh. Cert: test_mbox_writer +1 rerunTruncatesStaleOutput (second writer to the same dir -> file has only the new message) 13/13; gates green.
- [x] B7-33 [MED] mbox_parser.cpp:438,535,648 -- any delimiter-prefixed line treated MIME boundary; non-multipart root attachment treated body -> corrupts valid messages. Fix: proper boundary parse. DONE (163a0ec): (1) splitMimeParts treated ANY line beginning with the boundary prefix as a boundary (trimmed.startsWith(delimiter)) -- a body line sharing the prefix ("--ABC" vs "--ABCDEF ...") truncated the body + fabricated parts; new isBoundaryDelimiterLine() accepts a line only when the text after the delimiter is empty (open) or exactly "--" (close) per RFC 2046 5.1.1 (trailing whitespace allowed). (2) parseMimeMessage sent every non-multipart body to parseSinglePart -> a single-part message that IS a file (lone application/pdf, or Content-Disposition: attachment) had its bytes decoded into garbled body text; the single-part path now routes an attachment/non-text root through appendAttachment. Cert: test_mbox_parser +2 (singlePartAttachmentNotTreatedAsBody -> one attachment doc.pdf + empty body; boundaryPrefixLineNotTreatedAsBoundary -> "--ABCDEF" line + following text survive in body) 24/24; gates green.
- [x] B7-34 [MED] Remaining email medium/low grab-bag. DONE across 3 commits (494787b, 041bfeb + docs). 10 of 12 fixed, 2 accepted-inherent:
  - [x] static progress counters race (backup+restore updateProgress) -> function-static lastFile/lastByte watermarks moved to per-instance members reset in start* (494787b).
  - [x] is_selected omitted from UserProfile JSON -> serialized + deserialized both ways; test asserts round-trip (494787b).
  - [x] migration import mutates before validate -> importFromJson validates entries array first, parses metadata+entries into locals, commits atomically; test rejected-import-preserves-metadata (041bfeb).
  - [~] PromptUser never prompts (restore ConflictResolution::PromptUser) -> ACCEPTED-INHERENT: a background QThread worker cannot show a modal; it auto-renames and logs the rename honestly, which is the correct headless fallback (interactive resolution belongs in the GUI layer before dispatch).
  - [x] compression level stored-not-used -> the worker copies verbatim; now logs "compression is not applied; files are copied uncompressed" when a level>0 is requested (an uncompressed backup is still valid so it does not abort) (494787b).
  - [~] email report per-type totals never populated (total_emails/contacts/...) -> ACCEPTED-INHERENT: nothing computes per-type counts (would need a full per-item classification pass at report time); the report shows the accurate total_items + search hits. generateCsv/Json render the fields correctly when populated (proven by existing tests).
  - [x] email_report CSV path unescaped -> file_path was interpolated raw into a "%1" slot; new csvCell() quotes/escapes (formula-injection prefix + doubled quotes); test csvEscapesFilePathQuote (041bfeb).
  - [x] advanced MAPI/attachment-name search never evaluated -> attachment-name already worked; added matchPstItemRecipients (search_recipients) + matchPstItemMapiProperty (mapi_property_id/value); matchPstItem refactored to a flat self-gating first-hit chain (041bfeb). PST-only paths cert by inspection (no OST fixture).
  - [x] backup filter always m_users[0] -> new m_currentUserProfile member set per user in backupUser() so exclusion rules key on the right user (494787b).
  - [x] HTML From double-escape -> pre-inserted &lt;/&gt; then escapeHtml re-escaped the & into &amp;lt;; now built with literal < > and escaped once; test fromHeaderNotDoubleEscaped (041bfeb).
  - [x] conversion title `???` placeholder -> em-dash mangled to ??? by the ASCII rule; fixed to " - " (041bfeb).
  - [x] inline-image uses filename not Content-ID -> HTML embedder now resolves cid:<Content-ID> from the attachment (filename fallback kept); test inlineImageEmbeddedByContentId (041bfeb).
- [x] CERT B7: thread start/destroy stress; malicious .reg/junction/`..` path tests; short-write tests; hostile HTML/CSV fixtures; suite green. DONE: all 34 findings closed (32 fixed + tested, 2 accepted-inherent in B7-34). Thread stress (B7-01/03 start-then-destroy x5, ost cancel-during-conversion), malicious .reg (B7-04 content-confinement) / junction (B7-07 GetFinalPathNameByHandleW) / `..` (B7-08 relative-path guard), short-write (B7-18 writeFully stall-device), hostile HTML (B7-05 CSP+sanitize) / CSV (B7-17/B7-34 formula-injection) fixtures all covered. Full Release ctest 191/191 (0 failed, 191s) -- every email-subsystem target green.

## BATCH 8 -- Files subsystem
- [x] B8-01 [HIGH] src/core/file_management_file_system.cpp:1451 -- local recursive delete no empty/root/containment guard; empty path -> CWD. Fix: guard. DONE (9e73d84): deleteDirectory ran QDir(path).removeRecursively() on a local target with no guard -- an empty path resolves to the CWD (wipes it), a root ("C:/","/") wipes the volume. New pure static isUnsafeLocalDeletePath() flags empty/whitespace + root (given and resolved-absolute form); deleteDirectory refuses with a blocker before touching disk. Cert: test_file_management_file_system +1 (empty/ws/C:/ /C:\\ / all flagged, real nested dir not; deleteDirectory("") ok=false deletes nothing; real nested delete still works) 27/27; gates green.
- [x] B8-02 [HIGH] file_management_file_system.cpp:1139 -- raw entry names joined onto host dest; `..`/separator escape export dir. Fix: basename confinement. DONE (e6b834d): exportEntryToHost joined raw entry.name (parsed verbatim from an untrusted foreign image) onto the host export dir, so "../../evil" or a name with separators escaped the root via mkpath/filePath/copyFileToHost. New pure static confinedHostName() reduces to a bare host filename (QFileInfo::fileName) + rejects "."/".."/empty; unsafe entries skipped. Cert: test_file_management_file_system +1 (plain kept; ../../evil.sh -> evil.sh; sub/dir/leaf -> leaf; ../.././empty rejected) 28/28; gates green.
- [x] B8-03 [HIGH] file_management_file_system.cpp:267 -- non-local HFS/APFS ignore inbound read_only when computing can_write_files -> write-protected raw targets writable. Fix: honor read_only. DONE (ff43ddd): applyCapabilities gated the read-only write-block on local_file_system, so a raw HFS/APFS target the inventory reported read-only still got can_write_files=true. Gate now keys on inbound_read_only (captured before target.read_only is forced true for the non-local browsing badge), blocking writes+organize for any write-protected volume local or raw. Cert: test_file_management_file_system +1 (known-size raw APFS writable when not read-only; is_read_only=true -> can_write_files=false + can_organize=false) 29/29; gates green.
- [x] B8-04 [HIGH] file_management_file_system.cpp:169 -- APFS/HFS option builders hard-code writer enable/confirmation/evidence -> bridge can't fail closed absent evidence. Fix: require evidence. DONE 5c73df7: added pure predicate targetPermitsMutation(target)=can_write_files && !read_only; apfsRawWriteOptions(target)/hfsWriteOptions(target) derive enable_experimental_writer/enable_writer + destructive_certification_evidence + raw_media_hardware_certification_evidence + target_write_confirmed from it, so a write-protected/non-writable target fails closed in the engine (was advisory badges only). Certified writable path unchanged. CERT test_file_management_file_system 30/30.
- [x] B8-05 [HIGH] file_management_file_system.cpp:1760 -- replace deletes sole case-insensitive match on case-sensitive HFSX/APFS -> replacing foo deletes Foo. Fix: case-correct match. DONE 6231e57: exact (case-correct) match still deleted directly; lone differing-case occupant now routed through pure seam rawReplaceCaseAction(fs, ci_count) -> HFS+ replace (same file), HFSX treat-as-vacant (distinct file, create exact name), APFS refuse (per-volume case flag not surfaced to bridge), 2+ refuse. No wrong-file deletion. CERT 31/31.
- [x] B8-06 [HIGH] src/core/file_explorer_transfer_worker.cpp:46 -- replace deletes existing dest before validating/copying source; failure loses original. Fix: copy-then-replace. DONE 21e15ac: transferEntry Replace now stages copy to sibling .sak-stage-*, moves occupant aside to .sak-old-*, atomically swaps staged->final, drops backup; rolls back to original on any failure so copy/rename failure never destroys original. transferRawStaged takes explicit dest; cleanup resolves kind at exec time. CERT test_file_management_explorer_panel 80/80 (replaceCopyFailurePreservesOriginalDestination). NOTE renameWithinTarget same-target move left as-is (source never at risk; only cited copy path fixed).
- [x] B8-07 [HIGH] src/core/file_explorer_archive_service.cpp:294 -- compression truncates existing archive + removes on failure -> destroys prior archive. Fix: temp-then-rename. DONE 6701f5e (already fixed by B6-19/20 747a312): QZipWriter(zip_path) truncation replaced with QFile WriteOnly|NewOnly exclusive-create; remove-on-failure only touches the file it created. DUPLICATE of B6-19/20. Certified compressRefusesToClobberExistingArchive: compress onto occupied path refuses, pre-existing file byte-intact. CERT 81/81.
- [x] B8-08 [HIGH] file_explorer_archive_service.cpp:145 -- extraction silently replaces existing files, no rollback on later failure. Fix: collision policy + rollback. DONE 213b504: collision policy already fail-closed (writeExtractedFile NewOnly, never clobbers). Added rollback: ExtractionRollback ledger records files we wrote (all NewOnly-created) + dirs we created; on mid-extraction abort performExtractionRollback deletes recorded files + rmdir (empty-only, never pre-existing) deepest-first. State bundled in ExtractionContext (param budget). CERT extractRollsBackPartialTreeOnFailure 82/82.
- [x] B8-09 [HIGH] file_explorer_archive_service.cpp:346,422 -- materializes whole ZIP central dir before entry cap; smart-root repeats unbounded alloc. Fix: cap before materialize. DONE 0f243a7: consolidated boundedZipEntries(zip_path,reader,error*) reads EOCD-declared central-dir size, refuses >kMaxCentralDirBytes(64MiB) BEFORE fileInfoList(), then entry-count cap; routed listEntries+extractZip+hasSingleTopLevelRoot through it (extractZip + smart-root previously skipped the bound). CERT extractRejectsOversizeCentralDirectoryBeforeMaterializing (tampered-EOCD fixture) 83/83.
- [x] B8-10 [HIGH] file_explorer_archive_service.cpp:136 -- each ZIP entry decompressed into one QByteArray; 4-GiB per-file limit permits exhaustion. Fix: stream + lower cap. DONE a914460: QZipReader has no streaming decode, so per-file cap = peak decode RAM bound; lowered kExtractMaxFileBytes 4GiB->512MiB (declared uncompressed size already checked pre-decode in extractZipEntry). CERT extractRejectsEntryDeclaringOversizeUncompressed (central-dir uncompressed-size patched to ~2GiB) 84/84.
- [x] B8-11 [HIGH] src/core/xz_decompressor.cpp:20 -- lzma_auto_decoder given UINT64_MAX memlimit -> crafted header unbounded memory. Fix: real memlimit. DONE dcf0b0a: kXzDecoderMemLimitBytes=512MiB (covers xz -9/-9e 64MiB dict + large custom; oversized-dict header -> LZMA_MEMLIMIT_ERROR via decompressStep). CERT realXzStreamDecodesUnderMemLimit (real default-preset xz round-trips) 15/15.
- [x] B8-12 [HIGH] src/core/streaming_decompressor.cpp:108 -- first stream-end treated final EOF; drops concatenated members, accepts trailing garbage. Fix: continue/verify. DONE efc7532: on member stream-end, EOF only when inputEmpty && file.atEnd; else resetStreamForNextMember() (new per-backend hook: gzip inflateReset / bzip2 End+Init / xz lzma_end+auto_decoder, all preserve I/O ptrs) + continue. Trailing garbage -> continued decode fails closed. pumpDecoder split into onMemberEnd()/isTruncatedStream() for CCN. CERT concatenatedGzip/Xz MembersDecodeFully + trailingGarbageAfterMemberFailsClosed 18/18. (pigz multi-member gzip / cat'd xz were silently truncating flashed images.)
- [x] B8-13 [HIGH] src/core/file_recovery_engine.cpp:344 -- recovered files use truncating QFile, ignore short-write/close, report success after disk-full/partial. Fix: verify writes. DONE 4a51559: writeRecoveredFile now QSaveFile + writeFully + commit (atomic; short write cancels, commit fails closed -> no truncated file reported as success). CERT restore test verifies every restored file byte-complete by content hash 223/223.
- [x] B8-14 [HIGH] file_recovery_engine.cpp:420 -- candidate scan O(scan x max_candidate) on repeated unterminated signatures -> crafted-image CPU DoS. Fix: bound. DONE 1ff28c9: threaded forward-scan work counter through png/jpeg/pdf size finders + matchAt; appendScanCandidates bounds cumulative work to 4x data size + 64MiB floor (kScanWorkBudgetFactor/FloorBytes), stops with scan_cancelled + "bounded" warning (partial). CERT fileRecoveryEngine_boundsHostileUnterminatedSignatures (2MiB %PDF- repeat returns bounded, no hang) 225/225.
- [x] B8-15 [MED] file_recovery_engine.cpp:169 -- candidate id joined without basename validation -> `../` traversal outside restore dir. Fix: basename. DONE 1ff28c9: restoredFilePath confines candidate.id via QFileInfo::fileName (drops path components); ""/"."/".." -> empty return -> restoreCandidate skips with warning. CERT fileRecoveryEngine_confinesCraftedCandidateNames ("../pwned.bin" confined inside restore, never escapes; ".." skipped) 225/225.
- [x] B8-16 [HIGH] src/core/file_scanner.cpp:338 -- default scan blocks only is_symlink() dirs; Windows junctions followed, symlink files target-stat'd -> root escape + UNC cred leak. Fix: reparse-aware. DONE c388eb2: canDescendInto now gates recursion on isReparsePointEntry (non-following reparse-attr check) not is_symlink(), so a Windows JUNCTION (file_type::junction, is_symlink misses) is refused by default too -> no root-escape. In-app injectable callers already set skip_symlinks (drops reparse before this + suppresses target-following stat / UNC leak); default symlink-FILE emit unchanged (documented GUI behavior, skip_symlinks is the untrusted opt-in). CERT junction_notFollowedByDefault (mklink /J loop found once) 21/21.
- [x] B8-17 [HIGH] file_scanner.cpp:49 -- concurrent scans on header-documented thread-safe instance race m_visited_dirs/counters -> UB. Fix: sync or document not-thread-safe. DONE bbb2ea8: header claimed "Thread-safe" but m_visited_dirs (plain unordered_set, cleared+written per scan) + reset counters race on concurrent same-instance scans. All callers already use a local instance per scan; corrected contract to "not thread-safe, one scan()/instance, instance per thread; atomics only for cross-thread live-progress read". Doc-only.
- [x] B8-18 [MED] file_management_file_system.cpp:930 -- max_bytes+1 wraps to zero for UINT64_MAX -> disables raw-file read cap. Fix: guard wrap. DONE 0ce6ff1: readCapProbe(max_bytes) saturating (0 stays 0, UINT64_MAX stays UINT64_MAX, else +1) at local read + both raw copy-out sites; readLocalFile reads whole file via readAll when probe exceeds QFile::read arg range (size already <= cap). CERT readFileWithMaxUint64CapReadsWholeFile 32/32.
- [x] B8-19 [MED] file_management_file_system.cpp:1186 -- dir export/import ok=true despite depth/entry/symlink/capped-file omissions. Fix: surface omissions. DONE: ok doc overclaimed "every reachable file exported completely" while code set ok=blockers.isEmpty(), so symlink/entry-bound/capped-file skips still read ok=true (a capped file = truncated but counted a success). Added a distinct `complete` flag to both Directory{Export,Import}Result = ok AND zero symlinks_skipped/entries_skipped(/capped_files); corrected the ok doc to "finished without a hard blocker". Routed transfer_worker's manual re-derivation through !result.complete (single source of truth). CERT: importDropsEntriesPastDepthBoundWithSignal asserts ok==true && !complete; happy-path export/import assert complete==true; test 32/32, panel 84/84 (also de-flaked folderCopyPaste wait: inner.txt sorts after deep/ so it lands last).
- [x] B8-20 [MED] file_management_file_system.cpp:312 -- listing omits QDir::Hidden/System (show-hidden broken); maxEntries applied after full materialize. Fix: include flags; cap early. DONE: listLocalDirectory used entryInfoList(Dirs|Files|NoDotAndDotDot) so hidden/system entries never reached the model -- the view's show-hidden toggle (FileExplorerSortFilterModel filters on entry.hidden/leading-dot) was dead, and files a folder copy would still transfer were invisible. Rewrote to stream via QDirIterator with QDir::Hidden|QDir::System and a bounded top-N max-heap (collectLocalEntries): keeps the display-order-smallest max_entries in O(cap) memory instead of stat'ing+materializing the whole (possibly hostile/huge) directory before capping. Preserves sorted-prefix truncation (verified a/b/c) with the existing "Listing truncated" warning. CERT: listLocalDirectoryEnumeratesHiddenEntries (SetFileAttributesW +hidden, rides through as entry.hidden) + listLocalDirectoryCapsToSortedPrefixWithWarning; test 34/34, panel 84/84.
- [x] B8-21 [MED] Archive/service correctness: file_explorer_archive_service.cpp:335 (readability-only -> empty list ok=true), :223 (entry cap counts only files, dirs bypass), :311 (missing sources -> warnings, partial ok); file_explorer_archive_worker.cpp:46 (partial subset archived+completed), :249 (collision check discards listing failure), :194 (exhausted name -> childPath("")). Fix: per-item. DONE: (readability) ALREADY CLOSED by B8-09 boundedZipEntries -- a non-zip fails closed (central_dir_size<0), only a genuinely empty VALID zip lists 0 honestly. (dirs bypass cap) addDirectoryEntries now counts the wrapper dir and every subdir toward result->entries so a directory-only tree can no longer slip past the loop's entries<kArchiveMaxEntries guard. (missing source) compressToZip routes each source through addCompressSource: a requested source that no longer exists is a BLOCKER (ok=false -> exclusive-created archive removed), only a real special file stays a warning. (partial subset) runCompress uses collectValidatedSources -> if a raw source failed to stage (fewer host sources than requested), fail closed with a blocker instead of compressing a silent subset. (collision listing failure) destinationOccupied treats a failed raw listing as OCCUPIED (cannot prove free -> fail closed, never deliver into an unverified dest). (exhausted name) deliverFlattened skips + blocks when availableChildName returns empty, so childPath("") never targets the directory itself. CERT: service items 2+3 direct -- compressToZip_countsDirectoriesTowardEntryCap (entries==3 for wrapper+sub+file) + compressToZip_missingSourceFailsClosed (ok=false, partial archive removed); test_file_explorer_archive_service 6/6; worker items 4/5/6 are raw error-path guards covered by test_file_management_explorer_panel 84/84 (drives runCompress/deliverFlattened) + inspection. Refactor kept CCN<=10 (addCompressSource, collectValidatedSources). Panel entry-count assertion updated 3->5 (dirs now counted).
- [x] B8-22 [MED] Model/UI correctness: file_explorer_group_proxy_model.cpp:90 (dataChanged no regroup), :39 (nested resets); file_explorer_item_model.cpp:335 (sort permutes without remapping persistent indexes); file_explorer_status_center.cpp:301 (terminal progress leaves stale state); file_explorer_command_registry.cpp:253 (read-gating omits compress/extract/flatten). Fix: per-item. DONE: (sort persistent indexes) FileExplorerItemModel::sort emitted layoutChanged without remapping persistent indexes -- a Qt-contract violation that silently retargeted the selection/current/open editor; now snapshots each held index's entry path pre-sort and changePersistentIndexList to its new row (remapPersistentIndexesByPath helper). (dataChanged regroup) group proxy forwarded an in-place dataChanged as a repaint even when it changed the grouping key (a tag refresh under group-by-tag left the row stranded in its old section); added dataChangeAffectsGrouping (maps m_option->key role via groupingKeyRole) and regroups through the selection-preserving layout-change path when the key role changed. (nested resets) every source "aboutTo" routed straight to beginResetModel, so a batched source could nest it (Qt forbids); added a depth-guarded beginSourceReset/endSourceReset so only the outermost bracket opens the proxy reset. (terminal progress) reportProgress set m_return_result on a terminal status but never re-derived state, so the card spun forever/still counted in-progress; added applyTerminalTransition to flip kind/icon and clear in_progress/cancelable/indeterminate. (read-gating) isReadCommand now includes CompressIntoZip + all Extract*/FlattenFolder (they read sources/archive), so a writable-but-unreadable target disables them. CERT: itemModelSortRemapsPersistentIndexes + groupProxyRegroupsOnGroupingKeyChange (test_file_explorer_item_model 20/20); statusCenterCardTransitionsOnTerminalProgress + registryReadGatesCompressAndExtract (test_file_explorer_types 30/30); test_file_management_explorer_panel 84/84. All functions CCN<=10 (extracted remapPersistentIndexesByPath, connectDataChangedSignal).
- [x] B8-23 [MED] Hash/recovery correctness: file_hash.cpp:46 (zero chunk -> empty digest release), :105 (negative char to ::tolower UB); file_recovery_engine.cpp:482 (prefix compare claims whole-source unchanged), :389 (short read accepted complete); deleted_item_scanner.cpp:107 (per-item read fail not clearing reliable); decompressor_factory.cpp:78 (ZIP advertised, create() null); smart_file_filter.cpp:61 (invalid exclusion fail-open); recycle_bin.cpp:22 (empty/NUL/wildcard to SHFileOperationW). Fix: per-item. DONE: (zero chunk) file_hasher ctor coerces chunk_size 0 -> DEFAULT_CHUNK_SIZE (a release build otherwise hit file.read(0) -> empty buffer -> hashed the EMPTY-input digest as the file). (tolower UB) verifyHash routes each char through unsigned char before std::tolower (a garbled expected hash byte >127 is negative -> UB). (prefix compare) restoreCandidates.source_not_mutated only ever covered the hashed WINDOW; added source_hash_covered_whole (pure hashCoveredWholeSource seam: cap 0 or cap>=size -> whole) so a prefix match is not read as whole-source; app-action gate + JSON now expose+require it (production uses cap 0 -> unchanged). (short read) already fail-closed (readCandidateBytes/readSequentialBytes return {} on short read -> hash mismatch -> skipped; write path atomic since B8-13). (per-item read fail) deleted_item_scanner now clears m_recoverable_reliable when a per-item readItemDetail fails (non-cancel), mirroring the folder-items handling; extracted appendRecoverableBatch to keep CCN<=10. (ZIP advertised) removed zip from decompressor_factory's ext+magic tables so detectFormat/isCompressed/create agree (zip is an archive handled by the archive service, and the codebase already classes it in kArchiveExtensions, not kImageExtensions). (invalid exclusion) user_profile_backup_worker surfaces SmartFileFilter::invalidPatterns() as a warning so dropped exclusions (fail-open -> files backed up that the user meant to skip) are visible. (recycle) sendPathToRecycleBin refuses empty/wildcard(*,?)/embedded-NUL BEFORE SHFileOperationW (wildcards EXPAND -> mass recycle; embedded NUL splits the double-null list into extra targets). CERT: constructor_zeroChunkCoercedToDefault + verifyHash_highByteExpectedNoUb (test_file_hash 24/24); fileRecoveryEngine_prefixHashDoesNotClaimWholeSource + restore covered-whole assert (test_partition_manager_core 226/226); zipDetectionMatchesCreateCapability (test_decompressor_factory 19/19); new test_recycle_bin 5/5 (empty/wildcard/NUL rejected); smart-filter invalidPatterns seam already covered (excludePattern_invalidRecorded); app-action source_not_mutated regression green (tool_dispatch 266/266); backup worker 11/11. deleted_item_scanner per-item guard covered by inspection (no cheap PST-parser mock).
- [x] B8-24 [LOW] file_explorer_transfer_worker.cpp:282 (dir progress cannot reach total); grouping.cpp:34 (decimal thresholds labeled GiB/MiB); properties_calc.cpp:33 (exact-cap dir always incomplete); session_store.cpp:43 (unranged enum cast); include/sak/file_hash.h:149 (hashToHex declared no definition -> link fail for callers). Fix: per-item. DONE: (dir progress) discover() recursed into directories counting every nested entry into the items DENOMINATOR while transferItems advances the numerator once per TOP-LEVEL selection, so a directory transfer's item progress could never reach total; discoverHostTree/discoverRawTree now sum nested BYTES only (byte total drives the copy percentage) and no longer inflate the item count. (grouping units) size-bucket range labels said GiB/MiB/KiB (binary) over decimal thresholds; relabeled to GB/MB/KB to match the actual SI thresholds (test asserts bucket names, not ranges). (exact-cap incomplete) treeSize used >= cap so an exactly-full dir read as truncated; now probes cap+1 and marks incomplete only when entries > cap (mirrors B8-19/20). (unranged enum) readPane cast persisted ints straight to view enums; added validatedEnum (clamps to [0,max] with a default fallback) for viewMode/sortOrder/groupOption/groupOrder/groupDateUnit/folderPlacement AND the load()-side split enum, so corrupt/forward-version settings fall back instead of yielding an out-of-range enum. (hashToHex) dead declaration with no definition and no callers -> removed (a latent link failure the moment anyone called it). CERT: entryCapMarksIncomplete rewritten (exact-cap complete + over-cap incomplete; FakeFs now honors the cap) test_file_explorer_properties_calc 9/9; corruptEnumValuesFallBackToDefaults test_file_explorer_session_store 7/7; grouping name test green (item_model 20/20); dir-progress covered by panel 84/84 + inspection.
- [x] CERT B8: zip-bomb + traversal + symlink/junction + recursive-delete-guard fixtures; suite green. DONE: full Release offscreen ctest 192/192 passed, 0 failed (grew 191->192 with new test_recycle_bin). Security fixtures all green together: zip-bomb + zip-slip traversal + reparse-point crossing (B8-08/09/10 extractRejectsOversize*, extractRollsBack, boundedZipEntries), symlink/junction non-descent (B8-16 junction_notFollowedByDefault), replace-rollback preserving originals (B8-06), concatenated-member + trailing-garbage decode (B8-12), recycle wildcard/NUL/empty guard (B8-23 test_recycle_bin). B8 FILES BATCH FULLY CLOSED.

## BATCH 9 -- Network subsystem
- [x] B9-01 [CRIT->DEAD] src/core/imap_uploader.cpp:510,201 -- mismatched message/flag/date vectors rely on Q_ASSERT; release OOB at(). NOTE: ImapUploader has NO caller (dead code) -- latent until OST->IMAP wired. Fix: bounds-check (still fix) or gate behind wiring. DONE: uploadFolder now range-checks eml_contents/flags_list/dates lengths and returns error_code::invalid_argument (fail closed) instead of the compile-out Q_ASSERT that let a release OOB at() through. CERT uploadFolder_rejectsMismatchedVectorLengths.
- [x] B9-02 [CRIT->DEAD] imap_uploader.cpp:157,217,312 -- CREATE/APPEND reject + oversized skip increment failures but session finishes success (silent partial/zero migration). Fix: fail session. DONE: appendNext finishes success only when result.failed==0; any oversized-skip / APPEND-reject increments failed and the session finishes error_code::partial_failure ("N of M failed"), so a caller never reads a partial/zero migration as complete. Covered by inspection (session logic; full run needs a live server).
- [x] B9-03 [HIGH->DEAD] imap_uploader.cpp:201 -- caller/PST flags joined into APPEND without token validation -> CRLF/paren injection. Fix: validate tokens. DONE: isValidImapFlag (optional leading backslash then RFC3501 atom chars only; rejects CR/LF/space/parens/braces/quote/wildcards); flagsForCurrentMessage drops invalid flags before joining into the "(...)" list. CERT isValidImapFlag_accepts/rejects.
- [x] B9-04 [HIGH->DEAD] imap_uploader.cpp:238,432 -- use_ssl=false sends creds+bodies plaintext, no STARTTLS. Fix: enforce TLS. DONE: STARTTLS is not implemented, so the worker refuses use_ssl=false at start (connection_failed "Refusing plaintext IMAP; enable SSL/TLS.") BEFORE any socket connect or credential send. CERT testConnection_refusesPlaintext.
- [x] B9-05 [HIGH->DEAD] imap_uploader.cpp:334,362 -- parser accepts tags/`+` anywhere not line prefix -> false success/premature literal. Fix: line-prefix parse. DONE: hasCompleteLineWithPrefix scans only at CRLF line boundaries; bufferHasCompleteTaggedLine / taggedLineIsOk / the '+' continuation check all route through it, so a forged "tag OK" in a body/untagged line no longer counts and a stray '+' no longer sends the literal early. CERT taggedLineIsOk_onlyMatchesAtLineStart + hasCompleteLineWithPrefix_continuationOnlyAtLineStart. (New test_imap_uploader target, 8/8; exposed the 3 pure validators as static seams.)
- [x] B9-06 [HIGH] src/core/network_share_browser.cpp:225 -- write probe uses 8 UUID hex, opens without NewOnly; collision/race truncates+deletes existing remote file. Fix: full UUID + NewOnly. DONE: testReadWriteAccess now uses the FULL UUID (dropped the .left(8) prefix + the now-dead kTemporaryFileUuidChars) and opens WriteOnly|NewOnly, so a name collision or a planted file makes open() fail (canWrite stays false) instead of truncating+deleting a pre-existing remote file; remove() only ever deletes the probe we exclusively created. SMB-bound probe -> verified by build + inspection (no live-share unit harness).
- [x] B9-07 [HIGH] src/core/ethernet_config_manager.cpp:179,226,289 -- DHCP restore enables DHCP DNS then replaces captured servers with static; DNS-reset failure ignored. Fix: correct order + honor failure. DONE: restoreSettings ran restoreDnsServers UNCONDITIONALLY, so a DHCP-snapshot restore (which just set DNS to automatic via restoreDhcpMode) had that automatic DNS immediately overwritten with the captured STATIC servers -- a "DHCP" restore left pinned to static DNS. Now the static-DNS leg runs only on the !dhcpEnabled path; the DHCP path keeps automatic DNS and reports the actual DNS outcome via dnsApplied (restoreDhcpMode already captures the set-dnsservers result). Static path threads restoreDnsServers' primaryApplied into *dnsApplied. netsh-bound (changes host network) -> verified by build + inspection; the existing test covers snapshot serialization only.
- [x] B9-08 [HIGH] src/core/active_connections_monitor.cpp:109 -- TCP/UDP table-read failure -> empty snapshot, emits every prior connection closed, destroys baseline. Fix: preserve on error. DONE: refreshNow now checks m_lastRefreshError (tcp||udp read failure) BEFORE detectChanges: on a read error the empty/partial snapshot would diff every baseline connection as closed and then overwrite m_lastConnections (wiping the baseline; next good refresh re-reports all as new). On error it emits connectionsUpdated(m_lastConnections) (stable last-good view) and returns without diffing or overwriting. OS-API-bound (GetExtendedTcpTable failure not injectable) -> verified by build + inspection; existing test scopes lifecycle/config only.
- [x] B9-09 [HIGH] src/core/firewall_rule_auditor.cpp:287,416 -- COM failure emits errorOccurred but public ops still emit empty rulesEnumerated/auditComplete -> failed audit looks clean. Fix: don't emit clean on failure. DONE: enumerateViaCOM sets m_enumerationOk=false on the COM-init/partway-break failure paths (it already emitted errorOccurred + returned empty). enumerateRules now guards the rulesEnumerated emit on m_enumerationOk, and fullAudit returns without emitting auditComplete when enumeration failed -- so a failed security audit never also delivers a clean empty result. An empty-but-successful enumeration still emits. COM-bound -> build + inspection.
- [x] B9-10 [HIGH] firewall_rule_auditor.cpp:140,218 -- per-rule COM getter failure -> defaults, extraction still succeeds, hides rules. Fix: surface per-rule failure. DONE: added FirewallRule::complete (default true); every posture/scope getter (enabled/direction/action/protocol, local+remote ports+addresses, app/service/profiles) sets complete=false on failure so a defaulted field (Allow/Inbound/Any/empty) is not trusted as fact; enumerateViaCOM counts incomplete rules and emits an errorOccurred warning (not an enumeration failure, so the flagged rules are still returned). COM-bound -> build + inspection.
- [x] B9-11 [HIGH] firewall_rule_auditor.cpp:485,628,675 -- conflict logic ignores addresses/remote ports/service/selectors; SMB-gap misses wildcard/empty-port; unknown expr fails open no-overlap. Fix: full selector compare. DONE: (unknown expr) portsOverlap now treats a non-wildcard expression that parses to NO ports (a named service like "RPC") as conservatively OVERLAPPING rather than no-overlap, so it never hides a conflict (fail-safe). (selectors) rulesOverlap -> exposed as static rulesConflict and now requires BOTH local AND remote ports to overlap plus matching application AND service (servicesMatch) -- a pair disjoint on remote ports or bound to different services no longer registers as a false conflict; addresses stay conservatively-overlapping (CIDR disjointness out of scope, documented, never hides a conflict). (SMB gap) checkSmbGap now treats localPorts=="*" as covering 445 (mirrors checkRdpGap), catching a wildcard rule that exposes SMB on Public. CERT: portsOverlap_unknownExpressionOverlapsConservatively + rulesConflict_requiresAllSelectorsToOverlap; WIRED the orphaned test_firewall_rule_auditor into CMake (was never built) -> 17/17 (pure seams + live-firewall enumeration). SMB-gap wildcard verified by inspection (mirrors tested checkRdpGap).
- [x] B9-12 [HIGH] src/core/network_diagnostic_report_generator.cpp:91,105 + ethernet_config_manager.cpp:120 -- HTML report closes QFile before flush, never checks stream, always emits reportGenerated; writers truncate non-atomically. Fix: flush+check, atomic. DONE: generateHtml now writes via QSaveFile, scopes the QTextStream so it flushes AND is destroyed before commit() (the old code closed the file while the live stream still held buffered data -> dropped tail), checks out.status(), and emits reportGenerated ONLY after a successful commit (cancelWriting + errorOccurred otherwise). generateJson + ethernet saveToFile likewise moved to QSaveFile + writeFully + commit (atomic: a failed/short write never truncates a pre-existing report/backup). CERT: generateHtml_writesCompleteFileAndEmitsSignal (file byte-identical to toHtml() after CRLF-normalize, reportGenerated once, no error) + generateJson_writesValidJsonAndEmitsSignal (test_network_diagnostic_report 30/30); saveToFile_atomicRoundTrip incl. overwrite (WIRED orphaned test_ethernet_config_manager -> 15/15). Note: :256 DNS-type HTML injection is a B9-18 item.
- [x] B9-13 [HIGH] src/core/wifi_analyzer.cpp:191 -- auth/encryption matched to every BSSID sharing SSID -> evil-twin mislabeled secure. Fix: per-BSSID. WHAT: security now derived per-BSSID from THIS AP's own beacon (capability Privacy bit + RSN/WPA information elements) via new pure static WiFiAnalyzer::deriveBssSecurity; networkFromBssEntry sets auth/encryption/isSecure from it. The SSID-wide available-network aggregate (applyAuthAndEncryption) no longer flips isSecure -- it only refines the auth/cipher LABEL when its security class matches the BSSID's own, so an Open evil-twin sharing a secured SSID stays insecure. HOW: scanSecurityIes walks {id,len} IEs (RSN=48, WPA vendor=221/00:50:F2:01), rsnHasSae detects SAE AKM (00:0F:AC:08) -> WPA3; truncated-IE walk bounded (no over-read). CERT: wired ORPHANED test_wifi_analyzer into CMake; added 7 deriveBssSecurity tests (open/WEP/WPA1/WPA2/WPA3/evil-twin-open-stays-insecure/truncated-no-crash); test_wifi_analyzer 24/24 green Release offscreen; lizard+clang-format pass.
- [x] B9-14 [HIGH] wifi_analyzer.cpp:251 -- OUI DB unsynchronized mutable statics; concurrent scans race loaded/QHash -> UB. Fix: sync/once. WHAT: replaced the hand-rolled `static QHash db; static bool loaded=false;` + unlocked mutate-on-first-call with a single `static const QHash db = []{...}();` function-local static -- C++11 guarantees the initializer runs exactly once even under concurrent first calls, so parallel scans can no longer race the flag/QHash fill. HOW: extracted loadOuiDatabaseFile (parse first openable candidate) + seedFallbackVendors (built-in set) helpers; the init lambda loads then seeds only when empty. CERT: added lookupVendor tests (known fallback OUI 00:50:56->VMware proves once-init populated; unknown prefix + short input -> empty); test_wifi_analyzer 27/27 green Release offscreen; lizard+clang-format+cppcheck pass.
- [x] B9-15 [HIGH] src/core/wifi_setup_script.cpp:114 -- plaintext password XML to predictable %TEMP%\wifi_profile_sak.xml; race + creds left after failure. Fix: unique temp, wipe. WHAT: the generated .cmd no longer writes the passphrase-bearing WLAN profile XML to a fixed %TEMP%\wifi_profile_sak.xml (pre-createable/symlinkable by a local attacker, collides across runs). It now allocates a kernel-chosen unique file via PowerShell [System.IO.Path]::GetTempFileName() (atomically creates a uniquely named 0-byte file), guards `if not defined PROFILE_XML`, decodes into it with Set-Content -LiteralPath $env:PROFILE_XML, and deletes it on BOTH the add-profile failure and success branches so creds do not linger. NOTE: the runtime connectWifiWindows path already used QTemporaryFile XXXXXX + autoRemove -- unchanged. CERT: added usesUniqueTempFileAndWipesOnBothPaths (asserts the old filename is gone, GetTempFileName present, and exactly two `del "%PROFILE_XML%"` calls); test_wifi_setup_script 13/13 green Release offscreen; lizard+clang-format+cppcheck pass.
- [x] B9-16 [HIGH] src/core/bandwidth_tester.cpp:243,505 -- iPerf firewall add/remove async under one global rule name; races leave rule open / delete other instance's rule. Fix: unique rule per instance. WHAT: dropped the fixed global kFirewallRuleName="SAK_Utility_iPerf3". Each server start now composes a UNIQUE inbound rule name via new pure static composeFirewallRuleName(port, uniqueToken) seeded with QUuid::createUuid().toString(Id128), stored in m_firewallRuleName. HOW: createFirewallRule/removeFirewallRule now take the rule name; the finished + FailedToStart teardown lambdas CAPTURE the server's own ruleName by value (not m_firewallRuleName, which a later server may have overwritten) so each server only deletes its own rule; removeFirewallRule("") is a no-op (never nukes an unnamed rule set). Extracted connectServerProcessSignals helper to keep startIperfServer under the length cap. CERT: wired ORPHANED test_bandwidth_tester into CMake; added composeFirewallRuleName tests (contains port+token, stable for same inputs, distinct tokens -> distinct names, no quote/space); test_bandwidth_tester 14/14 green Release offscreen; lizard+clang-format+cppcheck pass.
- [x] B9-17 [HIGH] src/core/connectivity_tester.cpp:282,353,480 -- configs lack bounds: huge packet/max-hop allocate attacker-sized; negative delay/timeout cast multi-day; TTL/payload truncate. Fix: validate bounds. WHAT: ping/traceroute/mtr now run their config through a new pure static ConnectivityTester::sanitizeConfig overload (per config type) FIRST, clamping every numeric field to a safe range before any loop/alloc/cast: count[1..100000], intervalMs[0..60000] (negative would become a multi-day unsigned msleep), timeoutMs[1..60000] (negative -> huge DWORD), packetSizeBytes[0..65500] (>WORD overflows the IcmpSendEcho size cast + requests a giant buffer), ttl/maxHops[1..255] (UCHAR ceiling; hop==TTL), probesPerHop[1..10], cycles[1..100000]. Bounds constants added to netdiag namespace. All sendIcmpEcho call paths now receive bounded ttl/packet/timeout. HOW: entry points take rawConfig, do `const cfg = sanitizeConfig(rawConfig)`. CERT: wired ORPHANED test_connectivity_tester into CMake (also runs a live localhost ping); added 4 sanitize tests (over-range clamps to max, zero/negative clamps to min, valid passes through, for all three config types); test_connectivity_tester 18/18 green Release offscreen; lizard+clang-format+cppcheck pass.
- [x] B9-18 [MED] Network mediums grab-bag. Per-item: (share:163) enumerateShares now LOOPS on ERROR_MORE_DATA via resumeHandle (new appendSharesFromBuffer helper, void* to keep LAN Manager headers out of the public header) instead of processing one partial buffer as complete. (report:256) DNS record type now toHtmlEscaped() in the HTML report (+WiFi bssid/band/authentication/apVendor escaped for defense-in-depth); test html_dnsRecordType_htmlEscaped. (dns:353) flushDnsCache no longer emits dnsCacheFlushed() (the success signal) after a failure -- returns after errorOccurred. (dns:70) query/compareServers reject unsupported record types via new pure isSupportedRecordType instead of silently issuing an A query. (dns:232) reverseLookup requires four numeric 0..255 octets via new pure isNumericIpv4 (dropped the Q_ASSERT-empty). (dns:264) multi-server answer compare is now order-insensitive via new pure answersEquivalent (round-robin no longer flags false disagreement). (active-conns:323) detectChanges gated on a new m_hasBaseline flag not m_lastConnections.isEmpty(), so a genuinely-empty prior snapshot no longer suppresses the next refresh's new connections. (connectivity:365) ping reports result.sent = actual attempts (replies.size()), not the configured count, so a cancelled run's loss stats are honest. (bandwidth:402) parseIperfJson is now a pure static returning std::optional -- nullopt on parse error OR missing `end` throughput summary; runIperfTest emits errorOccurred instead of a bogus all-zero success; tests parseIperf_*. (bandwidth:164) measureHttpHeadLatencyMs returns -1 on HEAD failure (caller maps to 0) rather than reporting the full timeout as RTT. (wifi:223) triggerScanAndWait registers for the ACM scan-complete/scan-fail notification and waits bounded, replacing the blind fixed sleep that returned STALE cached results (falls back to the sleep only if WlanScan/registration fails). (wifi:397) continuous-scan timer keeps the last good scan on a transient read failure instead of clobbering m_lastScan with an empty list. (wifi_profile:76,110) per-profile detail-read failures counted + surfaced via logger; Q_ASSERT-empty removed from both parsers. (wifi_setup:182) SSID now enforced to the 32-OCTET WLAN limit (UTF-8 bytes) in both buildWifiSetupScriptWindows and connectWifiWindows (was 64 chars); test refusesOverlongSsid. (port_scanner:377) closed-port classified via the QAbstractSocket::ConnectionRefusedError ENUM, not the localized "refused" string, so it works on non-English Windows. CERT: test_dns_diagnostic_tool (wired orphan) 17/17, test_bandwidth_tester 17/17, test_wifi_setup_script 14/14, test_connectivity_tester 18/18, test_network_diagnostic_report 31/31, test_wifi_analyzer 27/27, test_wifi_profile_scanner 11/11, test_network_diagnostic_utils 51/51 -- all Release offscreen; lizard+clang-format+cppcheck pass. DEFERRED (not a quick fix): wifi_profile_scanner still parses localized netsh TEXT (parseWifiProfileNames/parseWifiSecurityType key off English "Profile"/"Authentication"); a full locale-independent fix means rewriting on WlanGetProfileList/WlanGetProfile -> logged to [[assistant-dominion-deferred]]. Also retained: resolveWlanAuth maps unknown/empty security -> WPA2-PSK as an intentional secure default. See B9-18-DEFERRED below.
  - [ ] B9-18-DEFERRED [MED] wifi_profile_scanner.cpp:76,95 -- profile-name and security-type parsing keys off English netsh strings ("Profile", "Authentication"); non-English Windows yields empty. Fix: rewrite on WLAN API (WlanGetProfileList/WlanGetProfile) instead of netsh text.
- [x] B9-19 [MED] Network missing. Per-item: (network_transfer_runner:117) added NetworkTransferRequest::max_response_bytes (0 = unlimited); the runner aborts in downloadProgress when the reported total or running received exceeds the cap (bounds QNetworkReply's buffer to ~cap) AND rejects an over-cap single-shot body in finish(), so a hostile/oversized response can no longer exhaust memory. Applied caps at the untrusted-JSON callers: vulnerability_scanner fetchGet/fetchPostJson (256 MiB) and package_internalization makeVersionRequest (32 MiB); binary installer downloads left uncapped (size-bounded by the package). (port_scanner:341) implemented maxConcurrent: new scanPortsConcurrently probes ports in batches of clamp(maxConcurrent,1,256) via std::async, joining each batch before the next so portScanned/scanProgress still emit from the calling thread in a stable order (was strictly serial, field ignored). (ethernet_config_manager:102) captureSettings now populates snapshot.macAddress via new static lookupAdapterMac (QNetworkInterface matched by human-readable/system name) instead of running `netsh interface show interface` -- which does not even return a MAC -- and discarding its output. CERT: test_ethernet_config_manager (wired orphan) 17/17 incl. lookupAdapterMac unknown-empty + real-adapter-MAC-format; test_network_diagnostic_utils 52/52 incl. live localhost batched-concurrency scan returning every requested port; test_bandwidth_tester 17/17; test_vulnerability_scanner 11/11; test_package_list_manager 25/25 -- all Release offscreen; lizard+clang-format+cppcheck pass.
- [x] CERT B9: firewall-fail-closed + wifi per-BSSID + bounds + injection tests; suite green. DONE 2026-07-31: full Release offscreen `ctest` = 100% pass, 199/199 (grew 192->199 this session by wiring 7 previously-orphaned test targets: test_wifi_analyzer, test_bandwidth_tester, test_connectivity_tester, test_dns_diagnostic_tool + earlier test_recycle_bin/test_imap_uploader/test_firewall_rule_auditor/test_ethernet_config_manager). Covers firewall fail-closed + rulesConflict, wifi per-BSSID deriveBssSecurity (incl. evil-twin-open-stays-insecure), connectivity sanitizeConfig bounds, DNS record-type/HTML-injection escaping, iperf parse-not-a-success, port maxConcurrent concurrency, ethernet MAC lookup, transfer byte cap.

## BATCH 10 -- Apps / supply-chain
- [x] B10-01 [CRIT] src/core/windows_usb_creator.cpp:103 -- (see B2-04). DUP: already fixed+certified as B2-04 (guardTargetDiskSafe preflight, fail-closed). Verified present in current tree.
- [x] B10-02 [CRIT] src/core/flash_coordinator.cpp:384 -- (see B2-05). DUP: already fixed+certified as B2-05 (physicalDriveHostsSystemVolume blocks OS-disk targets). Verified present.
- [x] B10-03 [CRIT] src/core/leftover_scanner.cpp:891 -- (see B2-06). DUP: already fixed+certified as B2-06 (installLocation exempt only as STRICT subfolder of protected root). Verified present.
- [x] B10-04 [CRIT] src/core/advanced_uninstall_controller.cpp:215 -- (see B2-07). DUP: already fixed+certified as B2-07 (GUI path screens every item via leftover_cleanup_item_guard.h before CleanupWorker). Verified present.
- [x] B10-05 [CRIT] src/core/flash_coordinator.cpp:515 -- (see B4-08). DUP: already fixed+certified as B4-08 (refuse-to-stop worker DETACHED, not terminate()'d mid-raw-write). Verified present.
- [x] B10-06 [HIGH] flash_coordinator.cpp:168 -- duplicate target paths not rejected (concurrent same-disk write). Fix: reject dup. WHAT: already implemented (validateTargets rejects via firstDuplicateTarget, case-/whitespace-insensitive) + integration-tested (testStartFlashRejectsDuplicateTargets). This session promoted firstDuplicateTarget from an anonymous-namespace free function to a public static seam FlashCoordinator::firstDuplicateTarget and added a direct pure-seam test (distinct->empty, empty-list->empty, exact-dup->offending path, PHYSICALDRIVE4 vs "  ...drive4 " case+trim still dup). CERT: test_flash_coordinator 14/14 Release offscreen; lizard+clang-format pass.
- [x] B10-07 [HIGH] flash_coordinator.cpp:285 -- signals emitted while m_mutex held; direct slots calling locked getters deadlock. Fix: emit unlocked. WHAT: onWorkerCompleted, onWorkerFailedFor, and updateProgress emitted driveCompleted/driveFailed/stateChanged/flashCompleted/progressUpdated (and called cleanupWorkers, which wait()s up to 15s) WHILE holding the non-recursive m_mutex. A same-thread direct-connected slot that reads state()/progress()/isFlashing() (all QMutexLocker) would re-lock and deadlock (and any getter caller stalled up to 15s during cleanup). HOW: each handler now mutates coordinator state under a scoped QMutexLocker, captures everything the signals need into locals (incl. a terminalMessage + FlashResult copy), releases the lock, THEN emits; new emitTerminalOutcome helper (called unlocked) dedups the terminal stateChanged+flashCompleted+cleanupWorkers tail across both worker handlers; updateProgress emits a progress snapshot copied under the lock. CERT: by inspection (emits provably outside the locker scope; no worker/direct-slot harness exists to drive the handlers) + test_flash_coordinator 14/14 unchanged (no regression); lizard (extracted helper to hold onWorkerCompleted <=70 len) + clang-format + cppcheck pass.
- [x] B10-08 [HIGH] src/core/uup_dump_api.cpp:109 + linux_distro_catalog.cpp:648 -- abort() synchronously removes from list being range-iterated -> iterator invalidation. Fix: snapshot before abort. WHAT: both cancelAll() range-iterated m_pendingReplies while reply->abort() synchronously delivered finished(), whose slot calls m_pendingReplies.removeOne(reply) -- mutating the list mid-iteration (UB). Now each snapshots the QList into a local + clears the member FIRST, then aborts/deleteLater from the copy (the slot's removeOne then no-ops on the empty member). Added a null-guard on the linux path too. CERT: verified via test_linux_distro_catalog (17/17, new testCancelAllWithNoPendingIsSafe exercises the snapshot-clear path + idempotency) + build of test_uup_iso_builder (compiles uup_dump_api); the abort->finished->removeOne race needs a live QNetworkReply so the structural fix is inspection-certified. lizard+clang-format+cppcheck pass.
- [x] B10-09 [HIGH] src/core/advanced_uninstall_controller.cpp:544,447 -- enum shutdown force-terminates, ignores second wait, permits child QThread destroy while running; uninstall completion Idle before thread exits -> next op destroys running worker. Fix: check waits, delay Idle. WHAT: the worker completion slots (onUninstallComplete/Failed/Cancelled, onCleanupComplete/Failed) fire from a WORKER signal delivered BEFORE QThread::run has returned, then setState(Idle) lets the next op reassign the unique_ptr while the thread is still live -- destroying a live QThread = fatal "QThread: Destroyed while thread is still running" abort. HOW: new file-local template retireWorker(unique_ptr<WorkerT>&) -- cooperative requestStop()+bounded wait(kThreadWaitMs); if it refuses, DETACH (release + setParent(nullptr) + QThread::finished->deleteLater, a bounded self-cleaning leak) instead of terminate()+destroy. Called before EVERY m_uninstall_worker/m_cleanup_worker reassignment (uninstallProgram, forceUninstall, removeRegistryEntry, cleanLeftovers, processNextQueueItem batch) AND replacing the terminate()+reset() dance in cleanupWorkers (so the destructor path is safe too). This makes destroying a still-exiting worker impossible even when Idle is set early (covers the "delay Idle" intent via safe retirement rather than restructuring the state machine). Enum-thread stopEnumThread already checked both waits and never deleted a still-running thread (safe; left as-is). CERT: by inspection (retire-before-reassign is structural; the exact completion->reassign race needs a live OS uninstall thread) + test_advanced_uninstall_controller 30/30 unchanged (retireWorker runs on every teardown via ~ctrl->cleanupWorkers); lizard+clang-format+cppcheck pass.
- [x] B10-10 [HIGH] src/core/package_internalization_engine.cpp:599 -- (see B1-07). DUP: already fixed+certified as B1-07 (double apostrophes in single-quoted PowerShell for both extract + repack native paths). Verified present.
- [x] B10-11 [HIGH] package_internalization_engine.cpp:187 -- unvalidated package IDs/versions form extraction/output paths -> traversal/overwrite/recursive cleanup outside work dir. Fix: validate IDs. WHAT: prepareInternalizationPaths concatenated package_id + version into work_dir/extract_dir/nupkg_path (and repack/removeRecursively targets); a crafted id/version with "../", "/", "\\", or a drive colon escaped work_dir. New pure static isSafePackageComponent (rejects empty/"."/".."/ path-separator / ':' / control chars) is now enforced in beginInternalization: package_id validated up front (errorOccurred + refuse) and the RESOLVED version validated after resolution (a hostile feed could return a traversal-bearing version) via finishWithError. CERT: new test_package_internalization_engine (4/4, accepts real dotted ids + semver/prerelease/build-metadata versions, rejects ..\/ separators + drive colon + control chars) Release offscreen; lizard+clang-format+cppcheck pass.
- [ ] B10-12 [HIGH] src/core/offline_deployment_worker.cpp:526 -- traversal-bearing package ID escapes _sak_temp_<id> then removeRecursively deletes escaped target. Fix: validate ID.
- [ ] B10-13 [HIGH] offline_deployment_worker.cpp:140 -- existing <output>/_work reused + recursively deleted without ownership marker. Fix: marker/confirm.
- [ ] B10-14 [HIGH] offline_deployment_worker.cpp:690 -- decoded URL filenames appended without basename/confinement -> encoded traversal outside output_dir. Fix: basename.
- [ ] B10-15 [HIGH] src/core/nuget_api_client.cpp:342 -- fallback filename uses unsanitized package_id -> traversal outside output_dir. Fix: sanitize.
- [ ] B10-16 [HIGH] src/core/script_rewriter.cpp:123 -- replacing only URL text leaves surrounding quotes -> invalid/inert PowerShell. Fix: replace full token.
- [ ] B10-17 [HIGH] package_internalization_engine.cpp:473 -- parsed installer checksums never verified before repack. Fix: verify checksum.
- [ ] B10-18 [HIGH] offline_deployment_worker.cpp:393 -- bundle install ignores manifest filename/checksum/size/internalized fields; invokes bare `choco` (PATH hijack). Fix: verify manifest + use bundled exe.
- [ ] B10-19 [HIGH] src/core/linux_iso_downloader.cpp:284,517 -- SourceForge may downgrade to HTTP + checksum-less entries reported complete; cancellation doesn't abort checksum/hash, new download replaces shared state. Fix: enforce HTTPS+checksum; abort verify.
- [ ] B10-20 [HIGH] src/core/uup_dump_api.cpp:421,384 -- missing/malformed SHA-1 accepted even for HTTP; builder omits integrity verify; invalid entries dropped, partial set emitted success. Fix: require checksum; surface drops.
- [ ] B10-21 [HIGH] src/core/uup_iso_builder.cpp:686,779,805 + uup_iso_builder.h:37 -- aria2 exit 7 proceeds verifying only one file; existing ISO deleted before conversion; converter errorOccurred emits no terminal failure (stuck forever); any-thread contract false. Fix: verify all files; convert-then-replace; handle errorOccurred; marshal.
- [ ] B10-22 [HIGH] src/core/windows_usb_creator_extract.cpp:561 -- missing/failed/timed-out bcdboot treated success; BIOS-only + file-presence checks falsely certify NTFS media UEFI-bootable. Fix: gate on bcdboot.
- [ ] B10-23 [HIGH] src/core/cleanup_worker.cpp:117 -- recycle-bin mode silently falls back to permanent deletion when recycling fails. Fix: surface fallback.
- [ ] B10-24 [HIGH] src/core/vulnerability_scanner.cpp:132,148 -- malformed JSON parses as empty catalog no error (false clean scan); NVD/CISA matching discards version ranges, never compares installed versions. Fix: surface parse error; compare versions.
- [ ] B10-25 [HIGH] src/core/nuget_api_client.cpp:427 -- dependency ranges discarded, non-root deps resolve to latest -> incompatible offline bundles. Fix: honor ranges.
- [ ] B10-26 [MED] app_installation_worker.cpp:102,438 -- maxConcurrent=0 permanent polling, >1 still serial; verification returns success when neither choco nor registry/AppX confirm install. Fix: bound; require confirmation.
- [ ] B10-27 [MED] Data-corruption writers: offline_deployment_worker.cpp:814 (manifest truncate-before-write), script_rewriter.cpp:79 (in-place truncate, no stream check), package_matcher.cpp:724 (mapping export truncate non-atomic). Fix: atomic writes.
- [ ] B10-28 [MED] nuget_api_client.cpp:169,190,604 -- concurrent dep-resolution shares/clears one graph state (mixing); cancel() permanent aborts nothing; framework-qualified parsing records framework token not dep ID. Fix: per-request state; abort replies; fix parse.
- [ ] B10-29 [MED] package_internalization_engine.cpp:129 -- no recognized URLs treated successful internalization. Fix: surface.
- [ ] B10-30 [MED] cleanup_worker.cpp:45 + advanced_uninstall_controller.cpp:267,142 + vulnerability_scanner.cpp:1547,502 -- cancellation reported success; enum cancel Idle-before-stop (stale completes new); autoCleanSafe stored-never-applied; cancel returns full count no cancelled flag; stale cache -> successful transfer no metadata. Fix: per-item.
- [ ] B10-31 [MED] Enumeration/parse: program_enumerator.cpp:125 (empty enum hits non-empty assert -> debug abort); app_scanner.cpp:97 (subkey > buffer stops enumeration silently); iso_analyzer.cpp:518 (El Torito only LBA 17, bootable before validating catalog); windows_user_scanner.cpp:343 (per-folder sizing usually reports zero); image_source.cpp:323 (CompressedImageSource::size returns uninitialized); leftover_scanner.cpp:556 (English-label + naive comma split); linux_distro_catalog.cpp:631 (new release keeps prev checksum URL); chocolatey_manager.cpp:521 (validation permits leading `-` option injection; rejects valid SemVer). Fix: per-item.
- [ ] B10-32 [LOW] Quality/dead: uup_iso_builder.h:225 (m_workerThread unused); flash_coordinator.cpp:234 (setBufferCount stored-not-used); app_scanner.h:129 (parseAppXPackages declared-never-defined); package_matcher.h:64 (MatchConfig::use_cache ignored). Fix: implement or remove.
- CERT B10: supply-chain tests (checksum-required, PATH-hijack blocked, package-ID traversal rejected, bcdboot gate, iterator-invalidation); suite green.

## BATCH 11 -- Partition APFS/HFS write-safety
- [ ] B11-01 [HIGH] src/core/partition_file_system_detector.cpp:2103,2123 + partition_apfs_file_system_reader.cpp:602 + partition_apfs_writer.cpp:4839 -- APFS geometry never reconciled with validated size; commit guard max(claimed,device) WIDENS writable range on oversized claim. Fix: min + reconcile.
- [ ] B11-02 [HIGH] partition_apfs_writer.cpp:5424,5627,5647,5663 -- checkpoint-map entry count consumed + COW-written before capacity check; cpm_size+blockSize-1 overflow. Fix: bound before write.
- [ ] B11-03 [HIGH] partition_apfs_writer.cpp:20924,20927,20932 -- raw format zeroes target before constructing all blocks, proceeds even with blockers, ignores writeImageBlocks failure. Fix: construct-then-write, honor blockers/failure.
- [ ] B11-04 [HIGH] partition_apfs_writer.cpp:4699,4708,20145 -- format writes lack target block-count/byte-range; blockIndex*blockSize can wrap past container. Fix: pass+enforce range bound.
- [ ] B11-05 [HIGH] partition_apfs_writer.cpp:743,6535,7206,8773 -- boundedNodeKeyCount hardening incomplete; extent-ref/fs-tree/free-queue/checkpoint walkers still trust raw counts -> runaway loops. Fix: bound all walkers.
- [ ] B11-06 [HIGH] include/sak/partition_hfs_internal.h:8180,8187,8195 -- HFS+ alternate-volume-header sync failure warning-only; caller sets ok=true -> stale backup header. Fix: fail on sync error.
- [ ] B11-07 [HIGH] src/core/partition_raw_device_io.cpp:255,270 -- unaligned raw write clamps scratch to DWORD_MAX but copies original maxSize+prefix -> heap OOB (needs >4GiB unaligned; latent). Fix: clamp copy length.
- [ ] B11-08 [HIGH] partition_raw_device_io.cpp:461,478 -- POSIX sparse copy treats every SEEK_DATA failure as EOD; EINVAL/EIO -> success with hole-filled dest (POSIX-only). Fix: distinguish errno.
- [ ] B11-09 [HIGH] src/core/partition_manager_controller.cpp:47 + partition_file_system_tool_runner.cpp:374 -- missing app-dir manifest -> CWD manifest becomes root of trust (self-hashed). Fix: require app-dir manifest.
- [ ] B11-10 [HIGH] src/core/partition_executor.cpp:152,163,231 -- execute() clears pre-dispatch cancel; cancel between loop check and setActiveBroker finds null broker -> destructive command still launches. Fix: honor pre-dispatch cancel.
- [ ] B11-11 [MED] src/core/apfs_keybag.cpp:62,91,151 + apfs_crypto.cpp:269 -- DER long-form len signed shift/add overflow before bounds check; PBKDF2 iterations unbounded from DER -> BCrypt hang; parseKeyBlob null-deref + stale wrappedKey; outer HMAC never verified. Fix: bound iterations, guard null, verify HMAC. (PARTIAL: some already guarded.)
- [ ] B11-12 [MED] src/core/encryption.cpp:75,194,306,400 -- EncryptionParams unvalidated (1 iter/short key/negative->huge); empty-plaintext round-trip broken. Fix: validate params; handle empty.
- [ ] B11-13 [MED] src/core/partition_manager_controller.cpp:263,355 -- applyIsRunning() uses isFinished() (flips before queued slot); double-apply -> old callback resets new executor. Fix: explicit running flag.
- [ ] B11-14 [MED] CLI DoS/secret: sak_hfs_writer_cli.cpp:208,581 (--name-pad unbounded QString), apfs:93/hfs:45 (readAll before 64MiB cap), sak_apfs_writer_cli.cpp:314,471 (invalid xattr/sparse silently mangled), :400,1875 (password on cmdline, unwiped QString), :1567 (raw resize same value, no-op). Fix: bound; validate; secure secret.
- Lower: partition_file_system_tool_runner.cpp:156 (fsck `-` target no `--`); partition_ext_file_system_reader.cpp:243,345 (trim removes valid leading/trailing space; duplicated warnings); secure_memory.h:121 (secure_buffer<T> non-trivial UB, only trivial instantiated); payloadUInt64 unchecked double->uint64 cast (script_builder:137, safety_validator:238); HFS attribute predicate returns true on scan-fail (:9563,9580).
- CERT B11: APFS/HFS range-bound + walker-bound + DER + cancellation-window regression tests; Mac-cert unaffected; suite green.

## BATCH 12 -- AI harness
- [ ] B12-01 [CRIT] src/ai/ai_orchestrator.cpp:511,642 + ai_orchestrator.h:38 -- workflows have no session-policy ceiling; catalog agent policy passed verbatim -> Chat/Research workflow gains package/mutating capability. Fix: clamp to session ceiling.
- [ ] B12-02 [HIGH] src/ai/ai_tool_policy.cpp:216,368 -- ReadOnlyPc fail-open blacklist misses redirection/reg add/sc stop/taskkill/shutdown/schtasks -> execute without lease/confirmation. Fix: allowlist or complete denylist.
- [ ] B12-03 [HIGH] src/ai/ai_workflow_placeholders.cpp:81 -- (see B1-05).
- [ ] B12-04 [HIGH] include/sak/ai/ai_lease_manager.h:26 -- lease TTL 3600s < 7200s max op -> second mutating op reclaims live lease -> concurrent mutation. Fix: TTL >= max op.
- [ ] B12-05 [MED] src/ai/ai_provider_gateway.cpp:693,695,705 -- WIN32_MCP_SECURITY_PROFILE + REDACT_SENSITIVE_OUTPUT set but never read by server -> full catalog advertised, raw sensitive output reaches model. Fix: consume env in server.
- [ ] B12-06 [MED] src/ai/ai_subagent_runner.cpp:424,520 + ai_subagent_runner.h:35 -- per-task timeout_seconds parsed never enforced; wall-clock can't interrupt sync call. Fix: enforce timeout.
- [ ] B12-07 [LOW] src/ai/ai_openai_model_client.cpp:162 + ai_mcp_http_client.cpp:214 + ai_mcp_stdio_client.cpp:295 -- unconditional semaphore acquire after QThread::start(); thread-create failure hangs forever. Fix: timed acquire / check start.
- [ ] B12-08 [LOW] src/ai/ai_provider_gateway.cpp:618,640 -- list_processes/kill_process/start_process classified but absent from manifest (dead). Fix: remove.
- CERT B12: policy-ceiling + lease-TTL + placeholder-injection tests; suite green.

## BATCH 13 -- win32 / browser control
- [ ] B13-01 [HIGH] src/win32mcp/win32_mcp_capture.cpp:107 + win32_mcp_ocr.cpp:228 + win32_mcp_geometry.h:12 -- ocr_region allocates full 16384x16384x4 (~1GiB) 32bpp surface before downscale. Fix: cap capture dims pre-alloc.
- [ ] B13-02 [MED] src/ai/ai_mcp_http_client.cpp:144,167 -- HTTP MCP replies no streaming cap; 1MiB limit applied only after full buffer. Fix: setReadBufferSize + incremental abort.
- [ ] B13-03 [MED] src/win32mcp/browser_extension_installer.cpp:486,489,499,509 -- uninstall ignores RegDelete/QFile::remove failures, always ok=true. Fix: surface failures.
- [ ] B13-04 [MED] src/win32mcp/browser_bridge.cpp:138 + browser_bridge_relay.h:37 + browser_control.cpp:38 -- onDetached() has no production caller; refs stay live after external navigation/tab-close/CDP-detach. Fix: wire detach event.
- [ ] B13-05 [MED] src/win32mcp/browser_bridge_pipe.cpp:219,267,276 -- 2nd start() move-assigns joinable std::thread -> std::terminate; stop() under once_flag disables later teardown. Fix: guard restart.
- [ ] B13-06 [MED] src/win32mcp/win32_mcp_entry.cpp:53 -- any chrome-extension:// arg selects relay mode; origin never validated vs pinned ID. Fix: validate origin.
- [ ] B13-07 [LOW] src/win32mcp/win32_mcp_ocr.cpp:220,228 -- signed x+width / y+height overflow UB before validation. Fix: checked arithmetic.
- [ ] B13-08 [LOW] src/win32mcp/browser_bridge_relay.cpp:221 + browser_bridge_relay.h:64 -- relay handshake accepts any welcome, never checks protocol value. Fix: verify protocol.
- [ ] B13-09 [LOW] src/ai/ai_mcp_http_client.cpp -- (buffer cap, see B13-02).
- [ ] B13-10 [LOW] Semaphore-hang (see B12-07) shared with MCP clients. Fix: with B12-07.
- CERT B13: capture-cap + uninstall-failure + relay-protocol tests; suite green.

---

## FALSE-POSITIVES (verified codex-wrong; do NOT fix)
- pst_parser.cpp:1454 visited-set "by value" -- member is a REFERENCE (pst_parser.h:265); fan-out detection intact.
- pst_parser.cpp:1005 negative attachment index OOB -- dominated by readAttachmentData success gate; callers pass >=0.
- generate_system_report_action.cpp:229 BiosSeralNumber -- real (typo'd) PS 5.1 Get-ComputerInfo property; correct spelling returns empty. Correct as written.

## PARTIAL / ACCEPTED (documented tradeoffs -- confirm no change needed during batch)
- partition clone zero-offset overwrite gated by target_wipe_confirmed + Assert-SakRawWriteTarget (still hardening via B2-03).
- VSS-snapshot fallback to live copy: by-design, manifest-verified (still B2-14 for consent).
- quick_action_controller.cpp:251 require_confirmation delegated to GUI (still tighten B3-08).
- partition_apfs_writer raw-format surfaces failure (ok=blockers.isEmpty).
- LPE elevated pipe = defense-in-depth only, not exploitable (still B5-04).
- ImapUploader = dead code, unwired (B9-01..05 still hardened before wiring).
- disk-selection GUI-gated by isSystemDrive (engine still single-layer -> B2-04/05).
