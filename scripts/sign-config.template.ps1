# ============================================================================
# sign-config.template.ps1 — Signing configuration template
#
# Copy this file to sign-config.ps1 (same directory) and fill in your values.
# sign-config.ps1 is listed in .gitignore and will never be committed.
# ============================================================================

# Your Azure Subscription ID (find it in the Azure Portal under Subscriptions)
$SubscriptionId = "YOUR-SUBSCRIPTION-ID-HERE"
